# Spectra removal - completed

Spectra has been removed from the maintained dependency graph. This file used to be
an execution contract for that migration; the operational instructions and progress
counts have been removed so they cannot be replayed against the current tree.

Current policy is in `AGENTS.md`. Provenance is preserved in
`retired/Spectra.UPSTREAM.md`, `dev/tauceti/spectra-provenance-map.md`, and source
module headers.

The original removal plan remains available in Git history.
