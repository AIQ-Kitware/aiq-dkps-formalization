# local/ is a forensic snapshot of this machine's Claude Code session store:
# ~1.5G of raw transcripts (including unrelated projects) plus account metadata.
# It must never be committed. Everything else here is small, derived and shared.
local/

# Joint outputs are regenerated by tools/merge_findings.py on whichever machine
# does the merge; keep them out of the per-agent commits to avoid conflicts.
JOINT-ANALYSIS.md
joint-metrics.json
joint-prompts.jsonl
