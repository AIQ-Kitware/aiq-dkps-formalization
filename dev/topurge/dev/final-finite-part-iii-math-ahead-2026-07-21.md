# Final finite Part III mathematics-ahead closure

Base: `3aeeefa0c26e5fa1292223a4257ea60ebddf4e4a6`.

This batch supplies complete candidate mathematics for the three remaining finite roots. It does not claim compiler certification.

## Generalized residual theory

The nonorthonormal trial map is whitened by the positive Gram square root. The exact residual factorization is proved in those coordinates. Arbitrary separated spectra use the entrywise Frobenius Sylvester estimate with constant one; the nuclear fallback follows from finite Cauchy--Schwarz. Continuation statements consume the existing quantitative common-contour witness instead of manufacturing Riesz projections from endpoint data.

## Planar sharpness

The standard planar basis, rotated line, projections, and four model perturbations are computed explicitly. Reusable two-dimensional singular-value lemmas recover singular values from a Gram diagonal or from Gram trace and determinant. The sine, tangent, sin two theta, and tan two theta equality models are proved at their mathematically correct norm endpoints. In particular, the symmetric sin two theta perturbation gives operator-norm sharpness only; its arbitrary-UI claim was false because its rank multiplicity differs from the one-sided angle map.

## Direct rotation

The direct rotation is the unitary polar factor of `S = P_V P_U + P_{V orthogonal} P_{U orthogonal}`. The full cosine is `|S|`, not the one-sided `|P_V P_U|`. The partial complex structure is `(R-|S|)|P_U-P_V|+`, and the kernel-factorization theorem gives `R = |S| + J|P_U-P_V|`.

Davis--Kahan Proposition 4.3 is reconstructed by Fan--Hoffman majorization of the Hermitian part of the pinched competitor. Proposition 4.4 uses the real principal-plane proof and is intentionally restricted to real spaces, as the source paper gives a complex counterexample. The original `FiniteTwoProjection` proof bodies are archived verbatim under `dev/retired-full-part-iii-ambient-route-2026-07-21/`.

## Compiler boundary

The compiler agent should repair names, coercions, imports, and tactic details. It should not restore the fictional principal-plane namespace, the one-sided full cosine formula, the complex Proposition 4.4 statement, or the vacuous root registry.
