# Lane claims

Coordination ledger for parallel agents working this repo. Purpose: avoid the
duplicated-effort collisions that occurred on the Sylvester analytic lane
(2026-07-23), where two agents independently proved the same five theorems.

**Identity note (2026-07-23):** agents are identified by their *human*, not by
the model running them (models change mid-session). Rows previously labeled
`opus` were jon's agent; rows labeled `fable` were edward's agent. Edward's
agent stopped after the circle-Riesz definitional layer; jon's agent (also
running Fable 5) inherited and completed that lane. Edward's agent has since
RESUMED (2026-07-23) and claims lanes under `edward (resumed)` below.

## Rules

- **Claim before the first edit.** Add/refresh your row below, commit it, then work.
- A claim covers the **listed declarations**, not the whole file. Two agents may
  work different declarations in one file if both are listed.
- **Release explicitly** when done (set status `done` or remove the row).
- **Unlisted = unclaimed.** If you want something not listed, add a row first.
- If two claims would overlap, the earlier-committed row wins; the other agent
  picks something else.

## Format

`agent | file(s) | declarations | date | status`

## Session state (2026-07-27 evening) — read this first

**Every row in the table below is `done`. No lane is held by anyone.** Edward's agent
finished for the day and released all of its claims; jon's agent stopped after the doc pass
that produced this note.

**Active track is Tau Ceti API polish. Finishing the paper mathematics is ON HOLD**
(jon's instruction, 2026-07-27). This is safe because every production tree is already
sorry-free — `DavisKahan/Frontier`, `DavisKahan/Sources`, `ForTauCeti`, `ForMathlib` and all
four paper libraries. The 54 remaining sorries live entirely in `DavisKahan/Experimental`,
which is deliberately outside `defaultTargets`: 21 in `Frontier/Section9Analytic.lean`
(needs interval Sobolev/Rellich, absent from pinned Mathlib), 9 `DirectRotation`,
8 `SinTheta/General`, 4 `Ideals/Rectangular`, 4 `Experimental/Frontier/Section3`, 8 elsewhere.
Treat all of those as out of scope until told otherwise.

**Verified green at hand-off:** default build 9274 jobs · `lake build Challenge` 8891 ·
`scripts/check_dependency_layers.py` OK (690 modules) · source census CLEAN (48) ·
`scripts/check_declaration_name_drift.py` 0 findings · `lake build ForTauCeti` 2 warnings,
both unused simp arguments on `ApproximationNumber/MinMax.lean:165` (see cross-lane findings).

### Where the polish backlog actually stands

`dev/tauceti-signature-polish-todo.md` sections 6, 7, 8, 9, 10, 11, 12.1 and 13 are RESOLVED
and pruned. What is genuinely left:

- **§5 approximation numbers — CLOSED. Do not re-audit it.** The bullet that used to sit
  here recorded a doc/tree divergence; that divergence was worked off item by item on
  2026-07-27, and §5 of `dev/tauceti-signature-polish-todo.md` now opens with a
  RECONCILED banner giving every subsection its disposition. **Re-verified against the tree
  2026-07-28** (jon/namek): `rank_comp_left_le_of_rank_le` is gone from `Basic.lean` — it
  could not be privatized (four consumers) so it moved out and generalized to
  `LinearMap.rank_comp_le_natCast_right` in `ForTauCeti/LinearAlgebra/Dimension/RankComp.lean`,
  and the only surviving mention is the *provenance record* that says so; the codomain is
  `approximationNumber … : ℝ` at `Basic.lean:143`; `approximationNumber_adjoint` carries its
  `@[simp]`. §5.1–§5.4 are all closed.
- **§12.2 ClosedOperator → LinearPMap (the U1 lane) — OPEN, released mid-migration
  2026-07-28.** The representation decision is forced by Mathlib/Tau Ceti and is no longer
  design-stage: use `LinearPMap` plus property predicates, preserve green builds through a
  temporary downstream adapter, migrate production consumers, and demote/delete the bundled
  DKPS foundation. The canonical `ForTauCeti` layer is **built and dependency-clean**, and a
  large share of consumers are migrated; what remains is **171 type-position `ClosedOperator`
  uses in 18 production modules, 41 of them already documented facades**. The full measured
  hand-off — per-module counts, what is contractible today, what is out of scope — is in
  `dev/tauceti/u1-linearpmap-migration.md` under "Release state"; read it before reclaiming.
  **§12.3–12.7 remain design-stage** until their own canonical representations are settled.
- **§13 adapter retirement — two of the three shims are already gone.** `operatorAbs` and
  `CourantFischerCompat` were both deleted (no `.lean` file anywhere in the tree names either;
  the only hit is a note in `dev/alternates/` recording the retirement). What is left is
  **`SymmetricOperatorIdealFamily.toRectangular`**, i.e. §13.2 phases C and D: retype the ~25
  ideal-parameterized sin-Θ theorems over the canonical family, then delete
  `RectangularSymmetricIdealFamily` and the adapter. Phase C was released un-started on
  2026-07-28 for an ordering reason that **no longer applies** — its chain reached the U1
  boundary, and U1 has since released. It should now be taken as one sweep, root-outward from
  `DavisKahan/Sylvester/Bounded.lean`. This is the largest genuinely-open item in the backlog.

### Conventions worth not rediscovering

- A rename must build `Challenge` explicitly and run
  `scripts/check_declaration_name_drift.py`; `Challenge` is outside `defaultTargets` and
  `comparator/*.json` stores declaration names as data. See AGENTS.md, "Comparator challenge rule".
- Status cells in the table below are prose. Three completed rows had to be prefixed with an
  explicit `done —` marker because an automated scan otherwise read them as open; keep new
  rows starting their status cell with `done` or `claimed`.
- Git identity for agent commits in this repo is `Jon Crall (Agent) <jon.crall@kitware.com>`,
  set repo-locally.

## Claims

> **Merge hazard, seen 2026-07-28.** Resolving a `dev/LANES.md` conflict by
> mechanically keeping *both* sides **duplicates a row** whenever the same lane
> was updated on both branches — you end up with the old `claimed` copy sitting
> next to the new `done` copy, and the stale one reads as an active claim that
> blocks other agents. Three rows were duplicated this way before it was caught.
> When both sides carry the *same* lane, keep the side with the more advanced
> status; keep both sides only when they are genuinely different lanes.


| agent | file(s) | declarations | date | status |
|-------|---------|-------------|------|--------|
| edward | `DavisKahan/DoubleAngle/TanTwoThetaKyFanInfinite.lean` → **renamed** `DavisKahan/DoubleAngle/TanTwoThetaKyFanFiniteCarrier.lean` (three theorem renames + module docstring); the two import lines that name it (`DavisKahan/DoubleAngle/All.lean`, `DavisKahan/Sources/DavisKahan1970/TanTwoTheta.lean`) and the three call sites there; `dev/TauCeti_theorem_naming_strength_audit_2026-07-27.md` §5. **Does NOT touch** `DavisKahan/DoubleAngle/{Unbounded,UnboundedIdeal}.lean` or `DavisKahan/TanTwoTheta/**` (recently jon (namek)'s sine-two-theta lane, now done — but staying clear anyway), `DavisKahan/SinTheta/**`, `DavisKahan/OperatorIdeal/**` (namek's Ky Fan area), `comparator/*.json`, or `Challenge/**`. | **Naming-strength audit item 5: `_infinite` names a theorem that is not the infinite-dimensional one.** Rated *"Rename before upstreaming this theorem family"*, and the file confirms the diagnosis in its own words. The section carries `variable {U : Submodule 𝕜 E} [FiniteDimensional 𝕜 U]`, and the Method paragraph opens "Everything happens inside the finite-dimensional carrier `M := U ⊔ T '' U`". So the ambient Hilbert space is arbitrary, but the *active configuration* is finite-dimensional and the proof is a compression to `M` followed by transport back. A reader seeing `_infinite` and a module titled "infinite dimensions" will take these for the unrestricted theorem; they are an ambient-space lifting of a finite one. **Plan.** Three renames — `kyFan_doubleAngleTangent_offDiagonal_le_infinite`, `kyFan_tanTwoTheta0_offDiagonal_le_infinite`, `tanTwoTheta0_offDiagonal_mem_and_gauge_le_infinite` — plus the module rename to `TanTwoThetaKyFanFiniteCarrier.lean`, which is the audit's own suggestion and matches the Method paragraph exactly. **One deviation from the audit's table, and it is deliberate:** it proposes the suffix `_of_finiteDimensional_invariantGraph`, adding "if the actual finite object is named differently in the theorem signature, substitute that exact mathematical noun". It is: the hypothesis is literally `[FiniteDimensional 𝕜 U]` on the **invariant subspace** `U`, not on a graph object — the graph of `T` is what makes the carrier `M`, but nothing named `invariantGraph` appears. So the suffix will be `_of_finiteDimensional_invariantSubspace`. **Scope check:** 8 theorem references across 2 files, 2 import lines, and no `comparator/*.json` pins — by far the smallest of the five audit items, and the only remaining one that does not touch a high-collision tree. (Item 4, deleting the `UnitaryInvariantIdealFamily` alias, is **94 references across 30 files** including `SinTheta/**` and `Sources/**`; it is mechanical but large and I am deliberately not bundling it here.) | 2026-07-28 | claimed |
| edward | `ForTauCeti/Analysis/InnerProductSpace/PolarDecomposition.lean` (rename two declarations, add one theorem); the seven consumer files that name them — `ForTauCeti/Analysis/InnerProductSpace/{SingularSubspace,PrincipalAngles,KyFan,UnitarilyInvariantNorm,AlignedBasis}.lean`, `DavisKahan/{Specialized/Statistics,FiniteDimensional/Sylvester/Interval}.lean`; `dev/TauCeti_theorem_naming_strength_audit_2026-07-27.md` §2. **Does NOT touch** `ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/KyFan.lean` or `DavisKahan/OperatorIdeal/ApproximationNumbers/{Core,Real/KyFanGauge}.lean` — jon (namek)'s Ky Fan real/complex twins row. **Note the near-miss:** my list includes `ForTauCeti/Analysis/InnerProductSpace/KyFan.lean`, which is a *different file* from namek's `ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/KyFan.lean`; same basename, different directory, no overlap. Also does not touch `DavisKahan/Sources/**`, `comparator/*.json`, or `Challenge/**`. | **Naming-strength audit item 2: `polarUnitary` implies a canonicity the construction does not have.** Rated *"Rename before the polar-decomposition PR"*, and reading the implementation confirms it. `polarUnitary A : E ≃ₗᵢ[𝕜] E` is built by `LinearIsometry.extend` applied to the polar factor restricted to `(ker A)ᗮ`. For singular `A` that extension is an arbitrary choice: any unitary from `ker A` onto `(range A)ᗮ` completes it, and Mathlib's `extend` merely picks one. So `A = U |A|` holds, but `polarUnitary A` reads as *the* polar unitary of `A` when there are many. The module's own docstring says "the kernel-completed unitary", singular definite article and all. **Plan.** Rename `polarUnitary → choosePolarUnitary` and `polarUnitary_apply_abs_apply → choosePolarUnitary_apply_abs_apply`, per the audit's preferred spelling. Add the statement the audit says should be primary — `exists_polar_decomposition_unitary : ∃ U : E ≃ₗᵢ[𝕜] E, A = (U : E →ₗ[𝕜] E) ∘ₗ abs A` — so downstream users who only need *some* unitary factor can take it without touching the choice, and keep `polar_decomposition_unitary` as the concrete-witness form under a name that says which witness. **One deviation from the audit, stated up front:** it also floats renaming `polarUnitaryEquiv` to `polarUnitaryEquivOfIsUnit`, while saying it "may remain". I intend to leave it. It takes `(hA : IsUnit A)` as an explicit argument, so the material hypothesis is already visible in the signature, and `_of_` suffixes on *definitions* (as opposed to theorems) are not the Mathlib convention. That one is genuinely canonical — the invertible polar factor is unique — so the bare name is honest. **Gate note:** this is a rename, so per AGENTS.md it must build `Challenge` explicitly and run `scripts/check_declaration_name_drift.py`. Pre-checked: **no `comparator/*.json` config pins any of these four names**, so the immutable-conformance rule does not bite; 40 `polarUnitary` references across 8 files. | 2026-07-28 | done — **renamed, and the honest statement added.** `polarUnitary → choosePolarUnitary`, `polarUnitary_apply_abs_apply → choosePolarUnitary_apply_abs_apply`, `polar_decomposition_unitary → polar_decomposition_choosePolarUnitary`, plus the new `exists_polar_decomposition_unitary`. The section docstring now says the kernel completion is a **choice** and points at `polarUnitaryEquiv` for the invertible case where the factor genuinely is unique — that sentence is the actual deliverable, since the old text said "the kernel-completed unitary", definite article and all. **The recommendation I did not take, and why:** the audit floats renaming `polarUnitaryEquiv → polarUnitaryEquivOfIsUnit` while saying it "may remain". Left alone. It takes `(hA : IsUnit A)` as an explicit argument so the hypothesis is already visible; `_of_` suffixes on *definitions* rather than theorems are not the Mathlib convention; and that factor is canonical, so the bare name is honest. Recorded in §2 of the audit rather than silently skipped. **One thing worth knowing for the next rename in `ForTauCeti`:** the longer name pushed **four** lines past 100 characters in `AlignedBasis.lean` and `PrincipalAngles.lean`, and because `ForTauCeti` runs with `warningAsError` on, those came through as hard build **errors**, not warnings. Budget for rewrapping when a rename lengthens a name inside a `∑`/`⟪⟫` expression. Gates, run per AGENTS.md's rename rule: `lake build ForTauCeti` green with `warningAsError` on · `lake build Challenge` **8827 jobs green** · `check_declaration_name_drift.py` **0 findings** · layers 709/0 · library-structure at baseline. |
| edward | `ForTauCeti/Analysis/InnerProductSpace/MoorePenroseInverse.lean` (delete four declarations); `DavisKahan/FiniteDimensional/Residual/AngleEmbeddings.lean` (rename three theorems and repoint their proofs); `dev/TauCeti_theorem_naming_strength_audit_2026-07-27.md` (mark item 3 resolved and correct one stale sentence). **Does NOT touch** `ForTauCeti/Analysis/OperatorIdeal/**` or `DavisKahan/OperatorIdeal/ApproximationNumbers/{Core,Real/KyFanGauge}.lean` (jon (namek)'s Ky Fan real/complex twins row), `DavisKahan/SinTheta/**`, `DavisKahan/{DoubleAngle,TanTwoTheta}/**`, `comparator/*.json`, or `Challenge/**`. | **Naming-strength audit item 3: `inverseOnRange` describes a domain it does not have.** `dev/TauCeti_theorem_naming_strength_audit_2026-07-27.md` §3 rates it *"Remove or redesign before the Moore--Penrose module is upstreamed"*, and the diagnosis holds: `inverseOnRange A hA : F →ₗ[𝕜] E` is `moorePenroseInverse A` verbatim — a **total** map on all of `F` whose behaviour off `range A` is the pseudoinverse extension, not an inverse. The injectivity argument is named `_hA` and genuinely unused, so the hypothesis is decoration; `inverseOnRange_eq_moorePenroseInverse` and `moorePenroseInverse_eq_inverseOnRange` are both `rfl`, i.e. the tree carries a two-way compatibility pair between a definition and an alias for it. That is exactly the "total definition with junk behaviour outside the valid regime under a name implying a canonical object" the audit's naming policy forbids. **The redesign the audit asks for needs no new definition.** It suggests `linearEquivRangeOfInjective : E ≃ₗ[𝕜] LinearMap.range A` — which is Mathlib's `LinearEquiv.ofInjective` and already exists. So the honest object is available and this lane is a **deletion**, not a construction. **Plan.** Delete `inverseOnRange`, `inverseOnRange_eq_moorePenroseInverse`, `inverseOnRange_comp_eq_id` and `moorePenroseInverse_eq_inverseOnRange`; the one real content, the left-inverse law, already exists as `moorePenroseInverse_comp_eq_id_of_injective`. Rename the three consumers in `AngleEmbeddings.lean` (`tanThetaEmbedding_eq_inverseOnRange`, `..._of_isTransverse`, `tanTwoThetaEmbedding_eq_inverseOnRange`) to `_eq_moorePenroseInverse` and drop the now-redundant rewrite step from each proof. Checked: **no `comparator/*.json` config and nothing under `Challenge/` pins any of these names**, so the immutable-conformance rule does not bite. **Also correcting a stale line in the audit itself:** §3 closes with "the name does not claim that all four Penrose equations have already been proved" — since that was written, jon (namek) proved all four plus uniqueness, so the caveat no longer applies and the module is stronger than the audit records. | 2026-07-28 | done — **deleted, and the consumers turned out to be weaker than the audit thought.** Four declarations gone from `MoorePenroseInverse.lean`; the real content, the left-inverse law, already existed as `moorePenroseInverse_comp_eq_id_of_injective`. **The correction worth recording:** the audit says to *rename* the three `AngleEmbeddings` consumers. They should be **deleted**, and I did. Since `inverseOnRange A hA` was a definitional alias for `moorePenroseInverse A`, and `tanThetaEmbedding` is *defined* by `moorePenroseInverse`, all three "compatibility" theorems were restatements of `rfl` — renaming them would have preserved three content-free lemmas under better names. The one genuinely useful fact buried in them, the translation of transversality into injectivity, is now stated on its own as `cosThetaMagnitude_injective_of_isTransverse`. Net: 7 declarations out, 1 in. Also: the audit's suggested replacement `linearEquivRangeOfInjective` did not need building — it is Mathlib's `LinearEquiv.ofInjective` — so the redesign was a pure deletion. §3 of the audit is updated with both corrections and marked resolved. Gates: `lake build ForTauCeti` green with `warningAsError` on; layers 710/0; name-drift 0 (7092 declarations indexed, down from 7098); library-structure at baseline. **Unrelated finding, flagged not fixed:** `python3 scripts/export_for_tauceti.py --check` reports **109 errors, every one of them `target missing`** — the exported files exist only in the `external/TauCeti` **submodule working tree**, so the green `--check` recorded on the extraction-manifest row below does **not** survive into another agent's checkout. Nothing to do with this lane; it means that gate is currently checkout-local and someone should decide whether the submodule pin is supposed to carry the exports. |
| edward | `DavisKahan/Interop/Spectra/CircleRieszProjection.lean` (split the `variable` block; **no statement changes, no renames**), `DavisKahan/SpectralTheory/CircleRieszEndpoints.lean` and `DavisKahan/Sylvester/RosenblumExistence.lean` (both mine, landed today — weaken their ambient typeclass to match). **Does NOT touch** `DavisKahan/Sources/DavisKahan1970/Section8RieszCircle.lean` beyond whatever the weakened definition forces mechanically (its own results genuinely need `IsSelfAdjointOperator` and stay inner-product), `DavisKahan/SinTheta/**`, `DavisKahan/{DoubleAngle,TanTwoTheta}/**`, `ForTauCeti/Analysis/OperatorIdeal/**` (jon (namek)'s Ky Fan row), `comparator/*.json`, or `Challenge/**`. | **~30 contour-integral results carry an inner product they never use — the first thing an upstream reviewer will ask about.** `circleRieszProjection` is declared under `[InnerProductSpace ℂ H]` in `Interop/Spectra/CircleRieszProjection.lean`, sharing one `variable` block with `CircleSeparatesRealSpectrum`, which *does* need it (it is stated in terms of `IsSelfAdjointOperator` and the **real** spectrum). But the projection itself is `(2 π i)⁻¹ ∮ Ring.inverse (z • 1 - A) dz` — pure Banach-space Cauchy theory, no inner product anywhere in the definition or in any proof about it. The two Rosenblum modules landed today inherited the restriction wholesale: `CircleRieszEndpoints` (9 declarations) and `RosenblumExistence` (~20, including `sylvesterEquiv` and the bounded-inverse estimate) are all stated for Hilbert spaces despite every proof going through `Ring.inverse`, `DiffContOnCl.circleIntegral_eq_zero`, the annulus deformation and `spectrum.resolvent_tendsto_cobounded` — none of which know what an inner product is. This is precisely the defect `dev/tauceti-signature-polish-todo.md` §2 calls the expected reviewer attack surface, and it is the reason I recorded it as a follow-on rather than shipping it silently. **Plan.** Split the `variable` block so `circleRieszProjection` is declared under `[NormedAddCommGroup H] [NormedSpace ℂ H] [CompleteSpace H]` while `CircleSeparatesRealSpectrum` keeps `[InnerProductSpace ℂ H]`; then relax the two downstream modules to match. This is a **strict weakening of hypotheses**, so every existing consumer keeps working by instance subsumption — the risk is not breakage but instance-resolution surprises at the seam where `Section8RieszCircle` mixes the weakened projection with its own self-adjoint hypotheses, which is exactly where I will check first. Expected: ~30 declarations become Banach-general, and Rosenblum stops being a Hilbert-space theorem, which it never was. | 2026-07-28 | done — **Rosenblum is no longer a Hilbert-space theorem, because it never was one.** The `variable` block in `Interop/Spectra/CircleRieszProjection.lean` is split: `CircleSeparatesRealSpectrum` keeps `[InnerProductSpace ℂ H]` (it is stated in terms of `IsSelfAdjointOperator` and the *real* spectrum, so it genuinely needs it), `circleRieszProjection` moves to `[NormedSpace ℂ H]`, and both `CircleRieszEndpoints` and `RosenblumExistence` follow. **One thing the lane found that was not in the plan:** `ContinuousLinearMap.sylvesterOperator` in `ForMathlib` had the **same defect and it was pre-existing** — the Sylvester operator is a statement about composition and had no business requiring an inner product. It is now over normed spaces too. Because this is a strict weakening, all ~10 existing call sites keep working through `InnerProductSpace.toNormedSpace` and **none changed**. **A practical gotcha for anyone doing a typeclass weakening here:** `lake env lean` reads the *existing* `.olean`s of imports, so until the changed module is rebuilt the downstream file fails with instance-mismatch errors and `whnf` heartbeat timeouts that look like real proof breakage and are not. Rebuild dependencies in order (`lake build <module>`) before believing any error. Gates: `lake build ForMathlib` and `lake build ForTauCeti` both green with `warningAsError` on; layers 710/0; name-drift 0; library-structure at baseline; all twelve headline declarations still axiom-clean. |
| jon (namek) | NEW `dev/tauceti/spectra-to-tauceti-port-ledger.md`; NEW `dev/tauceti/spectra-port-surface.json`; NEW `scripts/ExportSpectraUsage.lean`; NEW `scripts/spectra_port_surface.py`; a pointer paragraph in `dev/tauceti/convergence-matrix.md` §"Seed matrix" and in `dev/tauceti/spectra-provenance-map.md`. **DOCS + MEASUREMENT TOOLING ONLY — no `.lean` production file is edited, no declaration is renamed, moved or deleted.** Does NOT touch `DavisKahan/**`, `ForTauCeti/**`, `ForMathlib/**`, `vendor/**`, `comparator/*.json` or `Challenge/**`. | **Fill convergence-matrix Wave 5 ("Spectra decomposition and porting") to the declaration level, which it explicitly asks for and nobody has done.** Wave 5 names five donor clusters A–E but sizes them from `≈59 direct Spectra import lines`, a textual proxy; the seed matrix carries one row per *cluster*, and its own heading says "to be filled to the declaration level". Nothing in the repo currently answers "which Spectra declarations does production actually consume, and what in Tau Ceti collides with them". Deliverable: the exact port surface measured from the **compiled environment** (`Expr.getUsedConstants` over every declaration whose defining module is a production module, Experimental excluded), each constant assigned a cluster, a Tau Ceti disposition (`collides` / `absent` / `superseded`), and a provenance class; plus the two scripts that regenerate it, so the ledger cannot silently drift from the tree the way the 119-module figure in `spectra-provenance-map.md` did (it is now 152). | 2026-07-28 | done — **the port surface is 61 declarations, not 464 files or the 152-module closure.** Measured from the compiled environment (`Expr.getUsedConstants`), production references **61 `Spectra.*` constants** across **27 donor modules**, consumed by **178 declarations in 42 `DavisKahan` modules**; `ForTauCeti` and `ForMathlib` are **entirely Spectra-free**. Four findings worth carrying forward. **(1) One declaration is a third of the surface.** `Spectra.Resolvent.spectrum` has 75 uses in 26 modules and is a *two-line definition* over `H →ₗ.[ℂ] H` in a 90-line file — but it returns `Set ℝ`, against Mathlib's `spectrum 𝕜 a : Set 𝕜`. All 75 uses inherit that choice, so the codomain has to be settled **before** anything is ported or the 26-module rewrite happens twice. **(2) Spectra's unbounded layer is already on the canonical carrier** — `resolventSet`, `spectrum` and `OneParameterUnitaryGroup.generator` are all stated over `LinearPMap`, and `SelfAdjointOperator` is a structure *storing* one. U1 and the Spectra port do not conflict; but porting that wrapper verbatim would re-introduce exactly the bundle U1 is deleting. **(3) Exactly one genuine upstream collision, and one genuine gap.** Tracked Tau Ceti (629 modules — the working tree's other 83 are our own untracked `ForTauCeti` export, and comparing against the working tree reports our staged work as upstream coverage) collides only at `Analysis/Semigroups/**`: two `generator`s, two `resolvent`s (semigroup-Laplace vs `(A−z)⁻¹`), semigroup-vs-group. DKPS's surface there is 10 constants, dominated by `YosidaHille.genToGroup` (22 uses) — a Stone-theorem-shaped hole, the right size for a focused upstream addition rather than the subsystem transplant Wave 3 warns against. PVM/Borel calculus is absent from Tau Ceti entirely and needs its own roadmap; the two `ForTauCeti` near-misses (`SelfAdjointFunctionalCalculus` is finite-dimensional, `SpectralCutoff` is bounded-positive CFC) must not be mistaken for coverage. Clusters C (polar, 3 constants) and D (Hilbert–Schmidt) collide with `ForTauCeti`, not upstream, and C is nearly closed. **(4) Three provenance hazards, all cheap now and impossible later.** 14 DKPS-authored theorems are declared into `namespace Spectra.*` (11 in `Interop/Spectra/HilbertSchmidtColumnExpansion.lean:44`, 3 in `GapResolvent.lean:54`) — after a port they are indistinguishable from donor material by name; `vendor/Spectra/Spectra/Spaces/Tensor/Hilbert.lean` carries a real edit **not recorded in the managed patch**, against `Spectra.UPSTREAM.md`'s byte-identical rule; and `scripts/{enable,disable}_spectra_lake_dependency.py` still search for the marker `# BEGIN local Spectra development dependency` which `lakefile.toml` has not used for some time, so `disable` silently no-ops, while `scripts/check_spectra_parent_only_bridge.sh` calls two scripts that no longer exist — anyone using these to verify a de-Spectra step gets a false pass. **Drift gate:** the previous figure of record (119 closure modules, `spectra-provenance-map.md`) had drifted to 152 undetected, so the ledger is generated, not hand-maintained — `python3 scripts/spectra_port_surface.py <jsonl> --check` fails when it goes stale. Recommended order in the ledger's closing section; nothing here edits a `.lean` production file. Gates: default build **9290 jobs green**, ledger `--check` clean. |
| edward | NEW `ForMathlib/Analysis/InnerProductSpace/SylvesterOperatorL.lean` (the bundled operator; **additive only** — `ForMathlib/Analysis/InnerProductSpace/SylvesterOperator.lean` keeps its current `def` and every existing call site is untouched); additions to `DavisKahan/Sylvester/RosenblumExistence.lean`. **Does NOT touch** `DavisKahan/SinTheta/**`, `DavisKahan/{DoubleAngle,TanTwoTheta}/**` or `ForTauCeti/Analysis/OperatorIdeal/**` (jon (namek)'s Ky Fan real/complex twins row), `DavisKahan/Sources/**`, `comparator/*.json`, or `Challenge/**`. | **§12.5's bounded-inverse bullet: the Sylvester operator is not bundled, so "invertible" cannot be said.** `dev/tauceti-signature-polish-todo.md` §12.5 specifies `def sylvester (A) (B) : (E →L[𝕜] F) →L[𝕜] (E →L[𝕜] F)` and asks that *algebraic uniqueness*, *bounded inverse*, and *explicit integral solution* be three separate theorems. What exists is `ContinuousLinearMap.sylvesterOperator` (`ForMathlib/.../SylvesterOperator.lean`, its only declaration), a **bare function** `(X : E →L[𝕜] F) : E →L[𝕜] F`, unbundled — so nothing in the tree can state that it is injective *as an operator*, has closed range, or has a bounded inverse; every consumer re-proves estimates pointwise. The integral solution landed today; uniqueness landed in `c447c51`; the bounded inverse is the missing third. **Why it is cheap now:** `rosenblumSolution` is already a two-sided inverse — `comp_rosenblumSolution_sub_comp_eq` is the right inverse and `rosenblumSolution_comp_sub_comp` the left — so bijectivity is in hand and `ContinuousLinearEquiv.ofBijective` supplies boundedness of the inverse through the open mapping theorem, with `F →L[ℂ] E` complete because `E` is. That avoids the one genuinely expensive route, which would be bounding the contour integral by hand to build the inverse as a `LinearMap.mkContinuous`. **Plan.** (1) `sylvesterOperatorL A B : (E →L[𝕜] F) →L[𝕜] (E →L[𝕜] F)` built as `compL 𝕜 E F F A - (compL 𝕜 E E F).flip B`, `RCLike`-general and inside the `ForMathlib` firewall, with `sylvesterOperatorL_apply` and a `rfl` bridge to the existing unbundled `sylvesterOperator` so the two cannot drift. (2) In the Rosenblum module, `sylvesterEquiv` — a `ContinuousLinearEquiv` under circle separation whose `symm` *is* the contour solution — plus the consequences that make it worth bundling: injectivity, surjectivity, and the reverse estimate `‖X‖ ≤ ‖sylvesterEquiv.symm‖ * ‖A X - X B‖`, which is the shape the Davis--Kahan gap bounds actually consume. | 2026-07-28 | done — **the Sylvester operator can now be called invertible, which it could not before.** `ContinuousLinearMap.sylvesterOperatorL` (bundled, `RCLike`-general, added to the *existing* `ForMathlib/.../SylvesterOperator.lean` rather than a new module — one concept, one file) plus `sylvesterEquiv`, `sylvesterEquiv_symm_apply`, `norm_le_mul_norm_sylvesterOperator` and `exists_norm_le_mul_norm_sylvesterOperator` in the Rosenblum module. Six declarations, all axiom-clean; `lake build ForMathlib` green with `warningAsError` on; layers 710/0; name-drift 0; library-structure unchanged from baseline (3/76/3). **Two things worth knowing.** (1) **The bounded inverse cost nothing, because bijectivity was already in hand.** `comp_rosenblumSolution_sub_comp_eq` is the right inverse and `rosenblumSolution_comp_sub_comp` the left, so `ContinuousLinearEquiv.ofBijective` supplies boundedness of the inverse through the open mapping theorem — no hand-bound on the contour integral was needed. `F →L[ℂ] E` is complete because `E` is. (2) **This is what bundling buys and it is worth stating.** The estimate `‖X‖ ≤ K * ‖A X - X B‖` has a constant **uniform in `X`**; the pointwise `∃!` cannot express that, and it is exactly what the gap bounds consume. `sylvesterEquiv_symm_apply` keeps the abstract inverse identified with the explicit contour formula, so the two can never drift. `coe_sylvesterOperatorL` is `rfl` against the pre-existing unbundled `sylvesterOperator`, so **no existing call site changed**. **Follow-ons left unclaimed:** the quantitative form §12.5 also names, `norm_le_inv_gap_mul_norm_sylvester`, is *not* done — the contour gives a uniform `K` but not `K = 1/gap`; the gap-explicit bound already exists separately as the shifted-inverse gauge estimates, and reconciling the two is its own lane. Also still open: generalising `circleRieszProjection` from `InnerProductSpace ℂ` to `NormedSpace ℂ`, which would generalise all of today's Rosenblum results for free. |
| edward (aiq-gpu) | `DavisKahan/SpectralTheory/AbstractSpectrum.lean` (the duplicate `IsUnitaryOperator` definition only — `IsOrthogonalProjection` and the rest of that block are untouched); `DavisKahan/SpectralTheory/Compatibility.lean` (the one re-export line); `DavisKahan/Riccati/UnboundedBasic.lean` (import line only). **Does NOT touch** `DavisKahan/Sylvester/**` or `ForMathlib/**` (edward's §12.5 row), `ForTauCeti/Analysis/OperatorIdeal/**` or `dev/tauceti/spectra-*` (jon (namek)'s rows), `DkpsQuench2026/**` (doop), `comparator/*.json`, or `Challenge/**`. | **`IsUnitaryOperator` is defined twice, with byte-identical bodies, and one copy calls itself provisional.** `TauCeti.DavisKahan.Experimental.Foundation.IsUnitaryOperator` (`SpectralTheory/AbstractSpectrum.lean:47`, docstring: *"Provisional compatibility predicate for a norm-preserving onto operator"*) and `TauCeti.LinearPMap.IsUnitaryOperator` (`ForTauCeti/.../LinearPMap/Closed.lean:505`) are both `(∀ x, ‖W x‖ = ‖x‖) ∧ Function.Surjective W`. A third spelling, `TauCeti.DavisKahanExt.IsUnitaryOperator`, is an abbrev in `Compatibility.lean` pointing at the first. So the repository has **two definitions and three names for one predicate**, and the canonical one — the `ForTauCeti` copy, which `TauCeti.LinearPMap.UnitaryEquivalent` is stated over — is not the one the 50 call sites reach. This is a convergence-matrix *exact-duplicate* row that nothing has claimed. Plan: keep exactly one **definition** (the `ForTauCeti` one), demote the `Foundation` copy to a reducible abbrev over it so all 50 call sites and their anonymous-constructor proofs keep working unchanged, repoint the `Compatibility` re-export, and note the collapse on both declarations. Deliberately **not** a repo-wide rename: the two are definitionally equal, so a substitution across 20 files would be churn with no proof content, and the last repo-wide substitution in this repo surfaced its breakage as *"fail to show termination"* rather than an unknown identifier. Second item, cheap and adjacent: with the bundle gone from Riccati, `UnboundedBasic.lean` references only `TauCeti.LinearPMap.{IsSymmetric,UnitaryEquivalent}` plus Mathlib, so its `import DavisKahan.SpectralTheory.ReducingSubspace.Restriction` — which drags in the whole bundled `ClosedOperator/Basic` — can be narrowed to the `ForTauCeti` module, decoupling the Riccati cluster from the bundled foundation entirely. | 2026-07-28 | claimed |
| edward (aiq-gpu) | `DavisKahan/Riccati/{UnboundedCore,UnboundedTransport,UnboundedBasic,UnboundedReduction,UnboundedExistence}.lean`; `DavisKahan/Experimental/InfiniteDimensional/Riccati/**` (the 9 facade call sites, mechanical); `DavisKahan/Experimental/InfiniteDimensional/Riccati/{UnboundedCoordinateRestrictions,UnboundedDiagonalRestrictions}.lean` (retyped over `LinearPMap`, since the coordinate-restriction development is written only over the bundle and has no raw twin); `FinishTanTwoTheta/FinishTanTwoTheta/DavisKahan/Unbounded.lean` (**signature types only** — 16 binder lines, no proof body changes; claimed by nobody else); `dev/tauceti/u1-linearpmap-migration.md` (Riccati sub-analysis → execution record). **Does NOT touch** `DavisKahan/Sylvester/**` or `ForMathlib/**` (edward's §12.5 bundled-Sylvester row above, and jon (namek)'s ForMathlib gate row), `ForTauCeti/Analysis/OperatorIdeal/**` (jon (namek)'s Ky Fan real/complex twins), `DavisKahan/SinTheta/**` or `DavisKahan/Sources/**` (§13.2 phase C), `DavisKahan/SpectralTheory/**`, `Interop/**`, `comparator/*.json`, or `Challenge/**`. | **U1 Riccati slice — take the release note's own conclusion and execute it, including the Experimental policy call it deferred.** The lane was released twice with the analysis done and the sweep not started, both times for the same reason: the `closedOperatorDirectSum*` facade family is **two production call sites** from deletion (both in `UnboundedReduction.lean`), but 9 further call sites live in `Experimental/InfiniteDimensional/Riccati/**`, which U1's count excludes and which still has to compile — so the deletion was recorded as a *policy* question about Experimental rather than taken. **I am taking the policy call: Experimental migrates with production.** It is mechanical, not mathematical — every one of the 8 facades (`closedOperatorDirectSumDomain{,Fst,Snd,FstLinearMap,SndLinearMap}`, `closedOperatorDirectSumLinearMap`, `unboundedBlockOperatorPMapCore`, `unboundedBlockOperatorCorePMap`) already delegates to `TauCeti.LinearPMap.*` through `A.toLinearPMap`, so there is no proof to redo, only a name to change; and a documented compatibility layer for Experimental would keep the bundle alive in the one tree least able to justify it. Plan, in the order that keeps every intermediate state green: (R1) migrate the 11 facade call sites and delete `closedOperatorDirectSum*` from `UnboundedCore`; (R2) the same for `UnboundedTransport`, which is entirely the `ClosedOperator.pullback` facade over `TauCeti.LinearPMap.pullback*`; (R3) retire `UnboundedBlockData`, `PreservesRiccatiDomains`, `StrongSolvesRiccati` and `ContractiveReducingGraphSelection` in favour of their canonical twins. **Correction to the plan I claimed with, found by reading the tree:** R1 and R2 are *not* separable from R3 the way the release note assumed. `closedOperatorDirectSum` is consumed by the bundled `unboundedBlockOperatorCore`, which is consumed by a **complete parallel bundled stack running five Experimental modules deep** to the public endpoint `complex_unbounded_blockDiagonalization` — so deleting the facade bottom-up would strand exactly the conversion boundary the ordering note warns about. The sweep therefore runs **top-down** (FinishTanTwoTheta → `UnboundedPublic` → diagonal/coordinate restrictions → transport → production core), and the facade deletions fall out at the end. Every statement stays representation-preserving; no Riccati mathematics changes. | 2026-07-28 | done — **the Riccati cluster is completely off the `ClosedOperator` bundle: 48 type-position uses became 0, and `grep -R ClosedOperator DavisKahan/Riccati` now returns nothing.** `DavisKahan/Riccati/UnboundedTransport.lean` is deleted outright (120 lines, a pure `ClosedOperator.pullback` facade with **no production consumer at all**); so are the `closedOperatorDirectSum*` family, the bundled `unboundedBlockOperatorCore`, `UnboundedBlockData`, `PreservesRiccatiDomains`, `StrongSolvesRiccati`, `ContractiveReducingGraphSelection`, `ClosedOperator.UnitaryEquivalent`, and the whole `closedOperatorCoordinate*` development. **Net −800 lines and not one statement weakened.** **The release note's own diagnosis was wrong, and that is the thing worth carrying forward.** It recorded the facade as "two production call sites from deletion" with `Experimental/**` as a *policy* question. The real blocker was structural: the bundled core is consumed by a **complete parallel bundled stack five Experimental modules deep** — `UnboundedRotationTransport` → `UnboundedReductionTransport` → `UnboundedDiagonalRestrictions` → `UnboundedPublic` → `Unbounded` — ending at the public endpoint `complex_unbounded_blockDiagonalization`, plus `FinishTanTwoTheta/DavisKahan/Unbounded.lean`. Every level carried a raw *and* a bundled spelling of the same theorem, so a bottom-up deletion would have stranded exactly the mid-development conversion boundary the phase-C ordering note warns about — which is why two earlier attempts stopped at the measurement. **The sweep has to run top-down**, and did: FinishTanTwoTheta first, then the public modules, then the restrictions, then transport, then the core. **Three findings.** (1) **The bundle was hiding hypotheses the mathematics does not use.** `UnboundedCoordinateRestrictions` (274 lines) took a `ClosedOperator` throughout — i.e. assumed a dense domain and a closed graph — but every declaration in it uses only `D.domain` and `D.toFun`. Over `LinearPMap` it needs **neither**; density and closedness moved to the two theorems that genuinely need them (`coordinateRestriction0_dense`, `coordinateRestriction0_closedGraph`) and are now named arguments. Weakening the hypotheses was a *consequence* of the migration, not a separate goal. (2) **Three declarations were sitting in `namespace ClosedOperator` with nothing to do with closed operators** — `intertwines_orthogonal_projection_of_intertwines_projection`, `map_mem_of_intertwines_projection`, `symm_map_mem_of_intertwines_projection` are pure orthogonal-projection facts. They survive at `DavisKahanExt` level and are plausible `ForTauCeti` material for whoever takes a projection lane. (3) **Two declarations lost an argument**: `unboundedBlockDiagonalRestriction0/1` took the reduction proof `hred` only because the bundled constructor needed it to fill `dense_domain`/`closed_graph`; the `LinearPMap` version does not, so they are now functions of `H` and `X` alone. **One gotcha worth copying:** `rw [map_add]` does **not** fire through the `LinearPMap` `CoeFun` — the goal shows `↑D (a + b)` and the rewrite reports "did not find an occurrence of `?f (?x + ?y)`". Use `LinearPMap.map_add D _ _` directly. That was the single error in the whole sweep. A second, smaller one: `FinishTanTwoTheta` consumed symmetry through the bundled `IsSelfAdjoint.isSymmetric.toLinearMap_inner_eq` chain, so the canonical record needed new `UnboundedBlockData.isSymmetric{0,1}` helpers. **Gates:** `lake build DavisKahan.Experimental.InfiniteDimensional.Riccati.Unbounded` **8927 jobs, 0 errors** (this is the target that exercises every rewritten module); production `DavisKahan.Riccati.*` all green; `check_dependency_layers.py` OK (709 modules, 0 violations); `check_declaration_name_drift.py` 0 findings. **Not yet re-run after this commit:** the full default build, `Challenge`, and the source census — nothing in the lane touches `comparator/*.json`, `Challenge/**`, or any census declaration, but they are the honest gap. |
| edward | NEW `DavisKahan/Sylvester/RosenblumExistence.lean`; possibly one or two endpoint lemmas appended to `DavisKahan/Sources/DavisKahan1970/Section8RieszCircle.lean` (`circleRieszProjection` at `Set.univ` and at `∅`). **Does NOT touch** `DavisKahan/DoubleAngle/**` or `DavisKahan/TanTwoTheta/**` (jon (namek)'s sine-two-theta duplication row), `DavisKahan/SinTheta/**`, `DavisKahan/Sources/**` other than the two endpoint lemmas above, `ForTauCeti/**`, `comparator/*.json`, or `Challenge/**`. | **Rosenblum's half of Sylvester--Rosenblum: the existence theorem, which is absent from production.** Confirmed independently of namek's grep: the only `∃ S` statements under `DavisKahan/Sylvester/` are the shifted-inverse gauge bounds (`ShiftedInverseGauge.lean:42,103`), and the one genuine construction — `sylvesterNeumannSolution` (`Bounded.lean:293`) — solves `A S - S B = C` only under the Neumann smallness hypothesis `‖A⁻¹‖ * ‖B‖ < 1`, **not** under spectral disjointness. So every uniqueness theorem in the file is currently vacuous-safe but unpaired: a reader who takes "Sylvester" to mean existence *and* uniqueness gets half. **Why this is now a lemma rather than the campaign it was billed as.** The obstacle was always the operator-valued contour integral, and it is already built and sorry-free in this repository, one import away: `Section8RieszCircle.lean` has `circleRieszProjectionIntegral`, continuity of the resolvent integrand on a separating circle (`continuous_circleResolventIntegrand`), Cauchy's formula for the scalar integral (`scalar_circleIntegral_resolvent_indicator`), interval-integrability, and the identification of the circle projection with the genuine spectral projection. `CircleSeparatesRealSpectrum A hA B center radius` is exactly the separation hypothesis Rosenblum needs, and instantiating its set parameter at `Set.univ` (all of `spec A` inside the circle) and at `∅` (none of `spec B` inside) supplies the two endpoints. **Plan.** Define `rosenblumIntegrand z := Ring.inverse (z • 1 - A) ∘L C ∘L Ring.inverse (z • 1 - B)` and `rosenblumSolution := (2πi)⁻¹ • ∮_{|z-c|=r} rosenblumIntegrand z dz : F →L[ℂ] E`. The whole proof is one algebraic identity plus two projection evaluations: writing `A (z-A)⁻¹ = z (z-A)⁻¹ - 1` and `(z-B)⁻¹ B = z (z-B)⁻¹ - 1`, the `z`-terms cancel and the integrand of `A S - S B` collapses to `(z-A)⁻¹ C - C (z-B)⁻¹`, whose integral is `P_A C - C P_B = 1 · C - C · 0 = C`. Pulling `A ∘L ·` and `· ∘L B` through the integral is `ContinuousLinearMap.integral_comp_comm` at the bundled `compL` maps. **Deliverables:** the solution, `A S - S B = C` under circle separation, and — pairing it with the disjointness uniqueness landed in `c447c51` — the genuine `∃!`. | 2026-07-28 | done — **Rosenblum's half now exists, and it came out cheaper and more general than the plan.** Two modules, `DavisKahan/SpectralTheory/CircleRieszEndpoints.lean` and `DavisKahan/Sylvester/RosenblumExistence.lean`; six headline results, all axiom-clean, both modules warning-free. **Three things worth knowing.** (1) **The layer rule forced a better design, and it was the right one.** `DavisKahan.Sylvester` is a `generic_davis_kahan_prefixes` entry, so it may not import `DavisKahan/Sources/**` — and the `= 1` endpoint was going to come from `Section8RieszCircle.lean`'s identification of the circle projection with the *measurable* spectral projection, which is Sources and needs self-adjointness. Rather than re-open the `generic_imports_sources` allowlist that jon (namek) had just driven to 0, I proved both endpoints from Cauchy theory instead: `circleRieszProjection_eq_zero` is Cauchy's theorem on the disc, and `circleRieszProjection_eq_one` splits the resolvent at the centre, deforms to a large circle with `Complex.circleIntegral_eq_of_differentiable_on_annulus_off_countable`, and kills the remainder with `spectrum.resolvent_tendsto_cobounded`. **The deformation is the load-bearing step**: the remainder integral is radius-independent, which is what turns "small on large circles" into "zero on this circle". Net effect — the endpoints need **no self-adjointness and no PVM/Borel layer**, so they hold for arbitrary bounded operators on a complex Banach space, which is strictly more than the Sources route would have given. (2) **Existence and uniqueness are literally the same computation.** Off both spectra, `A R = z R - 1` and `T B = z T - 1`, so the `z`-terms cancel in `A (R C T) - (R C T) B = R C - C T`; integrating gives `P_A C - C P_B`. Reading the *same* identity with the data and the unknown exchanged gives `R (A X - X B) T = R X - X T`, which integrates to `P_A X - X P_B = X`. So the Rosenblum integral is a two-sided inverse of the Sylvester operator and `existsUnique_comp_sub_comp_eq` is a genuine `∃!`, not existence bolted onto a separately-proved uniqueness. `circleIntegral_resolvent_sub` is the single integrated step both halves call. (3) **Scope note, stated rather than hidden:** this is the *bounded* Sylvester equation. The uniqueness results in `PairwiseHomogeneousUniqueness.lean` are `LinearPMap`/`ClosedOperator` statements, so the two are not yet plumbed together; the unbounded existence theorem is a separate lane and is **not** claimed here. The ambient spaces are inner-product only because `circleRieszProjection` is declared that way in `Interop/Spectra/CircleRieszProjection.lean` — nothing in either new module uses the inner product, so **generalising that one definition to `NormedSpace ℂ` would generalise all six results for free**; that is a clean unclaimed follow-on. Gates: layers **710 modules / 0 violations**, name-drift **0 findings**, library-structure unchanged on rules 3/4/5 (rule 1 went 5 → 3, the two new modules being wired into `Sylvester/All` and `SpectralTheory/All`). |
| edward (resumed) | `DavisKahan/Riccati/{UnboundedBasic,UnboundedCore,UnboundedTransport}.lean` — **only** the declarations whose signature names `ClosedOperator`; their direct consumers where a type change forces an edit. **Does NOT touch** `DavisKahan/SinTheta/**` or `DavisKahan/Sources/**` (jon (namek)'s §13.2 phase-C row, which explicitly lists `SinTheta/Unbounded/IntervalExterior.lean` — so the two "contractible right now" endpoints named in the U1 release notes are **his**, not mine), `DavisKahan/SpectralTheory/**`, the `Restriction.lean` / `ClosedSylvesterEquation.lean` facade blocks, `Interop/**`, `comparator/*.json`, or `Challenge/**` | **U1 slice — migrate the unbounded Riccati cluster off the `ClosedOperator` bundle.** U1 was released mid-migration by jon (toothbrush) with the remaining work *measured* in `dev/tauceti/u1-linearpmap-migration.md`: 171 type-position uses of the bundle survive, and `Riccati/UnboundedCore` is the single largest un-migrated module at 32 (re-measured today: 34 occurrences, plus 17 in `UnboundedTransport` and 4 in `UnboundedBasic`). This cluster is un-migrated, self-contained, and — checked against every open row — claimed by nobody: namek's phase-C list covers sin-Θ and Sources, not Riccati. Plan: replace the bundled `ClosedOperator` in the Riccati block data, core, and transport records by Mathlib `LinearPMap` plus the closedness/symmetry/self-adjointness properties the staged `ForTauCeti` layer already carries, keeping every statement representation-preserving so no Riccati mathematics changes. Deletion of the `Interop/TauCeti/ClosedOperator.lean` adapter stays last by construction and is not in scope. | 2026-07-28 | **RELEASED — measured, not migrated; take it with the analysis below already done.** Recorded in `dev/tauceti/u1-linearpmap-migration.md` under "Riccati sub-analysis". The headline count overstates the work and misplaces the blocker. **8 of the `UnboundedCore` declarations are pure facades** (`closedOperatorDirectSumDomain{,Fst,Snd,FstLinearMap,SndLinearMap}`, `closedOperatorDirectSumLinearMap`, `unboundedBlockOperatorPMapCore`, `unboundedBlockOperatorCorePMap`) — each already delegates to `TauCeti.LinearPMap.*` via `A.toLinearPMap`, so there is no mathematics to migrate, only callers to move. `UnboundedTransport` (17) is the same shape end to end: it is entirely the `ClosedOperator.pullback` facade over `TauCeti.LinearPMap.pullback*`. **The whole `closedOperatorDirectSum*` family has exactly 2 production consumer sites**, both in `Riccati/UnboundedReduction.lean`; the other 9 are in `Experimental/InfiniteDimensional/Riccati/**`. So production is *two call sites* from deleting the facade outright — **what blocks it is `Experimental/**`, which U1's count excludes but which still has to compile.** That makes the deletion a policy call about Experimental (migrate it, or give it a documented compatibility layer), not a Riccati question, and it should be decided before the sweep rather than during it. I stopped rather than half-migrate: moving `UnboundedReduction` alone would leave production on the raw API and Experimental on the bundle — precisely the mid-development conversion boundary the phase-C ordering note warns against. |
| jon (namek) | `DavisKahan/DoubleAngle/Unbounded.lean` (delete three theorems, keep the two geometry lemmas); `DavisKahan/DoubleAngle/UnboundedIdeal.lean` (restate those three as specializations); one import line in `DavisKahan/TanTwoTheta/Unbounded.lean`. **Does NOT touch** `DavisKahan/Sylvester/**` (edward), any statement, or any other consumer. | **The unbounded sine-two-theta theorem is proved twice, and the operator-norm copy is the ideal copy at `N := operatorNorm`.** Found by a repository-wide near-duplicate scan (identical ≥14-line runs across files): `DoubleAngle/Unbounded.lean` and `DoubleAngle/UnboundedIdeal.lean` share **90 distinct 14-line windows**, the largest such pair in the tree. `sinTwoTheta_reflectionResidual_of_spectrum_gap` (165 lines) and `sinTwoTheta_reflectionResidual_gauge_of_spectrum_gap` (175 lines) are the same proof: ~130 lines of identical geometric spine — building `U, Uᶜ, Wᶜ, A₀, Λ, e, ΛJ, X, F₁` and their domain/intertwining/isometry facts — and they diverge only at the last step, one calling the operator-norm raw endpoint and the other the gauge one. Same for both `sinTwoTheta_addBounded_*_of_{spectrum_gap,intervalExterior}` pairs. Since `RectangularSymmetricIdealFamily.operatorNorm` has `Mem := fun _ => True` and `gauge := ‖·‖`, each operator-norm theorem is the second component of its gauge twin at that family. The dependency arrow runs the wrong way (`UnboundedIdeal` imports `Unbounded`), so the three move down rather than the proofs moving up; only one consumer import changes. **Scope note:** the same opNorm/gauge fork exists at the *base* of the chain (`SinTheta/Unbounded/Genuine{OpNorm,Gauge}.lean`) but there it is **not** duplication — the two take genuinely different routes and are ~6 lines each. This lane is only the place where the spine is copied. | 2026-07-28 | done — `Unbounded.lean` **318 → 94 lines**, and the operator-norm residual theorem is now a four-line derivation. Added `RectangularSymmetricIdealFamily.{operatorNorm_mem,operatorNorm_gauge}` (both `rfl`, both `@[simp]`) — the two facts that make "read the norm statement off the ideal statement" a rewrite instead of a second proof, and worth having on their own. The three operator-norm theorems moved to `UnboundedIdeal.lean` with statements and names unchanged; one consumer import changed (`TanTwoTheta/Unbounded.lean`). Gates: `DavisKahan.All` 9182 · layers 708 · name-drift 0 · all three axiom-clean. |
| jon (namek) | NEW `ForTauCeti/Analysis/Convex/Majorization.lean`; `ForTauCeti/Analysis/Normed/FiniteLpGauge.lean` (the `FiniteVector` vocabulary moves out; `FiniteSymmetricGauge.{update_le,mono,le_of_prefixSum_le}` become derivations); `ForTauCeti/Analysis/InnerProductSpace/UnitarilyInvariantNorm.lean` (`gauge_{update_le,mono}`, `gauge_le_gauge_of_prefix_sums_le` — proofs only, statements fixed); `ForTauCeti/Analysis/InnerProductSpace/RectangularUnitarilyInvariantNorm/Majorization.lean` (delete `mem_convex_of_prefix_sums_le`, repoint its one call site); `dev/tauceti/extraction-manifest.json` + `ForTauCeti/README.md` for the new module. **Does NOT touch** `DavisKahan/**`, `comparator/*.json`, `Challenge/**`, the U1 lane, or any statement outside the four files above. | **The Hardy–Littlewood–Pólya transfer lemma — the reusable core of majorization, currently written out three times.** The engine of the entire unitarily-invariant-norm theory is a T-transform descent, and the staging library contains **three verbatim copies of it**, ~860 lines in total, none of them public and none aware of the others: `FiniteSymmetricGauge.le_of_prefixSum_le` (`Analysis/Normed/FiniteLpGauge.lean:373`, abstract gauge), `UnitarilyInvariantNorm.gauge_le_gauge_of_prefix_sums_le` (`:508`, the UI-norm gauge) and the `private mem_convex_of_prefix_sums_le` (`RectangularUnitarilyInvariantNorm/Majorization.lean:245`, convex-set-valued) — whose own docstring admits the duplication ("the membership-valued analogue of …, formulated once here"). The supporting `update_le`/`mono` pair is duplicated the same three times. **One of the three subsumes the other two.** The convex-set form is the general one: a gauge's sublevel set `{x | Φ x ≤ Φ y}` is convex (subadditive + absolutely homogeneous), swap-closed and sign-flip-closed, so both gauge statements are instantiations. Plan: a new leaf module holding the `FiniteVector` majorization vocabulary (`prefixSum`, `WeaklyMajorized`, `zeroPadRight`, moved out of the 863-line `FiniteLpGauge`) plus the core, decomposed into the named steps a reader can use — `IsTTransform` (the T-transform / Robin Hood operation), `mem_of_isTTransform`, `mem_of_update_of_abs_le`, `mem_of_pointwise_le`, `exists_tTransform_of_not_le` (**the transfer lemma proper**, pure real-vector combinatorics with no `K` in it) and `mem_of_prefixSum_le`. Then the three consumers become derivations. Expected: −700 lines, one canonical theorem, and `FiniteLpGauge` back under Tau Ceti's 1000-line new-file ceiling. | 2026-07-28 | done — **860 lines of duplicated proof became one theorem, and not one statement changed.** `ForTauCeti/Analysis/Convex/Majorization.lean` holds the single descent, decomposed so the pieces are reusable rather than one 340-line block: `transfer` (the Robin Hood operation) with `transfer_apply_{left,right,of_ne}`, `IsTTransform` + `isTTransform_transfer`, `prefixSum_transfer`, `IsSymmetricConvex`, `exists_isTTransform_of_not_forall_le` (**the transfer lemma proper** — pure real-vector combinatorics, no `K` in it), `IsSymmetricConvex.{mem_of_isTTransform,update_mem,mem_of_forall_le,mem_of_prefixSum_le,mem_of_weaklyMajorized}`, plus the `FiniteVector` vocabulary and `FiniteSymmetricGauge` moved out of `FiniteLpGauge`. **Three things worth knowing.** (1) The bridge that collapses everything is `FiniteSymmetricGauge.isSymmetricConvex_sublevel`: a symmetric gauge's sublevel set `{x | Φ x ≤ r}` is convex, swap-closed and sign-flip-closed, so *both* gauge descents are instantiations of the set-valued one — `update_le`, `mono` and `le_of_prefixSum_le` are now one line each, on both gauges. The UI-norm side gets there through a new `UnitarilyInvariantNorm.finiteSymmetricGauge b`, whose four fields are exactly `gauge_add_le`/`gauge_real_smul`/`gauge_perm`/`gauge_neg_single` — a statement worth having on its own. (2) The three-case prefix-sum argument, which was the bulk of each copy, is now the *identity* `prefixSum k (transfer q j l δ) = prefixSum k q - [j<k]·δ + [l<k]·δ`, proved by splitting the transfer as `q + single j (-δ) + single l δ`; the cases become arithmetic. (3) A gotcha for the next such split: marking `transfer_apply_right` `@[simp]` silently broke the descent's endgame, because `simp` used it *before* the local `have` that said the transferred coordinate equals `z`'s. It is deliberately not a simp lemma. **Line counts:** `FiniteLpGauge` 863 → 257, `UnitarilyInvariantNorm` 931 → 594, rectangular `Majorization` 876 → 536, new module 691 — net −800. Everything is now comfortably under Tau Ceti's 1000-line new-file rule. **Gates:** `lake build ForTauCeti` 3419 jobs / **0 warnings** under upstream's linter set with `warningAsError` on · default build 9288 · `Challenge` 8827 · layers OK 708 · name-drift 0 findings · census CLEAN 48 · `export_for_tauceti.py --check` **OK** · all six new theorems axiom-clean. |
| jon (namek) | `ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/KyFan.lean` (generalize two theorems ℂ → `RCLike`, add one hypothesis-parameterized form); `DavisKahan/OperatorIdeal/ApproximationNumbers/Core.lean` and `.../Real/KyFanGauge.lean` (collapse the `_complex`/`_real` twins onto it). **Does NOT touch** `DavisKahan/Sylvester/**` or `DavisKahan/Riccati/**` (edward), `.../Real/Threshold.lean` (its complexification route is genuine, see below), `comparator/*.json`, or `Challenge/**`. | **The Ky Fan triangle inequality's localization argument is written twice, once over ℝ and once over ℂ, and it does not depend on the field.** Second-ranked pair in the duplicate scan: `ApproximationNumbers/Core.lean` and `ApproximationNumbers/Real/KyFanGauge.lean` share 38 identical 14-line windows. Eight `_real` theorems mirror eight `_complex` ones, and `kyFanApproximationGauge_add_le_real` is `..._complex` with ℂ→ℝ substituted throughout — same `Σ n : Fin k, Fin (n.1+1)` index type, same span, same three-step `calc`. **The ℝ/ℂ split is justified one layer down and nowhere else.** `Real/Threshold.lean` says so in its own docstring — Mathlib's continuous functional calculus is not available for operators on a *real* Hilbert space, so the real threshold theorem genuinely goes through the complexification, and that file stays. But everything above it — the finite-source compression and the finite-restriction localization — uses only `span`, `FiniteDimensional`, `orthogonalProjectionOnto` and `approximationNumber`, all of which are already `RCLike`-generic. Plan: generalize `kyFanGauge_add_le_of_finiteDimensional_source` from ℂ to `RCLike` (its proof should go through verbatim — the `_of_finiteDimensional` step it calls is *already* 𝕜-generic), then state the localization once as `kyFanGauge_add_le_of_forall_exists_finiteRestriction`, taking the finite-restriction approximation property as an explicit hypothesis, and instantiate it at ℂ with the min-max endpoint and at ℝ with the complexification one. | 2026-07-28 | done — **one predicate now carries the entire field dependence of the approximation-number theory, and eight `_real` copies became three instantiations.** `ContinuousLinearMap.HasMinMaxLowerBound 𝕜 E F` (`ForTauCeti/.../ApproximationNumber/FiniteRestriction.lean`) says: strictly below every approximation number there is a strictly larger uniform lower modulus on an `(n+1)`-dimensional subspace. That is the *only* thing the localization theory asks of the scalars. Over ℂ it is `hasMinMaxLowerBound_complex`, the min--max theorem via the continuous functional calculus on `T.modulus`; over ℝ it is `hasMinMaxLowerBound_real`, `Threshold.lean`'s transport through the complexification — a genuinely different proof, which is why that file stays at 781 lines and is *not* duplication. **Everything above it is now stated once.** New generic statements: `HasMinMaxLowerBound.{exists_finiteRestrictionApproximationNumber_gt_of_lt,approximationNumber_isLUB_finiteRestrictions,lt_approximationNumber_iff_exists_finiteDimensional_lowerBound}` (ForTauCeti), `ContinuousLinearMap.kyFanGauge_add_le_of_exists_finiteRestriction` (the localization step of the triangle inequality, taking the field-specific input as a hypothesis), and on the DavisKahan side `approximationSingularValue_comp_strongProjection_tendsto_of_minMax`, `kyFanApproximationGauge_comp_strongProjection_tendsto_of_minMax`, `kyFanApproximationGauge_add_le_of_minMax`. Also generalized ℂ → `RCLike` *in place*, since their proofs never used the field: the whole compression section of `ForTauCeti/.../KyFan.lean` (`approximationNumber_restrict_mono`, both `orthogonalProjectionOnto_comp_eq`, `kyFanGauge_add_le_of_finiteDimensional_source`) and Core's mirrors of them. **Line counts:** `Real/KyFanGauge.lean` **439 → 90**, `Real/Threshold.lean` 826 → 781, `ForTauCeti/.../FiniteRestriction.lean` 184 → 235 (the generic layer), Core 553 → 616 (ditto) — net −270 with a strictly stronger API. **Two names were deliberately not touched**: `approximationSingularValue_comp_strongProjection_tendsto_{complex,real}` and `kyFanApproximationGauge_add_le_real` are pinned by `Challenge/MathlibPending/ApproximationNumbers/Leaderboard.lean`, so they survive as the ℂ/ℝ instantiations; the five unpinned intermediate `_real` twins are deleted. |
| jon (namek) | `lakefile.toml` `lean_lib ForMathlib` (`globs`); the six `ForMathlib/**` modules that turn out never to have been built by their own target — `Analysis/InnerProductSpace/{ProjectionBlocks,ProjectionGap,QuadraticFormBounds,ReducingSubspace,SylvesterOperator,SpectralOrder/Complex}.lean` — **hygiene only: headers, `## Provenance`, line lengths, linter fixes; no statement changes**. **Does NOT touch** `DavisKahan/**`, `ForTauCeti/**`, `comparator/*.json`, `Challenge/**`, or `DavisKahan/Sylvester/**` (edward). | **Half of `ForMathlib` is invisible to its own build target, so the linter gate that lane installed covers only six of twelve modules.** `lean_lib ForMathlib` sets `leanOptions` but **no `globs`**, so the target builds `ForMathlib.lean` and its six transitive imports and nothing else; `ForTauCeti` next to it carries `globs = ["ForTauCeti.*"]` precisely to avoid this. The other six modules are compiled only as *dependencies of DavisKahan*, i.e. outside the library's `leanOptions` and therefore against this repository's default linter set rather than Mathlib's — which is the exact defect the ForMathlib header/linter lane was created to fix, still half-present. It is visible from outside: `lake build ForMathlib` reports the documented 3 warnings, while a DavisKahan build emits four `style.longLine` warnings from `ProjectionGap.lean` that the ForMathlib target never sees. Plan: add the glob, take the census under Mathlib's set, clear it, and check the six for the copyright/provenance treatment the other six got. | 2026-07-28 | done — glob added, census **3 → 9 → 0**, and `warningAsError` is now on. The six modules the target never built contributed six `style.longLine` warnings (four in `ProjectionGap.lean`, two in `SpectralOrder/Complex.lean`); all cleared by wrapping, no statement touched. Headers and `## Provenance` were already right on all twelve — that half of the earlier lane did land. **The three `[DecidableEq n]` warnings are settled rather than merely documented.** They are pinned by an immutable Challenge conformance statement, and `AGENTS.md` forbids changing a `Conformance.lean` statement to match an implementation, so they cannot be removed from either side; they are now suppressed with `set_option linter.unusedDecidableInType false in` at exactly those three declarations, reasoning at the point of use. That is what let the gate go on, and the gate is the point: with it off, three *known* warnings were also letting every *new* warning through — which is exactly how six accumulated unseen. Gates: `lake build ForMathlib` 0 warnings, default build 9288, layers OK 708, name-drift 0, comparator signature pre-flight for `pending-rank-factorization` **PASS**. |
| jon (namek) | `ForMathlib/**` (12 modules — headers and `## Provenance` sections, plus whatever the linter census turns up); `lakefile.toml` `lean_lib ForMathlib` options. **Does NOT touch** `ForTauCeti/**`, `DavisKahan/**`, or any statement. | **Give `ForMathlib` the treatment `ForTauCeti` just got.** The staging library destined for *Mathlib* has exactly the same two defects that were found in the Tau Ceti one tonight, and Mathlib's bar is at least as high: **6 of its 12 modules carry no copyright or licence header at all**, **all 12 lack a `## Provenance` section**, and `lean_lib ForMathlib` sets **no `leanOptions`**, so its warning count is measured against this repository's default linter set rather than Mathlib's. For `ForTauCeti` that gap was 78 warnings wide. Mathlib additionally enforces the copyright-header linter that Tau Ceti disables, so `style.header` should be **on** here, not off. Plan mirrors the one that worked: reconstruct provenance from each file's own header and the introducing commit, add the missing headers, turn on `mathlibStandardSet` + `longFile` *without* `warningAsError`, clear the census, then turn `warningAsError` on so it cannot regress. | 2026-07-28 | done — census **22 → 3** under Mathlib's linter set (0 under ours, which is the point). All 12 modules now carry the canonical four-line copyright block and a `## Provenance` section, with the staging prose moved out of the header into a `## Staging note` — Mathlib's header linter requires the block to be **exactly** those four lines, which is why the first pass produced *more* warnings, not fewer. Also fixed: two `show`-for-`change`, one over-long line, one malformed author line. **The 3 that remain are one cause and cannot be fixed from here** — `[DecidableEq n]` pinned by an immutable Challenge conformance statement; see the cross-lane finding. `warningAsError` is therefore deliberately left off, with the reason recorded in `lakefile.toml` and in the module. |
| jon (namek) | NEW `ForTauCeti/Analysis/InnerProductSpace/PolarPartialIsometry.lean`; a `TODO`-resolving note in `ForTauCeti/Analysis/InnerProductSpace/PolarIsometry.lean`. **Does NOT touch** `DavisKahan/Sylvester/**` or `DavisKahan/SpectralTheory/**` (U1 lane), `DavisKahan/Geometry/Polar/**` yet (that is the follow-on consumer swap), or `vendor/Spectra`. | **The general bounded polar decomposition, with a partial isometry.** Three separate things in the repository point at this same hole. (1) `ForTauCeti/.../PolarIsometry.lean` carries an explicit `TODO`: its `polarIsometry` is `M ∘L Ring.inverse |M|`, defined only when `|M|` is invertible, and the docstring says that when a general polar decomposition with a *partial* isometry exists it should **replace** this definition under the same name and make `polarIsometry_comp_modulus` unconditional. (2) `ForTauCeti/.../PolarDecomposition.lean` has the partial-isometry factor but only for `LinearMap` endomorphisms **in finite dimensions**. (3) Two production modules, `DavisKahan/Geometry/Polar/{PolarIsometryFinal,Section3Nonacute}.lean`, get the general bounded version from **Spectra** (`Spectra.QuantumMechanics.Channels.{polarPartial,polarRange,absOp,…}`); they are two of the twelve entries on the new rule-7 burn-down list, and AGENTS.md says the final migration target removes Spectra from the normal build. So building this closes a documented TODO, removes a duplicate foundation, and burns down migration debt at once. **Construction** (no new mathematics, but real Lean): `‖|M| x‖ = ‖M x‖` (`norm_modulus_apply`) makes `|M| x ↦ M x` a well-defined isometry on `range |M|`; corestrict `|M|` to `K := closure (range |M|)`, where it has dense range, extend by `LinearMap.extendOfNorm` (Mathlib `Analysis/Normed/Operator/Extend.lean`) to `W₀ : K →L[ℂ] F`, and set `polarPartial := W₀ ∘L K.starProjection`. Deliverables: the polar identity `polarPartial ∘L |M| = M` **unconditionally**, isometry on `K`, vanishing on `Kᗮ = ker M`, and the partial-isometry property. | 2026-07-28 | done — `ForTauCeti/Analysis/InnerProductSpace/PolarPartialIsometry.lean` (46 declarations) delivers all four, plus the uniqueness characterisation `eq_polarPartial_of_comp_modulus`, `W W⋆ W = W`, `W⋆ M = |M|`, `|M⋆| = W |M| W⋆`, and `W(M⋆) = W(M)⋆`. The consumer swap landed too: `DavisKahan/Geometry/Polar/PolarIsometryFinal.lean` is now 167 lines (was 332), Spectra-free, and out of the vendored `Spectra.QuantumMechanics.Channels` namespace it had been declaring into; `DavisKahan/Interop/Spectra/OperatorAbsoluteValue.lean` identifies Spectra's polar isometry with ours via the uniqueness lemma, so `Section3Nonacute` needed no change. Rule-7 burn-down 12 → 10. |
| jon (namek) | `dev/tauceti/extraction-manifest.json` (cluster + provenance records for the 55 undeclared staging modules); the `## Provenance` sections of the 44 `ForTauCeti/**` modules that lack one; `dev/tauceti/extraction-cluster-classification.md`; `ForTauCeti/README.md` cluster table. **Does NOT touch** any Lean *statement* — provenance is a docstring section — nor `DavisKahan/**`, `comparator/*.json`, `Challenge/**`, or the U1 lane. | **Finish the Tau Ceti migration: make every staged module actually exportable.** Audit result, and it is the real remaining surface: `ForTauCeti/` holds **67** modules but `dev/tauceti/extraction-manifest.json` declares **12**, in one cluster. The other **55 are invisible to the migration machinery** — outside `check_dependency_layers.py` rule 6 (extraction-closure) and outside `export_for_tauceti.py` entirely, which is manifest-driven. This is the same defect I hit on `OperatorIdeal/Family/**` earlier today, at scale. Two good pieces of news from the audit: the **import firewall already holds everywhere** — no staged module imports anything but Mathlib/TauCeti/ForTauCeti, so nothing is blocked on untangling dependencies — and the internal import graph falls into **12 weakly-connected components**, which are the natural cluster boundaries (44 / 8 / 4 / 2 / 2 / seven singletons). The blocking gap is provenance: `ForTauCeti/README.md` makes a `## Provenance` section a hard rule (original repository, source commit, module paths, declaration names, authors/copyright, license, extraction class, Spectra influence), because Kitware and third-party attribution must survive the move — and **44 of 67 modules have none**. Plan: (M1) reconstruct provenance per module from git history and the module's own header; (M2) declare every module in a cluster with a provenance record; (M3) get `export_for_tauceti.py --check` green per cluster; (M4) re-run the closure check, which now actually covers all 67. | 2026-07-28 | done — **the staging library now provably meets Tau Ceti's build gate.** All four phases landed. M1/M2: every one of the 71 modules carries an Apache-2.0 Kitware header and a `## Provenance` section; 31 had **no copyright or licence header at all** — the Wave-1 move carried the mathematics across and dropped it — and authors were recovered from each file's own "Formalized by" note, or from the introducing commit's `Co-Authored-By` trailer where the file had none. The redundant Wave-1 stub paragraph is deleted from 35 headers. M3: 12 clusters, each the **import closure** of its roots (the convention the existing cluster already followed), with a provenance record per module; the closure check now covers all of them, 698 modules, 0 violations. M4: `export_for_tauceti.py --write` then `--check` round-trips **all 71 modules MATCH, `export: OK`**, and no export target collides with an existing upstream file. **The two blockers in the cross-lane finding are both cleared.** (1) `RectangularUnitarilyInvariantNorm.lean` (2124 lines, against a 1500 hard ceiling and 1000 for a new file) is split into Basic / Majorization / BlockSum / Instances, with the original name kept as an aggregate import so **no downstream file changed**; every module is now under 1000 lines. Two things the split forced: a `private` lemma whose users ended up across a seam is now public, and a **`local` instance does not survive an import** — `Module ℝ (E →ₗ[𝕜] F)` had to be re-enabled with `attribute [local instance]` downstream rather than made global. (2) The library is built with upstream's own `leanOptions` and is at **0 warnings, down from 78**, with `warningAsError` now on so it cannot regress. **What is left is acceptance-side, not ours:** a PR to the Tau Ceti repository, review, and moving the submodule pin. The mechanical migration is finished.
| jon (namek) | `ForTauCeti/Analysis/InnerProductSpace/MoorePenroseInverse.lean` (181 lines) | **`moorePenroseInverse` proves only one of the four Penrose identities.** The file establishes `A A⁺ A = A` (`comp_moorePenroseInverse_comp`) and nothing else: `A⁺ A A⁺ = A⁺`, `(A A⁺)⋆ = A A⁺`, `(A⁺ A)⋆ = A⁺ A`, and uniqueness are all absent. So nothing in the repository certifies that this construction *is* the Moore–Penrose inverse rather than some other generalized inverse — it is, but that is unproved — while the name promises the full characterization and any downstream user will assume `A⁺A` is self-adjoint. **The proof route is short and already half-built.** With `v := TauCeti.rightSingularBasis A`, the existing `moorePenroseInverse_apply_apply_rightSingularBasis` gives `A⁺(A vₖ) = vₖ` when `σₖ ≠ 0`, and `apply_rightSingularBasis_eq_zero_of_singularValue_eq_zero` gives `A vₖ = 0` when `σₖ = 0`; together they say `A⁺ ∘ₗ A` is the orthogonal projection onto `span {vₖ : σₖ ≠ 0}`, diagonal in an orthonormal basis with 0/1 entries — which is (4), and (2) follows because `A⁺` already lands in that span. For (3), `A ∘ₗ A⁺ = ∑ᵢ (σᵢ²)⁻¹ • rankOne (A vᵢ) (A vᵢ)`, self-adjoint because `adjoint (rankOne x y) = rankOne y x` and the coefficients are real. Uniqueness is the standard four-identity argument. Expect the self-adjointness steps to want a helper for "diagonal in an orthonormal basis with real entries ⟹ `IsSymmetric`", which Mathlib does not appear to have in a directly usable form. | 2026-07-28 | done — **all four Penrose identities and uniqueness now hold; the name is earned.** Added: `moorePenroseInverse_comp_apply_rightSingularBasis` (the one fact the rest is read off — `A⁺A` is diagonal in the right singular basis with entries 0 and 1, i.e. the orthogonal projection onto the directions of nonzero singular value), `moorePenroseInverse_apply` (the evaluated form), identity (2) `moorePenroseInverse_comp_comp`, identity (3) `isSymmetric_comp_moorePenroseInverse`, identity (4) `isSymmetric_moorePenroseInverse_comp`, and `eq_moorePenroseInverse_of_penrose` — any map satisfying all four **is** `A⁺`. So the construction is now certified to be *the* Moore–Penrose inverse rather than some other generalized inverse, which nothing in the tree previously established. **Two things worth knowing.** (1) Identity (3) needs **no orthogonality**: `A A⁺` is visibly a real-coefficient combination of rank-one projections onto the images of the right singular vectors, so self-adjointness is a two-line computation once the evaluated form is available — the anticipated "diagonal in an orthonormal basis ⟹ IsSymmetric" helper was only needed for (4), and there it was cheaper to expand both arguments in the basis than to build the helper. (2) `LinearMap.IsSymmetric.adjoint_eq` (Mathlib `Analysis/InnerProductSpace/Adjoint.lean:598`) is the bridge the uniqueness proof runs on; the whole classical argument is two `calc` chains that both land on `B ∘ₗ A ∘ₗ A⁺`, each pushing an adjoint through the factorization of `A` supplied by the *other* map's first identity. Only one associativity step needed hand-holding. 6 new declarations, all axiom-clean, module warning-free.
| jon (namek) | NEW `ForTauCeti/Analysis/InnerProductSpace/HilbertSchmidtEnergy.lean`; NEW `DavisKahan/Interop/Spectra/HilbertSchmidtTensor.lean`; the basis-energy block and the tensor block of `DavisKahan/Sources/DavisKahan1970/Ideals/HilbertSchmidtBasis.lean`. **Does NOT touch** `DavisKahan/Sylvester/**` or `DavisKahan/SpectralTheory/**` (U1 lane), the singular-value side of `DavisKahan/Sources/DavisKahan1970/Ideals/HilbertSchmidt.lean`, or `vendor/Spectra`. | **The Hilbert–Schmidt energy, off Spectra.** Five of the ten remaining rule-7 entries are Hilbert–Schmidt modules, and the dependency is far shallower than the allowlist suggests. `HilbertSchmidtBasis.lean` uses Spectra for exactly two things. (1) `Spectra.QuantumMechanics.Channels.tsum_enorm_inner_sq` — **Parseval in `ℝ≥0∞`**, a six-line consequence of Mathlib's `HilbertBasis.hasSum_inner_mul_inner`, stated there only for `ℂ`. It is the whole content of `paperHilbertSchmidtBasisEnergy_adjoint_swap` and hence of basis-independence. (2) `Spectra.HilbertSchmidtTensor.*` — genuinely Spectra's tensor model, used only by the last four declarations, which are interop by nature and belong under `DavisKahan/Interop/Spectra/` where rule 7 exempts them. Splitting those two apart takes `HilbertSchmidtBasis` off Spectra entirely. **Deliverables:** a `RCLike`-general, rectangular `ContinuousLinearMap.hilbertSchmidtEnergy (b : HilbertBasis ι 𝕜 E) (T : E →L[𝕜] F) : ℝ≥0∞ := ∑' i, ‖T (b i)‖ₑ ^ 2` in ForTauCeti, with Parseval, the adjoint swap **without** Spectra's needless self-adjointness hypothesis, basis-independence, the finite-subset supremum form, and the ideal bounds `‖T‖ₑ ^ 2 ≤ energy` and `energy (A ∘L T ∘L B) ≤ ‖A‖ₑ ^ 2 * energy T * ‖B‖ₑ ^ 2`. This is also §12.4 groundwork: the energy is the squared gauge of the Hilbert–Schmidt ideal, so it is the natural input to a second `TauCeti.SymmetricOperatorIdealFamily` instance after the Ky Fan one. | 2026-07-28 | done — delivered in two staged modules and one bridge. `ForTauCeti/Analysis/InnerProductSpace/HilbertSchmidtEnergy.lean` has the energy with Parseval, the rectangular adjoint swap, basis independence, the operator-norm domination and the ideal bounds; `ForTauCeti/Analysis/OperatorIdeal/Family/HilbertSchmidt.lean` has the norm, the predicate `IsHilbertSchmidt`, and `TauCeti.hilbertSchmidtIdealFamily`, a full `SymmetricOperatorIdealFamily`. Its triangle inequality needed `ENNReal.tsum_sq_add_rpow_le`, a staged extension of Mathlib's `Finset`-level `ENNReal.Lp_add_le` to `tsum`. The scope grew by two items, both recorded here rather than taken silently: the archival column-expansion proof moved from `DavisKahan/Alternative/` to `DavisKahan/Interop/Spectra/` (it declares into Spectra's own namespace, so `Interop` is where rule 7 expects it) and its two now-empty aggregators were deleted; and the reconciliation with the paper development was added to `HilbertSchmidtBasis.lean`, where `tsum_approximationSingularValue_sq_eq_hilbertSchmidtEnergy` discharges the S3 obligation and `hilbertSchmidtENorm_eq_ofReal_paperHilbertSchmidtNorm` shows the staged gauge *is* the paper square norm. **Rule-7 burn-down 10 → 8.** |
| jon (namek) | NEW `ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/BernsteinNumbers.lean` (or an extension of `.../MinMax.lean`); afterwards, and only if it lands, `DavisKahan/Interop/Spectra/ApproximationNumberMinMax.lean` and `DavisKahan/OperatorIdeal/ApproximationNumbers/Core.lean` get repointed. **Does NOT touch** `DavisKahan/Sylvester/**` or `DavisKahan/SpectralTheory/**` (U1 lane), or `vendor/Spectra`. | **The hard half of the min--max theorem, without Spectra.** This is *the* structural blocker of the whole approximation-number ideal theory, and tracing it took most of the night. Cluster 1b (`BlockSum.lean`) is blocked on it. The Ky Fan triangle inequality `kyFanApproximationGauge_add_le_complex` is blocked on it — `DavisKahan/OperatorIdeal/ApproximationNumbers/Core.lean:627` proves it through `SpectraBridge.exists_finiteRestrictionApproximationNumber_gt_of_lt`. So the Ky Fan families, and with them trace class and every symmetric-gauge ideal, are blocked on it too. The reverse direction is already staged in `.../MinMax.lean` (`le_approximationNumber_of_linearIndependent`); the missing forward direction is `SpectraBridge.exists_linearIndependent_lowerBound_of_lt_approximationNumber`, whose Spectra proof uses the PVM/Borel layer. **It does not need PVM.** Plan, using only Mathlib's continuous functional calculus on `A := T.modulus`: put `g t := max (t - s) 0` for `r < s < aₙ(T)`, `K := ker (cfc g A)`, `M := Kᗮ`. (i) `t² - s² ≤ (‖A‖ + s) * g t` on `spectrum A ⊆ [0, ‖A‖]`, so `⟪A²y, y⟫ ≤ s²‖y‖²` on `K`, i.e. `‖A y‖ ≤ s‖y‖` there. (ii) with `u t := max (s - t) 0` we have `g * u = 0`, so `range (cfc u A) ⊆ K`, and `cfc u A` commutes with `cfc g A` so it preserves `M`; hence `cfc u A` kills `M`, and `s - t ≤ u t` gives `⟪A y, y⟫ ≥ s‖y‖²` on `M`, so `‖A y‖ ≥ s‖y‖` by Cauchy--Schwarz. (iii) if `finrank M ≤ n` then `T ∘L M.starProjection` has rank `≤ n` and `‖T - T ∘L M.starProjection‖ ≤ s < aₙ(T)`, contradiction — so `M` carries `n + 1` independent vectors, and `‖T x‖ = ‖A x‖ ≥ s‖x‖` on it. `norm_modulus_apply` is what makes `T` and `A` interchangeable throughout. | 2026-07-28 | done — and it unblocked more than the lane predicted. Staged in three modules: `ForTauCeti/Analysis/InnerProductSpace/SpectralCutoff.lean` (the two cutoff bounds), `ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/MinMaxUpper.lean` (the theorem), and `ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/FiniteRestriction.lean` (the localization corollaries, moved out of the interop layer once it stopped being interop). `DavisKahan/Interop/Spectra/ApproximationNumberMinMax.lean` went 336 lines with four Spectra imports → 162 with none → **deleted**, its six declarations renamespaced into `ContinuousLinearMap`. Verified by import-closure walk: `DavisKahan/OperatorIdeal/ApproximationNumbers/{Core,ScalarGeneric,BlockSum}.lean` no longer reach Spectra at all, so **extraction cluster 1b is unblocked** and the Ky Fan gauge triangle inequality is Spectra-free. Two follow-ons landed in the same lane rather than being left implicit: the Ky Fan gauge theory is now staged as `ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/KyFan.lean` (`ContinuousLinearMap.kyFanGauge`, all three steps of the triangle inequality), with `DavisKahan/.../Core.lean` reduced 807 → 553 lines of one-line delegations and **no** duplicated definition; and `ForTauCeti/Analysis/OperatorIdeal/Family/KyFan.lean` builds `TauCeti.kyFanIdealFamily` — the roadmap's parked Ky Fan instance, in the destination its own docstring named — **without** the `HasKyFanApproximationGaugeTriangle` capability class, which over `ℂ` is now discharged rather than assumed. The generic family and the staged one are `rfl`-equal. |
| jon (namek) | NEW `ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/SameSequence.lean`; NEW `ForTauCeti/Analysis/OperatorIdeal/Family/{TraceClass,KyFanDominance}.lean`; `DavisKahan/OperatorIdeal/ApproximationNumbers/{BlockSum,OperatorModulus}.lean`; the `SameApproximationSingularSequence` block of `DavisKahan/Sources/DavisKahan1970/SineTheta/Norms/{SingularValueTransport,UnitaryInvariantNorm}.lean`. | **Finish what the Spectra-free min--max unblocked.** Four things, all downstream of it. (1) **Trace class**: the nuclear norm is the supremum of the Ky Fan gauges, so its triangle inequality is theirs in the limit — `TauCeti.traceClassIdealFamily`. (2) **Fan dominance as a property**: `TauCeti.IsKyFanDominant`, the roadmap's S2 mixin, with instances for the operator-norm, Ky Fan and trace-class families. (3) **Cluster 1b's last blocker**: `BlockSum.lean` imported the *source* layer for one relation; staged as `ContinuousLinearMap.HasSameApproximationNumbers`. (4) **The `generic_imports_sources` allowlist to empty**: `OperatorModulus.lean`'s three generic theorems staged, its two paper-facing ones moved into the source module whose vocabulary they use. | 2026-07-28 | done — build 9287 green, dependency layers 707 modules / 0 violations, `generic_imports_sources` **12 → 0 entries across the two commits**, `production_imports_spectra` unchanged at 8 (all four remaining kinds are documented in the allowlist comment). `TauCeti.SymmetricOperatorIdealFamily` now has four instances built from four unrelated constructions. |
| jon (namek) | `DavisKahan/SinTheta/**`, `DavisKahan/{DoubleAngle,TanTwoTheta}/UnboundedIdeal.lean`, `DavisKahan/Sylvester/**` and `DavisKahan/Sources/DavisKahan1970/**` — **only** the declarations whose signature names `RectangularSymmetricIdealFamily`; `DavisKahan/OperatorIdeal/UnitarilyInvariant/RectangularFamily.lean` (**deletion**, phase D); `DavisKahan/Interop/TauCeti/RectangularFamilyAdapter.lean` (**deletion**, phase D); `dev/tauceti-signature-polish-todo.md` §13. **Does NOT touch** `comparator/*.json`, `Challenge/**`, `DavisKahan/Experimental/**`, or any mathematical content. | **§13.2 phases C+D — retire the last of the three §13 adapters.** `operatorAbs` and `CourantFischerCompat` are both already deleted; `SymmetricOperatorIdealFamily.toRectangular` is the only transitional shim left, and it is the one §13 itself calls the long-lived-alias risk. Phase C retypes the ideal-parameterized sin-Θ theorems from the legacy `RectangularSymmetricIdealFamily` to the canonical `TauCeti.SymmetricOperatorIdealFamily` with the gauge in `ℝ≥0∞`; phase D then deletes the structure and the adapter. **This is not relocation, it deletes hypotheses**: `sinThetaBlock_mem_and_gauge_eq_directedSinThetaOperator` goes from `(hblock : N.Mem b) : N.Mem d ∧ N.gauge d = N.gauge b` to just `N.gauge d = N.gauge b`, because in `ℝ≥0∞` an equality of gauges already carries membership; every `_mem_and_gauge_le` in the group loses its membership hypothesis and its membership conjunct the same way. **Why it is takeable now and was not before:** the previous holder released it un-started because the migration chain ran into `DavisKahan/Sylvester/**`, then inside the U1 lane, and a half-migrated chain leaves an `ENNReal.toReal` conversion boundary in the middle of the sin-Θ development — precisely the smell §13 exists to prevent. U1 released 2026-07-28, so the whole chain is now reachable and it can be taken as **one sweep, root-outward from `Sylvester/Bounded.lean`** (layer 1; 21 of the other 29 files transitively import it), not leaf-inward. The risk to watch is the other direction: the estimates carry real gap constants (`δ * gauge X ≤ gauge C`), so each needs `ENNReal.ofReal δ` and a `toReal` bridge at whichever boundary still wants `ℝ`, and `linarith` does not apply there. | 2026-07-28 | **released, NOT started — for two measured reasons, both new since the previous release note.** (1) **Edward is active in exactly this tree.** `origin/fable/sylvester-upstream-leaves` is a live branch and the sweep's root is `DavisKahan/Sylvester/Bounded.lean`; a 29-file rename across `Sylvester/**` and `SinTheta/**` would collide with it catastrophically. (2) **The churn is much larger than the file count suggests.** Across the 29 affected modules there are **467 `.gauge` and 248 `.Mem`** occurrences. The parameter-type swap is the small part; the renames are the job, and a mis-applied one changes meaning silently rather than failing to compile. **A design finding for whoever takes it, which changes the plan on record.** Phase C as specified — restate in `ℝ≥0∞` so the membership hypotheses disappear — is *not* the cheap half. The Neumann/Cauchy machinery in `Sylvester/Bounded.lean` (`sylvesterNeumannPartialSum_cauchy`, `gauge_sylvesterNeumannTerm_le`) is real-arithmetic throughout: geometric majorants, `Summable`, `mul_le_mul_of_nonneg_right`, and **differences of partial sums**, which `ℝ≥0∞`'s truncated subtraction does not support. Rewriting those is a campaign, not a retype. **The lower-risk route that gets §13's actual goal** — one structure, no adapter — is to delete `RectangularSymmetricIdealFamily` and replace it by a *derived real-valued view* of the canonical family: `SymmetricOperatorIdealFamily.Mem A := N.gauge A ≠ ∞` and `.realGauge A := (N.gauge A).toReal`, with the 14 structure fields and 16 derived lemmas of `RectangularFamily.lean` reproved as theorems (the adapter already contains every one of those proofs — they just stop constructing a record). That deletes 570 lines and unifies the parameter type **without touching a proof body**, and it leaves the `ℝ≥0∞` statement upgrade as a *per-theorem*, no-longer-all-or-nothing follow-on, because every theorem would already be parameterized by the canonical family. Do it when the Sylvester tree is quiet. |
| jon (namek) — **collision, see status: edward claimed this 2m earlier and by the earlier-row rule it was his; the work was already committed before either of us saw the other, so it is done, not contested** | `DavisKahan/Sylvester/PairwiseHomogeneousUniqueness.lean` | **Sylvester uniqueness is stated only under a quantitative gap, though disjointness suffices and is already proved in the same file.** `linearPMapSylvester_solution_unique_of_pairwiseSpectrumGap` and its bundle twin require `0 < δ` and `GenuinePairwiseSpectrumGap A B δ`; classically (Sylvester–Rosenblum) uniqueness needs only `spec(A) ∩ spec(B) = ∅`. The file *already* proves the homogeneous statement under mere disjointness (`linearPMapSylvester_homogeneous_eq_zero_of_disjoint_spectrum`), and the gap version is derived from it in three lines — so a `..._solution_unique_of_disjoint_spectrum` is one `sub_eq_zero` away and simply is not there. Separately worth recording: nothing named Sylvester in production proves **existence** of a solution, only uniqueness and bounds, so a reviewer reading "Sylvester" as existence+uniqueness gets half. | 2026-07-28 | done — `linearPMapSylvester_solution_unique_of_disjoint_spectrum` and its `ClosedOperator` twin `closedSylvester_solution_unique_of_disjoint_spectrum` now exist, and both gap versions are one-line corollaries of them. The gap hypotheses really were dead weight: the proof is the existing homogeneous-under-disjointness theorem plus `sub_eq_zero`, and `0 < δ` never appears. Docstrings now say where a positive separation *is* needed — the solution **bounds**, not uniqueness. Axiom-clean; `DavisKahan.Sylvester.PairwiseHomogeneousUniqueness` builds with no new warnings. **The second half of this row is confirmed and is a real gap, left unclaimed:** grepping all of `DavisKahan/**` outside `Experimental` turns up **no existence theorem for the Sylvester equation at all** — the only `∃ S` statements under `DavisKahan/Sylvester/` are the shifted-inverse gauge bounds. So a reader who takes "Sylvester" to mean existence *and* uniqueness gets half. Closing it means Rosenblum's integral formula, which is a campaign, not a lemma. **edward: this is already landed — do not redo it.** `c447c51` on `namek-work` adds `linearPMapSylvester_solution_unique_of_disjoint_spectrum` and its `ClosedOperator` twin and makes both gap versions one-line corollaries; green and axiom-clean. Timestamps: edward's claim `a45f61e` 09:26:36, mine `25c12a5` 09:28:51, implementation `c447c51` 09:31:45 — so his row was first by two minutes and I did not have it when I started. Recorded here rather than quietly kept because the ledger only works if collisions are visible. The follow-on in the same area is **unclaimed and yours if you want it**: there is *no existence theorem for the Sylvester equation anywhere in production* — grep confirms the only `∃ S` statements under `DavisKahan/Sylvester/` are shifted-inverse gauge bounds — so Rosenblum's half of Sylvester--Rosenblum is missing. |
| jon (namek) | `DavisKahan/FiniteDimensional/DirectRotation.lean`; `DavisKahan/FiniteDimensional/DirectRotation/{PrincipalPlanes,QNorm,Majorization,ShortRotationCounterexample}.lean`; `DavisKahan/FiniteDimensional/DirectRotation/PrincipalPlanes/Variational.lean`; `Acharyya2025/DavisKahan.lean`; `ForTauCeti/Analysis/InnerProductSpace/SingularSubspace.lean` — **docstrings and one new theorem; no existing statement changes**. **Does NOT touch** `DavisKahan/Sylvester/**`, `DavisKahan/SpectralTheory/**` (U1 lane), `comparator/*.json`, `Challenge/**`. | **Named results that oversell themselves.** Two audits turned up cases where the *name or docstring* claims more than the *statement* delivers — the failure mode a Tau Ceti reviewer catches immediately. (1) **HIGH, and it is a flat contradiction:** six sites advertise the restricted-displacement minimality (Davis–Kahan Corollary 4.1) as holding "without any angle restriction" / "with no angle hypothesis", while every one of the theorems carries `hacute : IsAcute U V` — and `IsAcute` is *defined* as "no principal angle is a quarter turn, in either direction" (`Core/AngleGeometry.lean:101`). The intent was clearly "no largest-angle *threshold*, unlike the retracted `Θ ≤ π/3` claim", which is true and worth saying; the current wording is not. Docstring-only fix. (2) `Acharyya2025.DavisKahan.sum_cross_inner_sq_le` is headed "Davis–Kahan sin-Θ bound **(standard)**" but proves `‖sinΘ‖_F ≤ √n·ε/gap` — a factor of the *ambient* dimension worse than the standard `‖S−T‖_F/gap`. Its own last sentence admits the crude constant, but "(standard)" is the label this repo uses for the genuine article (cf. `Weyl.lean:167`). The sharp form already exists at `Alternative/FiniteDimensional/EigenbasisFrobenius.lean:254`; relabel and cross-reference. (3) **A famous theorem is missing, not weak:** Weyl's inequality for *singular* values, `|σₖ(Â) − σₖ(A)| ≤ ‖Â − A‖`, appears nowhere in production. What exists is `abs_sq_singularValues_sub_le` — squared, with an extra `(a + â)` factor and two auxiliary norm hypotheses. The unsquared form should follow from the approximation-number Lipschitz bound plus `approximationNumber_eq_singularValues`, i.e. from API §5 already landed. Adding it is the item with actual mathematical content. | 2026-07-28 | done (items 1 and 2 of 3) — **six docstrings corrected and one famous theorem added.** (1) The "no angle restriction" claim is gone from all six sites; each now says what is actually dropped — the largest-angle *threshold* of the retracted `π/3` corollary — and why `IsAcute` stays (it is what makes `directRotation` exist, not a weakening of the conclusion). No statement changed. (2) `Acharyya2025.DavisKahan.sum_cross_inner_sq_le` no longer calls itself "(standard)"; it names itself the crude operator-norm form, quantifies the loss (a factor of the *ambient* dimension, `√n·ε/gap` against `‖S−T‖_F/gap`) and points at the two sharp results already in the tree. (3) **Weyl's inequality for singular values now exists**: `ContinuousLinearMap.abs_singularValues_sub_singularValues_le`, `|σₙ(T) − σₙ(S)| ≤ ‖T − S‖`. It was genuinely absent — what stood in for it was the squared Gram-transported variant with an extra `(a + â)` factor and two auxiliary hypotheses, which the sharp form implies but not conversely. Proving it exposed a second gap: **approximation numbers were never shown to be 1-Lipschitz**, roadmap item A.2. That is now `abs_approximationNumber_sub_approximationNumber_le`, proved at full field-generic normed-space generality from the existing additive law, and Weyl is its transport through Eckart–Young — so the Hilbert theorem is a corollary of a Banach one, which is the payoff of having stated the s-number layer without an inner product. The squared variant keeps its place (it is the shape the Gram-side arguments produce) with a docstring saying so. Gates: ForTauCeti 3402 / **0 warnings** · `Challenge` 8896 · layers 694 · name-drift 0 · census CLEAN 48 · PR-1 consistency OK · comparator pre-flight **all 23 configs PASS** · 10/10 new declarations axiom-clean. **Two scoped items left over, both unclaimed — see the two new rows below.** |
| jon (namek) | `DavisKahan/SinTheta/{Bounded/Core,FrameFactorization,FrameFactorizationGeneric,Unbounded/{Core,GenuineGauge,SpectrumGap,IntervalExterior,AllGap,LegacyGap},Real/{Unbounded,Generalized},Canonical}.lean`; `DavisKahan/{DoubleAngle,TanTwoTheta}/UnboundedIdeal.lean`; `DavisKahan/Sources/DavisKahan1970/{SineTheta/ProjectionBlocks,SineTheta/Norms/SingularValueTransport,SinTwoTheta,RemainingSourceSurface}.lean`; `DavisKahan/Interop/Spectra/BoundedPerturbationSinThetaIdeal.lean`. **Does NOT touch** `DavisKahan/Sylvester/**` or `DavisKahan/SpectralTheory/**` (jon (toothbrush)'s U1 lane — 23 of the remaining occurrences live there and migrate under U1 or after it releases), `comparator/*.json`, `Challenge/**`, or `DavisKahan/Experimental/**`. | **§13.2 phase C — restate the ideal-parameterized sin-Θ theorems over the canonical family.** With phases A+B landed, `RectangularSymmetricIdealFamily` survives only as a *parameter type* for ~25 production theorems, reached through the derived view. Phase C retypes them to `TauCeti.SymmetricOperatorIdealFamily` with the gauge in `ℝ≥0∞`. This is not relocation: it deletes hypotheses. `sinThetaBlock_mem_and_gauge_eq_directedSinThetaOperator` currently reads `(hblock : N.Mem b) : N.Mem d ∧ N.gauge d = N.gauge b`; in `ℝ≥0∞` it is `N.gauge d = N.gauge b`, because an equality of gauges already carries membership. Every `_mem_and_gauge_le` in this group loses its membership hypothesis and its membership conjunct the same way. The risk is the other direction and is why this is a separate phase: the estimates carry real gap constants (`δ * gauge X ≤ gauge C`), so each one needs `ENNReal.ofReal δ` and a `toReal` bridge at whichever boundary still wants `ℝ`, and `linarith` does not apply. Migrate leaf-inward in dependency-closed groups, rebuilding each group before moving on. Phase D (deleting the structure and the adapter) stays blocked on the U1 lane regardless of how far C gets. | 2026-07-28 | released, NOT started — **and the reason is an ordering constraint, not the mathematics.** Two canonical helpers landed on the way (`OperatorIdealFamily.gauge_comp_{left,right}_le_of_norm_le_one`, the one-sided contraction bounds the migration needs and the legacy record already had); those are kept. What stopped the migration: the payoff is real — `sinThetaBlock_mem_and_gauge_eq_directedSinThetaOperator` goes from `(hblock : N.Mem b) : N.Mem d ∧ N.gauge d = N.gauge b` to just `N.gauge d = N.gauge b` — but it is only a payoff if the **whole chain** moves, because each migrated theorem pushes an `ENNReal.toReal` conversion into its consumers. Migrated piecemeal it makes the tree *worse*: a conversion boundary in the middle of the sin-Θ development, which is precisely the long-lived-adapter smell §13 exists to prevent. **The chain cannot close.** Its root is `DavisKahan/SinTheta/Bounded/Core.lean`, which is genuinely unblocked (it does not import `Sylvester`). But following it outward — `Unbounded/Core` → `{AllGap, LegacyGap, IntervalExterior, GenuineGauge, SpectrumGap}` → `Real/{Unbounded,Generalized}`, `Canonical` → `{DoubleAngle,TanTwoTheta}/UnboundedIdeal`, `Sources/**` — it reaches the blocked Sylvester ideal theorems at `SinTheta/Unbounded/GenuineGauge.lean` (1 call) and `SinTheta/Unbounded/IntervalExterior.lean` (4 calls). Those callees are `(N : RectangularSymmetricIdealFamily)`-typed and live in `DavisKahan/Sylvester/**`, i.e. inside `jon (toothbrush)`'s U1 lane. So the conversion boundary would land there no matter which end I start from. **Phase C should be taken by whoever holds `DavisKahan/Sylvester/**`, or after U1 releases** — and it should be taken as one sweep, root-outward from `Sylvester/Bounded.lean` (layer 1; 21 of the other 29 files transitively import it), not leaf-inward. Phase D is downstream of that. |
| jon (namek) | `ForTauCeti/Analysis/OperatorIdeal/Family/{Basic,OperatorNorm}.lean` (the canonical structures — space parameters and docstring); NEW `ForTauCeti/Analysis/OperatorIdeal/Family/KyFan.lean` **or** a local definition in `DavisKahan/OperatorIdeal/ApproximationNumbers/ScalarGeneric.lean` if the dependency layer forbids the canonical path; `DavisKahan/OperatorIdeal/ApproximationNumbers/ScalarGeneric.lean` (`KyFanDominantIdealFamily`'s stored field); `DavisKahan/Interop/TauCeti/RectangularFamilyAdapter.lean`; `dev/tauceti-signature-polish-todo.md` §12.1/§13. **Does NOT touch** `DavisKahan/Sylvester/**`, `DavisKahan/SpectralTheory/ClosedOperator/**`, `DavisKahan/SpectralTheory/ReducingSubspace/**` or `DavisKahan/Interop/TauCeti/ClosedOperator.lean` — all held by jon (toothbrush)'s U1 lane, and three of them name `RectangularSymmetricIdealFamily`, so **phase 13.2C stops at that boundary and those files migrate under U1 or after it releases.** Also does not touch comparator JSON, `Challenge/**`, or `DavisKahan/Experimental/**`. | **§13.2 — swap `KyFanDominantIdealFamily`'s stored representation to the canonical `TauCeti.SymmetricOperatorIdealFamily`.** Step 1 (2026-07-27) routed all 376 `.Mem`/`.gauge` call sites through accessors, so this is now a one-file storage change plus proof repair. **The gate, and the design change it forces — read this before objecting to the diff.** `KyFanDominantIdealFamily` has two production instances, `operatorNorm` and `kyFan k hk`; the second is used by `DavisKahan/Sources/DavisKahan1970/SineTheta/{Symmetric,ProjectionBlocks,Theorem61Universal}.lean` for the paper's Ky Fan corollaries, so it cannot be dropped. Only `operatorNorm` has a canonical counterpart today. Building the Ky Fan one hits a structural obstruction: `SymmetricOperatorIdealFamily extends OperatorIdealFamily`, whose `gauge` field is quantified over **all normed spaces**, and while `∑_{n<k} aₙ(A)` is perfectly definable there (`ContinuousLinearMap.approximationNumber` is stated for seminormed spaces over a `NontriviallyNormedField`), its **subadditivity is not** — this repository's proof of `kyFanApproximationGauge_add_le` runs through singular values and majorization, both Hilbertian, and the classical additivity property `s_{m+n-1}(S+T) ≤ s_m(S) + s_n(T)` does not yield the Ky Fan sum bound (it leaks a duplicated `a₀` term at `k = 2`). So the Banach-wide base cannot host the motivating examples. **Resolution taken:** narrow `OperatorIdealFamily`'s space parameters to Hilbert (`InnerProductSpace` + `CompleteSpace`), keeping both universes, the `extends` layering, every derived declaration and every name. Nothing in the tree is lost — `operatorNormIdealFamily` is its only instance and Hilbert spaces are normed spaces — and all five gauges this development actually has (operator norm, Ky Fan, Schatten p, trace class, Hilbert–Schmidt) are Hilbert-space gauges. Rejected alternatives: a standalone Hilbert symmetric structure (duplicates ~25 derived declarations — `carrier`, `Elem`, `IsComplete`, the gauge lemmas — for no gain, since only 3 references to that API exist outside the defining file), and a second Hilbert base with the derived API moved onto it (preserves a Pietsch-general structure that has no instance and no consumer). The name is unpinned: no `comparator/*.json`, no `Challenge/**`, not in the extraction manifest. Phases: **A** canonical Ky Fan family via `ENNReal.ofReal`; **B** storage swap with `Mem`/`gauge` kept `ℝ`-valued over `.toReal`; **C** migrate direct legacy users outside the U1 boundary; **D** delete the legacy structure and the adapter. | 2026-07-28 | done (phases A+B) — **the canonical family exists and `KyFanDominantIdealFamily` now stores it.** Phase A: `kyFanSymmetricIdealFamily k hk : TauCeti.SymmetricOperatorIdealFamily 𝕜`, the `ENNReal.ofReal` transport of `kyFanApproximationGauge`, with all four ideal laws, adjoint invariance, and an `IsComplete` instance proved from the two-sided comparison `‖A‖ ≤ ∑_{n<k} aₙ(A) ≤ k‖A‖`. Phase B: the stored field is now the canonical family plus `isComplete`; `toRectangularSymmetricIdealFamily` is a derived `def`; `Mem`/`gauge` keep their meaning and their `ℝ` type. **Net effect on the code: −62 lines of inline legacy record, and two structure fields deleted outright** — in `ℝ≥0∞` Fan dominance needs neither a membership hypothesis nor a membership conclusion, since `gauge A ≤ gauge B` already carries `gauge B ≠ ∞ → gauge A ≠ ∞`; the historical two-part form survives as a theorem, so **not one downstream statement changed**. `kyFan_gauge_complete` deleted. **The design change, and it is the part to review:** `OperatorIdealFamily`'s space parameters narrowed Banach → Hilbert (`[RCLike 𝕜]`, `InnerProductSpace`, `CompleteSpace`), universes and `extends` layering and every name preserved. Forced, not stylistic: the Ky Fan gauge is *defined* at Banach generality but its subadditivity is Hilbertian and does not follow from `a_{m+n}(S+T) ≤ aₘ(S)+aₙ(T)`, so a Banach base has one instance and no route to the other four. Not a contradiction of a recorded decision — the Banach clause in roadmap decision 7 was a descriptive sub-bullet citing the *s-number* generality bar, which is a different layer and is untouched; both it and the §12.1 mirror are rewritten. **Doc debt cleared alongside:** the `Family/**` modules were missing from `dev/tauceti/extraction-manifest.json` entirely, i.e. outside both the closure check and `export_for_tauceti.py` — added, with provenance records. Also corrected: §12.1 (4 passages), §13 table row + 3 callouts, Appendix A (5 new rows — it had none for this migration), convergence matrix, declaration inventory, cluster classification, `ForTauCeti/README.md`, both roadmap READMEs, public-api review PR split. **Gates:** full default build green except the four U1-lane modules that were *already* red on `origin/main` before I touched anything (see cross-lane findings) · layers OK 694 · name-drift 0 · PR-1 consistency OK · `git diff --check` clean. **Phases C/D remain and are NOT claimed:** 106 direct type-position uses in 30 production modules, 23 of them inside the U1 boundary, then delete `RectangularSymmetricIdealFamily` and the adapter. |
| jon (toothbrush) | `ForTauCeti/Analysis/InnerProductSpace/LinearPMap/{Closed,Sylvester}.lean`; `DavisKahan/SpectralTheory/ClosedOperator/**`; direct production consumers in `DavisKahan/SpectralTheory/ReducingSubspace/**`, `DavisKahan/Sylvester/**`, `DavisKahan/Riccati/{UnboundedBasic,UnboundedCore,UnboundedReduction,UnboundedExistence,UnboundedTransport}.lean`, `DavisKahan/SinTheta/Unbounded/{Core,GenuineGauge}.lean`, and their mechanical `DavisKahan/Sources/DavisKahan1970/**` consumers; `DavisKahan/Interop/TauCeti/ClosedOperator.lean` when the adapter boundary can actually shrink; U1 planning/inventory documents. Current slice: raw reducing-subspace restriction/pullback/unitary transport and raw Riccati block data/core/graph handoff, plus raw unbounded sine-theta data, residual identity, ideal-gauge endpoint, and right-unbounded Neumann proof. **Does NOT touch** approximation-number §§5.1–5.4, comparator JSON, Challenge, Spectra PVM/Borel functional calculus, real-spectrum bridge proofs, complexification, or source-facing theorem redesign beyond representation-preserving consumer migration. | **U1 — canonical unbounded-operator representation migration.** Replace the bundled DKPS `ClosedOperator` foundation by Mathlib `LinearPMap` plus closedness/density/symmetry/self-adjointness properties. The reusable core is staged in `ForTauCeti`; the remaining work is consumer conversion and bundle contraction. Pairwise spectrum/uniqueness, generic Neumann estimates, full reducing-subspace restrictions, and pullback/unitary transport have raw implementations. The right-unbounded Neumann proof is now raw and its bundled theorem is a compatibility wrapper. Next classify remaining interval/exterior source consumers against their exact Spectra bounded-realization/resolvent boundary, then migrate every non-Spectra consumer. | 2026-07-28 | **released mid-migration — OPEN, take it.** Last slice landed: the canonical finite-interval source records `FiniteIntervalGeneralSinThetaProblem.{result,complementaryBlock_result}` (`DavisKahan/SinTheta/Canonical.lean`) now call the raw `linearPMap_*` interval/exterior endpoints through `UnboundedSinThetaData.toPMap` and the genuine-gap `.toPMap` view; both axiom-clean, focused build 8838 jobs, `DavisKahan.All` green, layers OK 699, name-drift 0. **What is left is measured, not guessed, and written up in `dev/tauceti/u1-linearpmap-migration.md` under "Release state" — read that before reclaiming.** Headline: gate U1.4's *first* command is met in substance (`ForTauCeti` has 3 `ClosedOperator` hits, all prose provenance, zero declarations); its *second* is not — **171 type-position uses across 18 production modules**, of which **41 are already documented compatibility facades** (`ReducingSubspace/Restriction.lean` 23, `Sylvester/ClosedSylvesterEquation.lean` 18) and are deletions rather than proofs, while the other 130 are genuine un-migrated records, largest first: `Riccati/UnboundedCore` 32, `Sources/**/SineTheta/CommonCore` 17, `SinTheta/Natural/Reducing` 14, `Riccati/UnboundedTransport` 13. `generalizedSinTheta_unbounded_{,exact_}of_genuineIntervalExteriorGap` are contractible **now** — no production caller outside their own module. `Interop/Spectra/**` and `OperatorIdeal/ComplexificationApproximation.lean` are outside this lane's declared scope and need their own claim; the `Interop/TauCeti/ClosedOperator.lean` adapter is deleted last by construction. **Two things unblock on this release:** the unclaimed `PairwiseHomogeneousUniqueness` row above ("take only with U1's owner") is now freely takeable, and §13.2 phases C+D — deferred by the ideal-family lane to whoever holds `DavisKahan/Sylvester/**`, i.e. to this one — are open, with the standing instruction that phase C be one root-outward sweep from `Sylvester/Bounded.lean`, never leaf-inward. |
| jon (namek) | dev/tauceti/tauceti-pr1-approximation-numbers.md; dev/tauceti/migration-build-log-2026-07-24.md; dev/tauceti/extraction-cluster-classification.md; NEW dev/tauceti/pr1-consistency-restoration-2026-07-27.md; NEW scripts/refresh_tauceti_pr1_consistency.py and its test; dev/tauceti/extraction-manifest.json **non-name integration metadata only** (revisions, module paths, dependency-closed cluster membership, status; preserves every `source_declarations` entry); dev/LANES.md this row only. **Does NOT touch** any Lean theorem file, Section 5.1--5.4 signature work, the active agent's manifest name entries, comparator JSON, Challenge, or the Tau Ceti submodule pointer | **Tau Ceti PR-1 consistency restoration.** Reconciled packaging with the real-valued approximation-number API and unified `ContinuousLinearMap.modulus`; computes the live transitive ForTauCeti export closure; marks July 24 validation historical; supports repeated blind refreshes on shared `main`; and preserves theorem-name lanes. | 2026-07-27 | done |
| edward (resumed) | ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/MinMax.lean; the 8 `DavisKahan` call sites of the renamed theorems; dev/tauceti-signature-polish-todo.md §5.4; ForTauCetiRoadmap/ApproximationNumbers/README.md milestone B.4 note. **Does NOT touch** §5.2 Adjoint (its one item is P1 "keep/annotate"), the ForTauCeti warning-hygiene lane (jon), or §8.1/§8.3/§10 (jon) | Tau Ceti P0 §5.4 — the **last open P0 in §5**, so closing it prunes the section. `lowerBound_le_approximationNumber_of_finrank` is (a) named after `lowerBound`, which is not an object in the statement, (b) pinned to the exact dimension `finrank 𝕜 V = n + 1` where the proof only needs "bigger than n", and (c) stated with a unit-vector premise instead of a homogeneous bound, so it says nothing at `x = 0` and does not scale. Plan: state the primary result over `(n : Cardinal) < Module.rank 𝕜 V` — which **drops the `[FiniteDimensional 𝕜 V]` instance entirely**, since an infinite-dimensional test subspace satisfies it trivially — with the homogeneous hypothesis `c * ‖x‖ ≤ ‖T x‖`; derive the `finrank` and unit-vector forms from it (unit-norm ⇒ homogeneous needs no sign hypothesis on `c`, as `c * 0 ≤ 0`); rename all three conclusion-outward and repoint consumers, carrying no aliases. **Reuse evidence for the §5.4 P1's open question** ("does this family wrapper have independent consumers? otherwise keep private"): it has **8**, across 6 DavisKahan modules, so it stays public — recording that rather than leaving the question open. The sketch's fourth ask, "the ultimate API should expose a supremum/max-min formula", is **not** in scope here and will be recorded as the remaining roadmap B.4 item rather than faked: only the lower half is unconditional in infinite dimensions, which is exactly what B.4 already says it must be honest about. | 2026-07-27 | done — **§5 now has no open P0.** Primary `le_approximationNumber_of_lt_rank` over `(n : Cardinal) < Module.rank 𝕜 V` with the homogeneous hypothesis `c * ‖x‖ ≤ ‖T x‖`. **Both changes are real generalizations, not renames:** stating the size hypothesis on `Module.rank` instead of `finrank` **removed the `[FiniteDimensional 𝕜 V]` instance outright** — the proof never needed more than "`V` is too big to be killed by a rank ≤ n map", so the exact dimension `finrank V = n + 1` was doing no work; and the homogeneous bound says something at `x = 0` and scales, where a unit-vector premise does neither. Derived: `le_approximationNumber_of_finrank_lt` (classical unit-vector form; the conversion needs **no** sign hypothesis on `c`, since at `x = 0` it reads `c * 0 ≤ 0`) and `le_approximationNumber_of_linearIndependent` (family form). All named conclusion-outward, `lowerBound` gone; no aliases kept, 8 call sites across 6 DavisKahan modules repointed. **§5.4 P1's open question answered with evidence:** the family wrapper has **8** consumers, so it stays public — recorded on the theorem rather than left open. **Deliberately NOT faked:** the sketch's "expose a supremum/max-min formula" — only this half is unconditional in infinite dimensions, exactly what roadmap B.4 says the two-sided package must be honest about, so it stays a B.4 item. |
| edward (resumed) | NEW scripts/check_declaration_name_drift.py; scripts/tests/ if that directory holds the existing script tests; dev/tauceti-signature-polish-todo.md §13 (the "gate gap" callout only — **not** the deletion table rows, which are jon's reconciliation); AGENTS.md gate list if there is one. **Does NOT touch** any Lean file, any `comparator/*.json` content, or any Challenge module | **The gate gap jon recorded at the end of §13, explicitly unclaimed.** §13's own rule says "after each canonical API lands, repoint consumers, remove duplicate declarations, and run a grep gate for old fully qualified names" — there is no such gate, and its absence has now bitten twice in one day: the §9.2 rename passed a green 9272-job default build and still broke `Challenge.MathlibPending.RankPsdRealization.Leaderboard`, and 24 stale `ForMathlib.*` names sat in `comparator/*.json` for days making the conformance gate silently vacuous. Both escaped because `Challenge` is outside `defaultTargets` and because `comparator/*.json` restates declaration names as **data**, which no compiler checks. Plan: a fast, build-free static gate that (a) extracts every declaration name asserted as data — `comparator/*.json` `theorem_names`, `#print axioms` lines in `Challenge/**/Leaderboard.lean` — and checks each resolves to a real declaration in the tree, (b) checks each pinned name's *namespace* is consistent between the challenge module and the shipped declaration (the `ForMathlib`-vs-`TauCeti` mismatch class), and (c) reports rather than guesses. It must run without `lake build` so it is usable as a pre-commit gate, which means resolving names by parsing declarations out of the sources, not by asking Lean. | 2026-07-27 | done — `scripts/check_declaration_name_drift.py` + `scripts/tests/test_check_declaration_name_drift.py` (14 tests). Build-free (parses declarations out of the sources instead of asking Lean), so it runs in ~1s and works on a tree that does not compile. Three **hard** checks — `pinned-name-resolves`, `pinned-name-in-challenge` (the `ForMathlib`-vs-`TauCeti` mismatch class, which compiles on *both* sides and fails only in the comparator's export comparison), `pinned-name-unaudited` — plus one **informational** note class: a leaderboard auditing more than its config pins is legitimate, so it must not fail the gate. On first run it reported 15 findings; after fixing the script's own bugs and with today's comparator repairs in place it is **0 findings, 4 notes**. **Two bugs found writing it, both pinned by regression tests because both produced silently wrong names rather than errors:** (a) a bare `end` closing a `section` popped a `namespace`, corrupting every subsequent name in the file — `namespace`/`section` must share one stack; (b) `\s*$` under `re.MULTILINE` **spans newlines**, so `end` on one line and `section` on the next parsed as a single `end section` and unbalanced the file — all four patterns now use `[ \t]` for horizontal whitespace. Limits stated in the module docstring rather than hidden: resolution is syntactic, so it does not follow `export`/`open … in`/alias targets and a pass is a tripwire, not a proof; `check_comparator_signatures.py` remains ground truth. Documented in dev/README.md and the §13 gate-gap callout is marked CLOSED. |
| jon | ForTauCeti/Analysis/InnerProductSpace/{TwoDimensionalSingularValues,MoorePenroseInverse,UnitarilyInvariantNorm,RectangularUnitarilyInvariantNorm,OperatorModulus,BasisSpan}.lean — **warning-clearing only**, no signature or statement changes | **Drive `lake build ForTauCeti` to zero linter warnings.** The staging library currently emits 20 across 6 files, and every one is a §14 pre-PR checklist violation that an adversarial Mathlib reviewer flags on sight: 2 × "automatically included section variable(s) unused" (`diagOp_const`, `diagOp_const_pair` carrying a spurious `[FiniteDimensional 𝕜 E]`) plus one in a private `OperatorModulus` helper — these are literally the checklist's "No assumption is unused or merely inherited from a broad section variable block"; 6 × deprecated `Matrix.toEuclideanLin_apply` → `Matrix.toLpLin_apply`; 4 × `try 'simp' instead of 'simpa'`; 2 × unused simp argument; 2 × `'push_cast'/'change' tactic does nothing`; 2 × `tac1 <;> tac2` where `(tac1; tac2)` suffices. Scope discipline: this touches only proof bodies, `omit` clauses and deprecated lemma names — **no statement, name, namespace or signature changes**, so it cannot orphan a `comparator/*.json` entry or a Leaderboard and needs no rename gate. Two of the six files were authored/migrated in edward's §6/§7 lanes, but both of those lanes are released `done`; his open claims are §5.1 (`ApproximationNumber/**`) and the comparator json, neither of which is touched here. Success criterion is objective: warning count 0, ForTauCeti green, and the full default build still 9272. | 2026-07-27 | done — **`lake build ForTauCeti` is now warning-free (0 of 20 remain), 3397 jobs green.** Verified by `git diff` that no declaration header changed: only proof bodies, `omit` clauses and one `set_option`, so no rename gate was needed. FINDING #1 (cascade): removing the unused `[CompleteSpace E/F/G]` from the private `opNorm_eq_of_forall_norm_apply_eq` **exposed a second unused instance** in the public `norm_modulus_comp` — it had only ever needed `[CompleteSpace G]` because the helper's signature dragged it in. So one unused-hypothesis fix can unmask another downstream; clearing these is iterative, not a single pass. Both are now `omit`ed. FINDING #2 (linter false positive, deliberately NOT silenced repo-wide): `unnecessarySeqFocus` fires on `simp [...] <;> ring` in `singularValues_lowerLeft_two_by_two` and suggests `(simp; ring)`. That suggestion **does not compile** — `simp` closes some `fin_cases` branches outright, so the final `ring` must tolerate zero remaining goals and sequencing it fails with "No goals to be solved". I tried the suggestion, got three errors, reverted, and left a scoped `set_option linter.unnecessarySeqFocus false in` with the reason written above the declaration. Worth knowing before anyone else takes the linter's advice here. Everything else was straightforward: 6 × deprecated `Matrix.toEuclideanLin_apply` → `Matrix.toLpLin_apply`, 4 × `simpa` → `simp`, 2 × unused simp argument dropped, 2 × no-op `push_cast`, 1 × no-op `change`, 3 × unused section variables. **Scope note after merging edward's §5.4:** the six files in this lane are warning-free, but `lake build ForTauCeti` now reports 2 again, both in `ApproximationNumber/MinMax.lean:165` — his **open** claim, so left untouched and reported to him in the cross-lane findings instead. Warning-zero for the library as a whole is therefore a moving target while other lanes are open; it holds for this lane's files. |
| jon | NEW Challenge.lean (root aggregate); Challenge/MathlibPending/DavisKahanSylvesterPiOverTwo/Conformance.lean; lakefile.toml if the lib block needs globs. **Explicitly NOT `comparator/*.json`** — the stale `ForMathlib.*` name lists there stay edward's comparator lane, including the two in `pending-davis-kahan-sylvester-pi-over-two.json` | **Make the conformance gate buildable at all.** Found while verifying my §9.2 repair: `lake build Challenge` fails with "some modules have bad imports" for a reason unrelated to any name drift — **there is no `Challenge.lean` root aggregate**. Every other library has one (`ForTauCeti.lean`, `DavisKahan.lean`, `ForMathlib.lean`, the four paper libs), but the `[[lean_lib]] name = "Challenge"` block declares no root module and no globs, so the library target can never build and only individual modules ever compile. I swept all 49 Challenge modules individually: **48 green, 1 red.** The red one is `DavisKahanSylvesterPiOverTwo/Conformance.lean`, and the git history says it has **never compiled since it was created** — it references `SpectraSeparated` unqualified while importing only a UI-norm module, and `SpectraSeparated` has only ever existed in `DavisKahan/FiniteDimensional/Core/SpectralGap.lean`, never in `ForMathlib` (checked with `git grep` at `1fbd391^`). So this is not fallout from edward's dedup, which only flipped its namespace and import prefix; the file was dead on arrival and the missing root aggregate is exactly why nobody ever saw it. Plan: add the missing import to that Conformance file (predicate only — the challenged theorems live in `Sylvester.All`, which only the Leaderboard imports, so the challenge stays honest), then add `Challenge.lean` so the gate builds as a library and this class of rot cannot hide again. | 2026-07-27 | done — **`lake build Challenge` now succeeds for the first time (8891 jobs).** Two fixes. (1) The never-compiling `DavisKahanSylvesterPiOverTwo/Conformance.lean` gained the one missing import, `DavisKahan.FiniteDimensional.Core.SpectralGap`, which supplies the *hypothesis* predicate `SpectraSeparated`; the challenged theorems live in `Sylvester.All`, which only the paired Leaderboard imports, so the challenge stays honest and its two `sorry` placeholders are untouched. (2) The library block gained `globs = ["Challenge.*"]` plus a documented, intentionally-empty `Challenge.lean`, mirroring `ForTauCeti.lean` exactly — that is the established house pattern, and `ForTauCeti` was the only lib already using it. `Challenge` deliberately stays OUT of `defaultTargets` (the lakefile comment says so, and these modules carry intentional `sorry`s), so the default build is unchanged — verified still 9272 jobs. The point is that `lake build Challenge` is now a runnable gate rather than an unresolvable target. FINDING worth keeping: the missing glob is *why* rot could hide. A file that never compiled since the day it was written survived indefinitely because the only way to compile it was to name it explicitly, and nothing did. This is the same blind spot that let my §9.2 rename through — recorded in `Challenge.lean`'s header so the next person renaming a staged declaration knows to run `lake build Challenge`. NOT touched: `comparator/*.json` name lists (edward's lane), including the two stale `ForMathlib.DavisKahanTheory.*` entries in `pending-davis-kahan-sylvester-pi-over-two.json`. **Lane release includes the durable fix:** AGENTS.md's "Comparator challenge rule" section now carries the rename gate §13's closing bullet asks for — grep the whole repo including `Challenge/` and `comparator/*.json`, build `Challenge` explicitly, and the `--no-build`/single-config usage note for the signature pre-flight. It also records that adding an `import` so a Conformance statement can *elaborate* is not a breach of the immutability rule (the statement and its placeholders stay untouched), whereas filling a placeholder or bending a statement to match an implementation is — worth pinning down, since this lane had to add exactly such an import. |
| jon | dev/tauceti-signature-polish-todo.md §13 (deletion/adapter table) — DOCS ONLY, no Lean files | Tau Ceti §13 ledger reconciliation. §13 is the cross-cutting "what still has a duplicate/adapter to retire" table, and it has gone stale against work that has actually landed, in a way that actively misleads: it still lists `finiteMean / appendFin` as "Likely generic duplicates → Replace with Finset/Fintype API, P0", but my §8.1 audit **disproved that for `finiteMean`** (`Finset.expect` needs `Module ℚ≥0 E`, which does not synthesize; `Finset.centroid` has `Classical.arbitrary` empty-family junk) while `appendFin` is deleted. Likewise `specSubspace` was renamed to `OrthonormalBasis.spanIndices` in edward's §6, and `operatorAbs` was demoted to a documented shim in his §7 — both rows still read as open P0s. Scope: reconcile every row against what landed, cite the commit/section that settled it, and keep genuinely-open rows (ClosedOperator/§12.2, Spectra SelfAdjointOperator, GenuinePairwiseSpectrumGap) untouched and clearly open. Also cross-check §14's pre-PR checklist against the §8–§10 lanes for any item the completed work shows to be unenforceable. Touches NO Lean file and NO other agent's section, so it cannot collide with edward's §5.1 or comparator lanes. | 2026-07-27 | done — §13 table reconciled row by row against the tree (each row re-checked by grep, not assumed). Corrected: **`finiteMean` was listed as a P0 "likely generic duplicate → replace with Finset/Fintype API" and that is wrong** — demoted to P2 with the disproof recorded inline. `appendFin` struck through (deleted, §8.1). `specSubspace` struck through (§6 renamed it to `OrthonormalBasis.spanIndices`; it survives only inside CourantFischerCompat). `operatorAbs` struck through (§7; survives as a reducible abbrev in a documented shim). `RectangularSymmetricIdealFamily` struck through (§12.1). Left clearly OPEN and untouched: `ClosedOperator` (P0, §12.2), `Spectra SelfAdjointOperator` (P1), `GenuinePairwiseSpectrumGap` (P1, still live across DavisKahan/Sources with `PairwiseSpectrumGap` aliased to it). TWO FINDINGS surfaced by the reconciliation, both recorded as callouts in §13: (1) **three transitional adapters are now live simultaneously** — `operatorAbs`, `CourantFischerCompat`, `SymmetricOperatorIdealFamily.toRectangular` — which is precisely the "long-lived aliases obscure which API reviewers are evaluating" risk the section itself warns about; retiring them is distinct work from the canonical-API lanes that created them and **is currently unclaimed by anyone**. (2) **the section's own last bullet asks for "a grep gate for old fully qualified names" after each rename, and no such gate exists** — its absence is not theoretical, since my §9.2 rename passed a green 9272-job build and still broke a Challenge module, because `Challenge` is outside `defaultTargets` and `comparator/*.json` stores declaration names as data. Any such gate must cover `Challenge/` and `comparator/*.json`. |
| jon | ForTauCeti/MeasureTheory/{CfcMeasurable,CompactExists,Function/ConvergenceInMeasure,Measure/Typeclasses/Probability}.lean; dev/tauceti-signature-polish-todo.md §10 | Tau Ceti §10 measure-theory helpers — 4 P0 + 1 P1, all dispositioned "reuse audit" or "strengthen". **COUPLING WARNING recorded up front, learned from my §9.2 miss:** unlike §8/§9 these declarations are wired into the conformance machinery — `Challenge/MathlibPending/{CfcMeasurable,TendstoInMeasure,ProbabilityQoL}/{Conformance,Leaderboard}.lean` restate them verbatim and `comparator/{pending-cfc-measurable,pending-tendsto-in-measure,pending-probability-qol}.json` list them by name, plus live consumers in `Acharyya2024/WellKnown.lean` and `DkpsQuench2026/Paper/EvaluationConcentration.lean`. `Challenge` is NOT in `defaultTargets`, so a green `lake build` does not cover it. Any rename or deletion therefore has to move the Conformance restatements and comparator name lists together. **Scope discipline:** edward's comparator conformance-gate lane is open on `comparator/*.json`, so I will NOT edit those files in this lane — if the audit forces a rename I will land the library side, build `Challenge` explicitly, and hand him the exact name map rather than racing him in the same file. Plan: (1) audit `one_sub_measure_compl_le` against Mathlib probability API — the backlog itself predicts it "may not merit a new public declaration", and if so the MathlibPending leaderboard entry for it is claiming credit for something upstream already has, which is worth surfacing regardless of what we do with the code; (2) audit the three `tendstoInMeasure_*` rate lemmas for proof-specific shape; (3) the `measurableSet_exists_mem_le` P0 asks for the canonical measurable-infimum object (`measurable_iInf_compact`) with the set-membership form as a derived corollary. Does NOT touch §5.1 (edward), §8.2 (edward), §12.1 (edward). | 2026-07-27 | done — **the reuse audit CLEARED the section: nothing deleted, nothing renamed.** That is the opposite of this section's framing (and of my own prior guess that §10 would end in deletions). Every candidate was put to `exact?` against Mathlib rather than judged by name. `one_sub_measure_compl_le` KEEP — `exact?` fails; upstream `prob_compl_eq_one_sub₀` needs `NullMeasurableSet` and `prob_compl_le_one_sub_of_le_prob` needs `MeasurableSet`, so the measurability-free form genuinely does not exist. `measurable_of_iUnion_restrict` KEEP — `exact?` fails; upstream has only the two-set `measurable_of_restrict_of_restrict_compl`. The three `tendstoInMeasure_*` KEEP unchanged — Mathlib's ConvergenceInMeasure has entry points from a.e. convergence, from eLpNorm, and the iff-reformulations, but NO vanishing-rate/high-probability entry point; and the section's stated risk ("proof-specific rate lemmas") does not hold since they are stated over an arbitrary filter and arbitrary `EDist E`. **P0 satisfied for `measurableSet_exists_mem_le`:** added the missing canonical measurable-infimum object `measurable_iInf_of_isCompact` plus the attainment bridge `exists_mem_le_iff_iInf_le` (the latter with `omit [MeasurableSpace Ω]` — the linter caught that the instance was unused, it is pure order/topology). FINDING: the backlog wanted the set form derived FROM the infimum lemma; it is done the other way round, because the existential form is what the uncountable-index separability/sequential-compactness argument actually proves and what consumers use, while the infimum statement follows from it in four lines via `measurable_of_Iic`. Reversing it would mean rewriting the hard proof for no gain. Deriving rather than renaming also kept the comparator/Challenge coupling untouched, as scoped in the claim. Verified: all five §10 `Challenge` modules build individually green, ForTauCeti green, layer + census gates OK. NOTE: `lake build Challenge` as a whole is RED, but from MY earlier §9.2 rename, not this lane — flagged with the exact one-line repair in the cross-lane findings section above; the repair sits inside edward's committed comparator claim so I left it to him. |
| edward (resumed) | ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/FiniteDimensional.lean; NEW ForTauCeti/Analysis/InnerProductSpace/SingularValues.lean (the CLM accessor); the ~7 files naming `toLinearMap.singularValues` (25 occurrences); dev/tauceti-signature-polish-todo.md §5.3. **Does NOT touch** §5.4 MinMax, §8.1/§8.3 (jon), §10 (jon), or `DavisKahan.OperatorIdeal.ApproximationNumbers.Core.approximationSingularValue` — see the note below, that is a separate lane | Tau Ceti P0 §5.3 — the three P0s there share one root cause: every public statement says `T.toLinearMap.singularValues n`, leaking a `ContinuousLinearMap → LinearMap` coercion into consumers, and the review asks for a CLM-level accessor. Plan: define `ContinuousLinearMap.singularValues (T) : ℕ →₀ ℝ := T.toLinearMap.singularValues` with a `@[simp]` bridge and delegating lemmas for the facts actually used at CLM level, then restate `singularValues_le_norm_sub_of_rank_le`, `singularValues_le_approximationNumber` and the flagship `approximationNumber_eq_singularValues` over it. **Planned deviation from the review sketch, to verify:** it asks for a *singular* `singularValue`, but Mathlib's object is a `Finsupp` sequence `ℕ →₀ ℝ` and is irrevocably plural for that reason — the sketch's own escape clause — so the accessor stays plural and the deviation is recorded. The `⟨value, proof⟩ : NNReal` constructor noise the sketch also flags is already gone (decision 2 made the API real-valued); that half of the P0 is stale and will be marked so. | 2026-07-27 | done — NEW `ForTauCeti/Analysis/InnerProductSpace/SingularValues.lean` supplies `ContinuousLinearMap.singularValues T := T.toLinearMap.singularValues` with the `@[simp]` bridge `toLinearMap_singularValues` (oriented so `simp` **removes** the coercion, not introduces it) plus one-line delegations for the facts the operator layer uses. All four §5.3 statements — `singularValues_le_norm_sub_of_rank_le`, `singularValues_le_approximationNumber`, `approximationNumber_le_singularValues` and the headline `approximationNumber_eq_singularValues` — are restated over it, so the flagship now reads `T.approximationNumber n = T.singularValues n` with no coercion anywhere in the statement. Proofs that still need Mathlib's `LinearMap`-level lemmas convert back with a single `rw [← toLinearMap_singularValues]`, so nothing is duplicated. **Deviation confirmed and recorded:** the accessor stays PLURAL — Mathlib's object is the whole `Finsupp` sequence `ℕ →₀ ℝ` and `T.singularValues n` is application to it, which is exactly the sketch's own "irrevocably plural" escape clause; a singular accessor would be a second definition wrapping the first. **One half of the sketch was already stale:** the `⟨value, proof⟩ : NNReal` constructor noise it flags had been removed by the §5.1 codomain decision. One downstream repair needed (`DavisKahan/OperatorIdeal/ApproximationNumbers/Core.lean`): its `simpa only [LinearMap.coe_toContinuousLinearMap]` bridge now also needs `← ContinuousLinearMap.toLinearMap_singularValues` — a good sign the bridge lemma does its job. Layer check 692 OK. |
| edward (resumed) | ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/Basic.lean; NEW ForTauCeti/SetTheory/Cardinal/Lift.lean; the 3 sibling ApproximationNumber modules + DavisKahan/Sources/DavisKahan1970/Ideals/HilbertSchmidtBasis.lean **only** where they name the moved/renamed declarations; ForTauCetiRoadmap/ApproximationNumbers/README.md (new decision); dev/tauceti-signature-polish-todo.md §5.1. **Does NOT touch** §5.2 Adjoint / §5.3 FiniteDimensional / §5.4 MinMax mathematics (their own P0s stay open), §8.1 CenteredScatter, or anything under Challenge/comparator (my other open lane) | Tau Ceti P0 §5.1 — the **flagship PR A1 blocker**. Three items remain now that the codomain is settled: (1) the **index-convention decision**, which the backlog says must be "explicitly approved [in the roadmap] or the entire API reindexed before any PR" — current `aₙ(T) = dist(T, {rank ≤ n})` is zero-based, so `a₀ = ‖T‖`, against Pietsch's one-based `rank < n` with `a₁ = ‖T‖`; (2) `Cardinal.le_natCast_of_lift_le`, a **public extension of Mathlib's `Cardinal` namespace living inside an operator-ideal file** — placement is the review's objection, and it has 4 call sites across 3 ForTauCeti modules + 1 DavisKahan consumer so it cannot simply be privatized; (3) the definition body is **globally exposed** by a `@[simp] rfl` lemma `approximationNumber_def`, i.e. the simp normal form of a high-level invariant is a raw `ciInf` over a subtype. Plan: approve zero-based with recorded evidence, or reindex; move the Cardinal lemma to its own staged module in the natural iff shape; de-simp/rename the unfolding lemma and repoint proofs that relied on it; and give the conclusion-describing names the review asked for (`approximationNumber_le_norm_sub`, `le_approximationNumber_iff`) with the old names retained only where a consumer needs them | 2026-07-27 | done — all three items closed. (1) **Index convention DECIDED zero-based**, as roadmap decision 1 with the reviewer-facing argument, and a new "The index convention" section in the module docstring. Decisive evidence: Mathlib's `LinearMap.singularValues` is zero-indexed *and says so in its own docstring*, so the flagship identification is the index-free `aₙ(T) = σₙ(T)`; Pietsch's one-based numbering would make it `sₙ(T) = σ_{n-1}(T)` — **truncated `ℕ` subtraction plus an `n ≠ 0` side condition in the headline theorem** — and would put a `-1` in every ideal inequality (`a_{m+n} ≤ aₘ + aₙ` vs `s_{m+n-1}`), while `a₀ = ‖T‖` would stop being a theorem and become a definition by fiat over an empty infimum. **Deliberately NOT done:** no one-based `sNumber` def and no bridge *theorem* — a bridge needs something to bridge to, and adding it reintroduces exactly the duplicate API the "do not carry both" rule (decision 2) exists to prevent; the translation `sₙ = a_{n-1}` is documented instead. (2) `Cardinal.le_natCast_of_lift_le` moved to NEW `ForTauCeti/SetTheory/Cardinal/Lift.lean` as the **iff** `Cardinal.lift_le_natCast`, `@[simp]`, matching its Mathlib neighbours `aleph/beth/omega_natCast_le_lift` — separately upstreamable, so the operator-ideal PR now carries no `Cardinal` namespace extension at all. Privatizing (the review's first option) was impossible: 4 call sites, 3 modules, plus a DavisKahan consumer. **Gotcha for the upstreamer:** the two `↑n` sit in *different* universes, so both need explicit ascriptions and the proof must rewrite under `conv_lhs` — a bare `rw [← lift_natCast]` unifies the sides and fails with a universe constraint. (3) `approximationNumber_def` (a `@[simp] rfl` lemma exposing a raw `ciInf` over a subtype as the normal form of a norm-like quantity) → `approximationNumber_eq_iInf`, **de-simped**; `approximationNumber_le` → `approximationNumber_le_norm_sub` (10 call sites); `le_approximationNumber` → the characteristic iff `le_approximationNumber_iff` with no wrapper retained (4 call sites now use `.mpr`). Appendix A corrected: its `_nnnorm_` sketch names are stale since decision 2 made the API real-valued. **Left open on purpose:** the blanket `@[expose] public section` question raised alongside these items is a module-system change and belongs to the deferred repo-wide module-system pass, not to one file. Full build 9273 green, 0 errors; layer check 691 OK; census CLEAN (48). |
| edward (resumed) | comparator/*.json (the `theorem_names` lists ONLY — no `permitted_axioms`, no challenge/solution module repointing beyond what a rename forces); Challenge/MathlibPending/RankPsdRealization/{Conformance,Leaderboard}.lean (the ONE declaration `eigenvalues₀_eq_zero_of_le`, repointed to jon's new name); dev/LANES.md. **Does NOT touch** any ForTauCeti/DavisKahan proof, any other Challenge module, or the deliberate proof holes in any Conformance file | **Comparator conformance gate is broken repo-wide.** `scripts/check_comparator_signatures.py` compares the challenge statement against the shipped declaration by full type; 31 distinct names across ~20 of the 23 `comparator/*.json` configs still refer to pre-dedup `ForMathlib.*` declarations, so those configs error out rather than compare — the gate has been silently vacuous since the Wave-1 CourantFischer dedup (1fbd391) moved 37 modules into the `TauCeti.*` namespace. Triage already done by declaration site: **5 names are correct as-is** (`ForMathlib.Topology.Berge` ×3, `ForMathlib.Matrix` PosDef/RankFactorization ×2 — still genuinely in `ForMathlib`); **~24 are pure namespace drift** `ForMathlib.X` → `TauCeti.X`, including the `partIII_*`/`projector_difference_*` aliases in DavisKahan/Sources/DavisKahan1970/PartIII.lean and one that needs a *different* prefix than the obvious one (`ForMathlib.Matrix.gram_eq_gram_iff_exists_linearIsometryEquiv_map_eq` now lives at `TauCeti.LinearMap.Matrix.…`); **1 is a live regression** — jon's §9.2 rename today (33000c0) renamed `Matrix.PosSemidef.eigenvalues₀_eq_zero_of_le` to `…_of_rank_le` and made `i` implicit, which breaks `Challenge.MathlibPending.RankPsdRealization.Leaderboard` outright (`Unknown constant`), so that one challenge statement is restated to match rather than merely repointed | 2026-07-27 | done — 24 names repointed `ForMathlib.*` → `TauCeti.*` across 20 configs; the 5 that genuinely still live in `ForMathlib` verified correct as they stand. **Two needed the fix in the OPPOSITE direction:** the mass namespace flip had also rewritten the *challenge* modules `Berge/Conformance` and `RankFactorization/Conformance` to `namespace TauCeti` while their solutions stayed in `ForMathlib`, so those are moved back, and `RankPsdRealization/Conformance` — which pins one declaration of each kind — has its namespace split with the one cross-namespace reference qualified. jon's §9.2 rename regression fixed in the same pass (his `4dd6b57` made the same Leaderboard edit independently; merged cleanly). Spot-checked end to end: `pending-near-isometry` PASSes both theorems on universe signature and full type. The one config I left open — `DavisKahanSylvesterPiOverTwo`, whose Conformance could not compile for want of `SpectraSeparated` — was **closed by jon** in `b574401`. **Correction to my diagnosis:** I recorded it as fallout from the dedup repointing its import; jon's `git grep` at `1fbd391^` shows the file had **never compiled since the day it was written** (`SpectraSeparated` has only ever lived in `DavisKahan/FiniteDimensional/Core/SpectralGap.lean`, never in `ForMathlib`), and the dedup only flipped its namespace and import prefix. The real reason nobody noticed is the missing `Challenge` root aggregate, which jon also fixed by importing `DavisKahan.FiniteDimensional.Core.SpectralGap` (the predicate is challenge *hypothesis* data, not part of what is to be proved). |
| jon | ForTauCeti/Analysis/InnerProductSpace/{CenteredScatter,OrthogonalSeries}.lean; dev/tauceti-signature-polish-todo.md §8.1 + §8.3; consumers DkpsQuench2026/Spectral/GramSpectrum.lean and DavisKahan/Alternative/OperatorIdeal/HilbertSchmidt/ColumnExpansion.lean | Tau Ceti §8.1 + §8.3 — the two remaining §8 items (§8.2 is edward's, already RESOLVED), so closing these prunes §8 entirely. **Preliminary reuse audit already run; both files look substantially duplicative.** §8.3 OrthogonalSeries: Mathlib's `OrthogonalFamily` API in `Analysis/InnerProductSpace/Subspace.lean` has statement-for-statement counterparts — `OrthogonalFamily.norm_sum` ↔ our `norm_sq_finset_sum_of_pairwise_inner_eq_zero`, `OrthogonalFamily.norm_sq_sdiff_sum` ↔ our `norm_sq_sdiff_sum_of_...`, `OrthogonalFamily.summable_iff_norm_sq_summable` ↔ our `summable_iff_norm_sq_summable_of_...`. Difference is only the indexing: Mathlib takes an orthogonal family of *subspaces* `G i` with isometries `V i`, ours takes *vectors* with pairwise `⟪f i, f j⟫ = 0`. Plan: build the bridge (`G i := 𝕜 ∙ f i` with `subtypeₗᵢ`) and delete what it subsumes, keeping only genuinely-new vector-level statements as thin corollaries with short names. §8.1 CenteredScatter: `Finset.centroid` already exists upstream (`LinearAlgebra/AffineSpace/Centroid.lean`), so `finiteMean` is a probable duplicate; `appendFin` is scaffolding for one theorem (delete or privatize in favour of `Fin.cons`/`Fin.snoc`); `centeredScatter` to be reviewed for `Finset`-indexing and CLM-vs-LinearMap. Expected outcome for both files is net DELETION rather than polish — will report honestly if the audit instead shows the duplication is superficial. Does NOT touch §8.2 NearIsometry/PolarIsometry or §12.1 Family (both edward). | 2026-07-27 | done — both files resolved and pruned; §8 now waits only on edward's §8.2 P1 tail. All declarations axiom-clean, both files build warning-free, full build green. **§8.3: the duplication was real.** Mathlib's `OrthogonalFamily` has statement-for-statement counterparts of THREE of the five declarations, differing only by subspace-vs-vector indexing. FINDING: the genuinely missing upstream piece is a *constructor*, not the theorems — the only bridge from vectors is `Orthonormal.orthogonalFamily`, which demands UNIT vectors, so nothing covers a merely pairwise-orthogonal family. That constructor is now `orthogonalFamily_of_pairwise_inner_eq_zero` (lines `𝕜 ∙ f i` with `subtypeₗᵢ`, one line from `Submodule.isOrtho_span`), and because `V i (l i)` is `f i` definitionally the three duplicated results become one-line specializations. ~90 lines of duplicated proof deleted (hand-rolled Pythagoras induction, symmetric-difference identity, ε–N Cauchy argument). `norm_sq_sdiff_sum_of_...` went UNUSED once the Cauchy argument was replaced and was deleted outright — the backlog had only proposed privatizing it. Kept `summable_of_..._partial_sum_norm_le` and `HasSum.norm_sq_eq_tsum_of_...` (no upstream counterpart). Only one rename (`norm_sq_finset_sum_` → `norm_sum_sq_`, `finset` was redundant); the rest deliberately keep `_of_pairwise_inner_eq_zero` to mirror upstream spelling, since the backlog's proposed `_of_pairwise_orthogonal` would name a predicate that does not exist for vectors. Consumer needed NO edit. **§8.1: mixed.** `appendFin` was exactly `Fin.snoc` → DELETED with its two simp lemmas. FINDING: `Fin.lastCases` and `Fin.snoc` are NOT definitionally equal (`snoc` transports along `cast`), so GramSpectrum.lean's `rfl` bridge had to become a real `funext` proof; its statement keeps `Fin.lastCases` because that spelling propagates into Regularity.lean, outside this lane. `centeredScatter` retyped `E →ₗ[𝕜] E` → `E →L[𝕜] E` (its `rankOne` summands were already continuous, so the old codomain discarded continuity for nothing); verified against the compiler that CLM `IsPositive` and Löwner `le_def` both exist and need NO `CompleteSpace`, so the move costs no generality. FINDING (contradicts the backlog's expectation): **`finiteMean` is NOT a duplicate and was kept** — `Finset.expect` requires `Module ℚ≥0 E`, which fails to synthesize for a general 𝕜-inner-product space (checked by elaboration, not inspection), and `Finset.centroid` is affine-hierarchy with a different empty-family junk value; either would force non-𝕜 scalars into pure 𝕜-module algebra. NOT DONE and recorded as such: the `Fin n` → `Finset` generalization — the add-one identity is intrinsically about extending `Fin n` to `Fin (n+1)`, so it is a redesign of the headline theorem plus its 60-line proof, not a signature edit. Also fixed: stale `ForMathlib.*` docstring links, and all deprecated `ContinuousLinearMap.{sum,add,smul}_apply` / `coe_sum` / `coe_smul'` uses. |
| jon | ForTauCeti/Analysis/Matrix/Spectrum.lean; dev/tauceti-signature-polish-todo.md §9.2 | Tau Ceti P1 §9.2 sorted-eigenvalue tail — completes §9 so the whole section can be pruned. Reuse audit already run: Mathlib's `eigenvalues₀` API carries only `eigenvalues₀_antitone` plus charpoly relations, and BOTH supporting facts exist upstream only for the *unsorted* `eigenvalues` (`rank_eq_card_non_zero_eigs`, `PosSemidef.eigenvalues_nonneg`). The single existing theorem derives both inline, so this is a basic-theory build-out, not just the rename the backlog asked for. Plan: (a) extract `Matrix.IsHermitian.rank_eq_card_non_zero_eigenvalues₀` in the exact shape of Mathlib's unsorted counterpart — holds for ANY Hermitian matrix, no PosSemidef; (b) extract `Matrix.PosSemidef.eigenvalues₀_nonneg`, the sorted counterpart of `eigenvalues_nonneg`; (c) rename the headline to `Matrix.PosSemidef.eigenvalues₀_eq_zero_of_rank_le` (rank in the name, per the backlog) with `i` implicit, now a short counting corollary. PosSemidef is genuinely required for (c) — a rank-one Hermitian with a negative eigenvalue sorts it last, so the tail is not zero — but NOT for (a); separating them is the point. Does NOT touch §8.2 NearIsometry/PolarIsometry (edward) or §12.1 Family (edward). | 2026-07-27 | done — **§9 is now fully resolved and pruned from the backlog (9.1 + 9.2 both closed).** All three declarations axiom-clean; ForTauCeti green (3396 jobs). The backlog's open question ("is the natural conclusion support/cardinality, with the pointwise theorem a corollary?") answered YES: both supporting facts were buried inside the one proof. Exports: `IsHermitian.rank_eq_card_non_zero_eigenvalues₀` (sorted counterpart of Mathlib's `rank_eq_card_non_zero_eigs`), `PosSemidef.eigenvalues₀_nonneg` (sorted counterpart of `eigenvalues_nonneg`), and the renamed headline `PosSemidef.eigenvalues₀_eq_zero_of_rank_le` with `i` implicit. FINDING: PosSemidef is **essential** to the headline (a rank-one Hermitian whose nonzero eigenvalue is negative sorts it LAST, so the tail is not zero) but **inessential** to the rank count — the old proof carried the hypothesis through both only because the statement it served needed it; separating them is the substance of the pass and makes the counting lemma the more reusable export. Mathlib's index equivalence between the two eigenvalue indexings is kept private (implementation detail; no public statement mentions it). **CORRECTION (same day, caught by edward): "no consumers existed to repoint" was WRONG.** My consumer grep covered ForTauCeti/DavisKahan/the four paper libraries but excluded `Challenge/` and `comparator/`, and `Challenge` is not in `defaultTargets`, so neither the grep nor the full build saw that `Challenge.MathlibPending.RankPsdRealization.Leaderboard` consumes this declaration by its old name — the rename broke it outright (`Unknown constant`). Edward's comparator conformance-gate lane picks up the repair. LESSON for every rename lane: grep the WHOLE repo (including `Challenge/` and `comparator/*.json`) and remember the default build does not cover `Challenge`. |
| edward (resumed) | NEW ForTauCeti/Analysis/OperatorIdeal/Family/{Basic,OperatorNorm}.lean; NEW DavisKahan/Interop/TauCeti/RectangularFamilyAdapter.lean; dev/tauceti-signature-polish-todo.md §12.1; dev/tauceti/convergence-matrix.md (symmetric-ideal row); ForTauCetiRoadmap/ApproximationNumbers/README.md (the roadmap area that already owns `TauCeti/Analysis/OperatorIdeal/` — a separate area folder would have split it). **Does NOT touch** DavisKahan/OperatorIdeal/UnitarilyInvariant/RectangularFamily.lean (68 consumers stay on the old structure this pass) or Experimental/InfiniteDimensional/Ideals/Symmetric.lean (jon's `sinAngle_reflected_mem_gauge_eq` leaf) | Tau Ceti P0 §12.1 **symmetric-ideal-family extensionality defect**: `RectangularSymmetricIdealFamily` carries a *total* gauge on every operator while the laws constrain it only on members, so two families agreeing on every ideal element can differ — defeating extensionality ("free data" failure mode). Also: zero/add/smul/completeness are hand-rolled structure fields rather than typeclasses, and source/target share one universe. Canonical replacement: `carrier : Submodule 𝕜 (E →L[𝕜] F)` (membership + module laws for free) with the gauge defined **only on the carrier subtype** (extensionality becomes structural), a derived `NormedAddCommGroup`/`NormedSpace` instance on the carrier so completeness is `CompleteSpace`, independent source/target universes for the non-adjoint layer, and adjoint symmetry split into a diagonal-universe mixin. Plus a forgetful adapter proving the new design implies the old one, so all 68 production consumers keep compiling and the migration can be incremental | 2026-07-27 | done — canonical `TauCeti.OperatorIdealFamily` (**Banach — superseded 2026-07-28, see the `jon (namek)` §13.2 row: the space parameters are now Hilbert, because the Ky Fan/Schatten gauges cannot instantiate a Banach-wide family**; independent source/target universes) + `SymmetricOperatorIdealFamily` (Hilbert diagonal, adjoint-invariant) landed with the operator-norm instance and a full forgetful adapter to the legacy record. **Representation decided: one `ℝ≥0∞`-valued total gauge as the sole data field**, with the ideal derived as its finiteness domain (`carrier : Submodule`). Consequences: `ext` becomes provable (the defect is gone *structurally* — a membership-plus-gauge pair has nothing to be extensional about off the ideal); all four remaining laws are unconditional (they hold verbatim at non-members in `ℝ≥0∞`), so no axiom and no downstream lemma carries a membership hypothesis; 14 fields → 4 (closure under `0`/`+`/`•`/`-`/finite sums is `Submodule` membership, `gauge 0 = 0` follows from homogeneity at `c = 0`, definiteness from `‖A‖ₑ ≤ gauge A`); completeness is the `IsComplete` mixin = `CompleteSpace (N.Elem E F)` for the *ideal* norm (`Elem` must be a type synonym — the bare subtype already carries the operator norm and the two differ), replacing the hand-rolled ε–N criterion. **FINDING (obstruction, recorded in the module docstring and roadmap):** adjoint symmetry is incompatible with independent source/target universes — the adjoint exchanges the two — so the layering into two structures is forced, not stylistic. Validation: `SymmetricOperatorIdealFamily.toRectangular` derives every one of the legacy record's 14 fields including `gauge_complete`; there is deliberately no inverse. All 13 new declarations axiom-clean; full build 9270 green; layer check 688 OK; census CLEAN. The 68 production consumers are untouched and migrate incrementally; `KyFanDominantIdealFamily` has the same free-data shape and should become a mixin during that migration. Docs: backlog §12.1 rewritten as RESOLVED (bullet-by-bullet), matrix row + P0 table rows updated, roadmap decision 7 added with full rationale. |
| edward (resumed) | ForTauCeti/Analysis/InnerProductSpace/NearIsometry.lean (rewrite); NEW ForTauCeti/Analysis/InnerProductSpace/PolarIsometry.lean; dev/tauceti-signature-polish-todo.md §8.2 + the P0 table row; consumers Acharyya2025/PolarFactor.lean and Challenge/MathlibPending/NearIsometry/{Conformance,Leaderboard}.lean. **Does NOT touch** ForTauCeti/Analysis/CStarAlgebra/SelfAdjointGapInverse.lean (jon's §9.1) — will not consume `IsSelfAdjoint.norm_le_of_spectrum_subset_Icc`; nor DavisKahan/Geometry/Polar/** (jon's polar campaign; different object — that `polarIsometry` is the *partial* isometry of a general operator, this one is the *isometry* of a bounded-below operator) | Tau Ceti P0 §8.2 **near-isometry redesign**: `exists_linearIsometryEquiv_norm_sub_le` returns the canonical polar factor only *existentially*, is duplicated verbatim at `LinearMap` and `ContinuousLinearMap` level, is ℝ-and-finite-dimension-only for no mathematical reason, and hides the surjectivity argument. Plan: (1) NEW canonical bundled object `ContinuousLinearMap.polarIsometry : E →ₗᵢ[ℂ] F` for any `M` with invertible modulus (`IsUnit M.modulus`), over **general** Hilbert spaces — no finite-dimensionality — with the polar identity `M = W ∘L \|M\|` and the pointwise estimate `‖M x - W x‖ ≤ ‖\|M\| - 1‖ * ‖x‖`; (2) the square-root contraction `‖\|M\| - 1‖ ≤ ‖M⋆M - 1‖` (algebraic: `(\|M\|-1)(\|M\|+1) = M⋆M-1` with `‖(\|M\|+1)⁻¹‖ ≤ 1`), which **sharpens the constant from `2 * δ` to `δ`** and drops `δ ≤ 1/2` to `δ < 1`; (3) `LinearIsometryEquiv` only as an explicitly-hypothesised corollary (surjective, or finite-dimensional with equal finrank); (4) repair the ℝ finite-dimensional theorem in place to the same sharp constant via the polar identity `M = W ∘ G^(1/2)` (kills the lossy `‖M‖ ≤ √(1+δ)` step), keep ONE public theorem per level, and replace the scalar lemma `abs_one_sub_inv_sqrt_le` by the cleaner `\|√μ - 1\| ≤ \|μ - 1\|` staged for `Real.Sqrt` | 2026-07-27 | done — all four parts landed. NEW `PolarIsometry.lean`: canonical `ContinuousLinearMap.polarIsometry M = M ∘L Ring.inverse M.modulus`, **total** in `M` (junk off the bounded-below locus, `Ring.inverse` style) so it rewrites/composes, with `IsUnit M.modulus` carried by each theorem; polar identity, isometry, bundled `polarLinearIsometry`/`polarLinearIsometryEquiv` (surjectivity **explicit**, no hidden injective⇒surjective), all over **arbitrary complex Hilbert spaces, rectangular** — the old statement's finite-dimensionality and squareness were never needed. NEW `SpecialFunctions/Sqrt.lean` holds both scalar lemmas so the real and complex developments share them without importing each other. **Constant sharpened `2δ → δ` and hypothesis weakened `δ ≤ 1/2 → δ < 1`, in BOTH developments**: exposing the factorization makes `‖M x - W x‖ = ‖S x - x‖` an *equality*, so the lossy `‖M‖ ≤ √(1+δ)` and `√(1+δ) ≤ 2` steps are deleted and the sharp scalar lemma `\|√μ-1\| ≤ \|μ-1\|` (no smallness hypothesis) replaces the inverse-square-root one. The real `exists_linearIsometryEquiv_comp_polarFactor` also pins the factor: `S` symmetric with `S ∘ S = Mᵀ ∘ M`, i.e. `S` **is** the modulus, so the canonical object is recoverable from the existential. **FINDING (recorded in both docstrings + backlog §8.2):** the requested ℝ→RCLike generalization is blocked *upstream*, not by the proof — Mathlib registers the C⋆-algebra instance on `E →L[𝕜] E` only for `𝕜 = ℂ`, so there is no real operator square root; the two-development split is therefore forced, and the real file is now documented as the specialization that will be deleted if a real square root lands. Historical `exists_linearIsometryEquiv_norm_sub_le` kept **verbatim** at both levels as one-line corollaries (Acharyya2025 paper fidelity + comparator full-type export check). Also fixed: `comparator/pending-near-isometry.json` still named pre-dedup `ForMathlib.*` decls and could never have passed — repointed, now PASS. **~20 other comparator configs (36 names) have the same staleness — unclaimed follow-up lane, see below.** All 22 declarations axiom-clean; full build 9272 green; layer check 690 OK; census CLEAN (48). |
| jon | ForTauCeti/Analysis/CStarAlgebra/SelfAdjointGapInverse.lean; dev/tauceti-signature-polish-todo.md §9.1; the 4 consumer files below | Tau Ceti P0 §9.1 self-adjoint gap-inverse redesign — BOTH declarations in the file. (1) `IsSelfAdjoint.norm_le_of_spectrum_subset_Icc`: reuse audit against Mathlib's self-adjoint norm/spectral-radius + CFC spectrum API; keep only if no direct replacement exists, else delete and repoint. (2) `IsSelfAdjoint.exists_two_sided_inverse_of_spectrum_gap`: REDESIGN off the existential — the bundled `∃ j, j * a = 1 ∧ a * j = 1 ∧ ‖j‖ ≤ r⁻¹` is not characteristic C\*-algebra API. Target split: `IsSelfAdjoint.isUnit_of_abs_spectrum_ge` + `IsSelfAdjoint.norm_inv_le_of_abs_spectrum_ge` over the canonical `Ring.inverse`/`⅟`, with the hypothesis restated to whichever of `∀ x ∈ spectrum ℝ a, r ≤ \|x\|` / `spectrum ℝ a ∩ Set.Ioo (-r) r = ∅` / `r ≤ Metric.infDist 0 (spectrum ℝ a)` matches existing Mathlib API. Consumers to repoint (8 call sites, 4 files): DavisKahan/Sylvester/GenuineSpectrum.lean (105, 107, 374, 376), DavisKahan/TanTheta/GenuineSpectrum.lean (69, 153), DavisKahan/TanTheta/UnboundedGenuineSpectrum.lean (131), DavisKahan/Experimental/InfiniteDimensional/Riccati/BoundedExistence.lean (269, 272). Does NOT include §9.2 Matrix/Spectrum.lean (`PosSemidef.eigenvalues₀_eq_zero_of_le`) — left unclaimed. | 2026-07-27 | done — both P0 items settled, all three declarations axiom-clean, backlog §9.1 pruned. **(1) Reuse audit: no direct Mathlib replacement, but the honest statement is an iff.** `norm_cfc_le_iff` (isometric CFC) specialized through `cfc_id` gives *both* directions from the same three-line proof, so the one-directional form was strictly weaker than what the proof yields; kept and strengthened to `IsSelfAdjoint.norm_le_iff_spectrum_subset_Icc : ‖a‖ ≤ r ↔ spectrum ℝ a ⊆ Set.Icc (-r) r`. One-directional form NOT retained (consumers use `.mpr`). **(2) Existential eliminated**, split into `isUnit_of_forall_le_abs` + `IsSelfAdjoint.norm_ringInverse_le` over the canonical `Ring.inverse`. FINDING #1: invertibility needs **neither self-adjointness nor a C\*-algebra** — it is `spectrum.isUnit_of_zero_notMem` plus "a gap around 0 excludes 0", so that lemma is stated at `[Ring A] [Algebra ℝ A]` and deliberately sits *outside* the `IsSelfAdjoint` namespace (carrying `ha` there would be an unused hypothesis, exactly the §3 review target). FINDING #2: the old ~30-line proof was **reimplementing Mathlib** — it built the inverse by hand from two `cfc_mul`/`cfc_congr` round trips, which is precisely `cfcUnits`/`cfc_inv`; `cfc_ringInverse_id` states the identity outright, collapsing the proof to four lines and yielding the canonical inverse instead of an anonymous witness. Hypothesis kept as `∀ x ∈ spectrum ℝ a, r ≤ \|x\|` (not the `Ioo`/`infDist` alternatives the backlog floated): all four call sites discharge it pointwise, and it is the form `norm_cfc_le` consumes. Split Mathlib destinations recorded (Isometric.lean for the norm results; Spectrum/Basic.lean for `isUnit_of_forall_le_abs`). Consumers repointed: 8 call sites / 4 files — 3 of the 8 destructured a `_hJ2` they never used, direct evidence the conjunction was over-packaged. Gates: full build green, layer check 684 OK, census CLEAN (48). |
| edward (resumed) | ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/{Basic,Adjoint,MinMax,FiniteDimensional}.lean + new NNReal transitional shim; ForTauCetiRoadmap/ApproximationNumbers/README.md; dev/tauceti-signature-polish-todo.md §5; paper-layer consumers of `approximationNumber` | Tau Ceti P0 (flagship PR A1 blocker): settle the **approximation-number codomain**, currently recorded as *Open*. Decision + execution: canonical `ContinuousLinearMap.approximationNumber : ℝ` (Mathlib precedent `Metric.infDist`; both boundaries currently pay a tax — `⟨v, proof⟩` constructor noise against `singularValues : ℝ` upward, and a real-valued `approximationSingularValue` wrapper downward used in 27 files), with `approximationNumber_nonneg`; historical ℝ≥0 API preserved for the paper layer as a documented not-for-upstream shim so downstream proofs keep compiling | 2026-07-24 | done — decided **ℝ**, and executed with NO ℝ≥0 API retained (no shim needed): the paper layer turned out to have only ~33 genuinely NNReal-typed lines, so a direct conversion was feasible and "do not carry both APIs" holds exactly. Canonical `approximationNumber : ℝ` + `approximationNumber_nonneg` (two private facts — a `Nonempty` index instance and a `BddBelow` range lemma — replace lattice completeness, discharged once in the two characterization lemmas). Payoff: the flagship Eckart–Young statements lost their `⟨value, proof⟩` constructor noise (`T.approximationNumber n = T.toLinearMap.singularValues n`), and two whole NNReal↔ℝ bridging layers (complex + real Courant–Fischer localization) plus ~a dozen `congrArg (coe)`/`simpa` wrappers were deleted. 12 downstream files converted. Full build green (9266); layer check 684 OK; census CLEAN; headline declarations axiom-clean. Decision + rationale recorded in ForTauCetiRoadmap/ApproximationNumbers/README.md (decision 2) and backlog §5. |
| edward (resumed) | ForTauCeti/Analysis/InnerProductSpace/{OperatorModulus(new),OperatorAbsoluteValue}.lean, ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/OperatorModulus.lean (delete), DavisKahan/OperatorIdeal/ApproximationNumbers/OperatorModulus.lean (dedup) + its 3 rectangular consumers | Tau Ceti P0 §7 operator-modulus unification: ONE canonical `ContinuousLinearMap.modulus` (rectangular, `CFC.sqrt (T.adjoint ∘L T)`) replacing all THREE overlapping copies (`TauCeti.operatorAbs`, ForTauCeti `rectangularOperatorModulus`, and the still-live DavisKahan-side `rectangularOperatorModulus` redefinition); square case becomes specialization theorems; `operatorAbs` demoted to a transitional shim | 2026-07-24 | done — canonical `ContinuousLinearMap.modulus` landed with the full 14-declaration API; the dead ForTauCeti copy deleted, the DavisKahan-side redefinition excised (73 lines) and its 3 consumers repointed, `operatorAbs` reduced to a reducible alias in a documented not-for-upstream shim. Generalizations beyond dedup: the square-only composition laws are now rectangular (`norm_modulus_comp` from the pointwise isometry, `norm_comp_modulus` by adjoint conjugation), as are uniqueness and commutation. All 14 declarations axiom-clean; full build 9266 green; layer check 684 OK; census CLEAN. Backlog §7 pruned; migration name map recorded. |
| edward (resumed) | ForMathlib → ForTauCeti: the CourantFischer weakly-connected component (37 modules: the singular-value/UI-norm/frame subgraph + the probability/statistics cluster per the recorded phase-0 classification), ForMathlib.lean aggregate, all consumer import/name repoints, new DavisKahan/Interop/TauCeti/CourantFischerCompat.lean (transitional adapter with the old predicate-based CF signatures) | Wave-1 CourantFischer dedup commit: delete ForMathlib/CourantFischer.lean (ForTauCeti carries the redesigned API), migrate the closure in one commit (firewall forces the weakly-connected component to move together), namespace `ForMathlib.*` → `TauCeti.*`, provenance headers (module-system conversion deferred — see migration doc) | 2026-07-24 | done — 37-module component migrated in one commit; ForMathlib/CourantFischer.lean deleted; historical CF signatures ride the transitional CourantFischerCompat shim; ~380 paper-layer files flipped from extending `namespace ForMathlib` to `namespace TauCeti`; non-elaborating DkVariant comparison record parked in dev/alternates/. ForMathlib now 12 modules. Full build 9266 green; layer check 684 OK; census CLEAN (48). |
| edward (resumed) | ForTauCeti/Analysis/InnerProductSpace/{CourantFischer,BasisSpan(new)}.lean, dev/tauceti/convergence-matrix.md + formathlib-to-fortauceti-migration.md (phase-0 classification), dev/tauceti-signature-polish-todo.md §6 | Tau Ceti P0: Courant–Fischer API redesign on the canonical ForTauCeti copy (specSubspace → OrthonormalBasis.spanIndices generalized off Fin/predicates; eigenvalue API into LinearMap.IsSymmetric namespace; genuine sup-inf Courant–Fischer endpoint theorem; Weyl at ContinuousLinearMap level) + phase-0 stats-subsystem classification unblocking the CourantFischer dedup. ForMathlib originals untouched (dedup repoints once, later, to the final API). | 2026-07-24 | done — polish-todo §6 fully executed and pruned (living checklist). New `BasisSpan.lean`: `OrthonormalBasis.spanIndices` over a general finite index type and `Set` selection with the complete basic API (membership iff, Finset/Set finrank, orthogonal complement, monotonicity). `CourantFischer.lean`: eigenvalue API in the `LinearMap.IsSymmetric` namespace (`re_inner_apply_self_…` naming, `eigenvalue_mono`, `exists_submodule_forall_unit_…` quantifier fix), NEW headline `eigenvalues_eq_iSup_iInf_re_inner` (the genuine sup-inf Courant–Fischer equality — previously missing), Weyl at CLM level `abs_eigenvalue_sub_eigenvalue_le_norm` with `‖T−S‖` (coe-IsSymmetric hypotheses; no toContinuousLinearMap, no Star instance needed). Phase-0 decision recorded in convergence-matrix.md (stats chain = own ForTauCeti cluster riding the dedup commit; evidence: linear chain, zero paper consumers); dedup name map added to formathlib-to-fortauceti-migration.md. All 16 new/renamed declarations axiom-clean; ForTauCeti green (3207 jobs); full default build green (9266 jobs); dependency-layer check OK (685 modules). |
| edward (stopped) | Sylvester/{FourierSemigroup,OrderedSemigroup,CompactIntegral,FiniteBlockReconstruction}.lean | whole Sylvester analytic lane (sorry-free) | 2026-07-23 | done |
| edward (stopped) | Sylvester/Basic.lean | norm_sylvester_le_of_orderedSeparation, compact_mem_of_separatedSylvester_solution | 2026-07-23 | done |
| edward (stopped) | InfiniteDimensional/GraphSubspace.lean | acuteAngularOperator, acuteAngularOperator_spec, acute_iff_exists_bounded_angularOperator | 2026-07-23 | done |
| jon | MathAhead/HiddenFoundations/PolarIsometryFinal.lean | polarIsometry_comp_adjoint_self | 2026-07-23 | done |
| jon (window A) | MathAhead/HiddenFoundations/Section3Nonacute.lean, Interop/Spectra/{DirectRotation,OperatorAbsoluteValue}.lean, Frontier/Section3.lean | nonacute polar campaign — DONE. Three primary leaves closed axiom-clean: sourceCompression_nonnegative, crossed_blocks_general (via W+W*=2\|C\|), crossedDefectEquivOfPaperDirectRotation (via T+T* ≥ 0, no predicate strengthening needed). proposition3_2_completed (both dirs) + parameterization proved. PROMOTED to Frontier/Section3: proposition3_2_exists_iff_crossedDefectsEquivalent, proposition3_2_parameterized_nonuniqueness, corollary3_2_reversal_source_form (Frontier/Section3 now imports Section3Nonacute — first Frontier→MathAhead edge; acyclic since the construction never imports Frontier/Section3). Remaining Frontier/Section3 sorries: Prop 3.1/3.3/3.4/3.5, classification. NOTE: does NOT include PolarIsometryFinal.lean adjoint_polarIsometry (window B) | 2026-07-23 | done |
| jon (window B) | MathAhead/HiddenFoundations/PolarIsometryFinal.lean | adjoint_polarIsometry ONLY: (polarIsometry T)† = polarIsometry (T†) | 2026-07-23 | done — proved axiom-clean (propext/Classical.choice/Quot.sound only). Route: the abs-adjoint conjugation `\|T†\| = U\|T\|U†` via C⋆ square-root uniqueness (`CFC.sqrt_unique`; conjugate is `star_right_conjugate_nonneg` and squares to `T T†` through `U†U = P_K`, `U\|T\| = T`, `\|T\|U† = T†`). From it the left decomposition `T = \|T†\|U`, hence `W ∘L T = \|T\|` and `U† ∘L T = \|T\|` (Spectra `polarIsometry_adjoint_comp`). Then `U† = W` by agreement on `L = closure(range T)` (density: `closure_minimal` on the closed equalizer) and common vanishing on `Lᗮ = ker T†` (initial/final projection identities + `\|T†\|` self-adjoint kernel), assembled by the orthogonal split `x = P_L x + (x−P_L x)`. No new `closure_range_abs_adjoint` lemma was needed — the pointwise conjugation identity subsumes it. Parse note: postfix `†` binds below application, so `(A†) (v)` needs the extra parens. |
| jon | SinTheta/RCLikeSpectralBridge.lean (new), SinTheta/SpectralBridge.lean (Experimental) | phantom machinery + estimate repair; ALL RCLike leaves now closed (pencil invertibility at non-real parameters proved directly, inverse spectral mapping via spectrum.map_inv, false normal-norm leaf removed) — both files sorry-free | 2026-07-23 | done |
| jon | SinTheta/General.lean, DoubleAngle.lean, DirectRotation.lean | Continuation-chain repair: all three elaborate (documented leaf obligations); ContinuationCore → ContinuationSpectralIdentification builds green | 2026-07-23 | done |
| jon | SinTheta/RestrictionCompat.lean | restrictedSpectrum_top_eq_realSpectrum_general, boundedRealSpectrum_eq_realSpectrum, mem_realResolventSet_ofBounded_iff | 2026-07-23 | done |
| jon | Frontier/Core.lean, Frontier/RieszCircle.lean | circle Riesz lane: all 9 obligations closed (RieszCircle sorry-free, incl. the projection identity via the new contour-free SinTheta/CayleySelectorBridge.lean); signatures 4 and 5 gained the necessary `0 ≤ radius` | 2026-07-23 | done |
| edward (resumed) | Frontier/Section8.lean (DirectRotationCompression + SourceTheorems sections — ALL 9 remaining sorries) | directRotationUpper/LowerCompressionData, theorem8_1_upper/lowerCompressionRepulsion_of_directRotation, Theorem81SourceConclusion (restatement), theorem8_1_selectedBranch_and_spectralRepulsion, perturbation/residualHalfGapBridge_of_sourceHypotheses, theorem8_2_perturbation/residualHalfGap_selectedBranch. NOTE: current statements are false as stated (placeholder `id id` blocks force `2‖x‖²=‖x‖²`; 8.1 compression reduces to a sign condition on E; 8.2 bridges lack the quantitative contour link). Honest restatement: the paper's 8.1(i) cosine-block inequality at quadratic-form level needs only the orthogonal target splitting (cross terms vanish since the target branch reduces A+E) + the SpectralOrder.Complex restriction-spectrum form-bound bridges — no direct rotation. 8.2 gains the explicit `radius·‖E‖/margin² < √2/2` hypothesis. | 2026-07-23 | done — Section8.lean is now SORRY-FREE. All 9 restated declarations proved axiom-clean: target-splitting compression certificates (`upper/lowerCompressionRepulsionData_of_targetSplitting`, kernel-side form shifted by the cut so its global bound is the branch form bound), the faithful 8.1(i) compression inequalities, `Theorem81SourceConclusion` (compression fields restated to compare the perturbed form on the old branch with its cosine-block compression into the new branch), the 8.1 assembly, and both 8.2 alternatives via the half-gap bridges with the quantitative circle hypothesis. New helpers: `re_inner_le_of_spectrumIn_Iic`/`le_re_inner_of_spectrumIn_Ici` (SpectrumIn → quadratic-form bound via SpectralOrder.Complex bridges) and `re_inner_splitting_of_invariant` (cross terms vanish through a reducing subspace). Open analytic step recorded in docstrings: constructing the quantitative circle datum from the printed half-gap hypotheses (Krein replacement for the residual case). |
| edward (resumed) | Frontier/Core.lean, Frontier/Section3.lean | genericHalmosCosineSq, genericHalmosSineSq, genericHalmosCosineSq_add_sineSq (Core), crossed_intersections_are_halmos_defects (Section3) — Halmos generic-angle operators via compression to the generic summand | 2026-07-23 | done — all four proved sorry-free, axiom-clean. Defs = compressOperator of the full-space halmosCosineSq/SineSq (the generic part reduces both projections, so compression is the honest restriction); Pythagoras from the full-space resolution of the identity + linearity of compression; crossed intersections are definitional (`⟨rfl, rfl⟩`). Unblocks the Section3 classification statements that consume genericHalmosCosineSq. |
| jon | DoubleAngle.lean | reflection-transport leaf closure: reflectedSubspace_orthogonal (new) + reflectedSubspace_hasOrthogonalProjection + reduces_reflectedSubspace PROVED (self-adjoint involution: (J''U)ᗮ = J''(Uᗮ); conjugated projection is an idempotent with reflected range). DoubleAngle 8→6 leaves; chain + Section8 stay green | 2026-07-23 | done |
| edward (resumed) | Frontier/Section8.lean (ContinuationBridge section), Frontier/CircleContour.lean (new) | spectralContinuationWitness_of_circle, spectralContinuationWitness_of_circle_endpoints, selectedBranchProjectionLipschitzConstant_of_circle + the circle instantiation of PiecewiseC1ClosedContour/SpectralSeparatingContour | 2026-07-23 | done — all three proved sorry-free after jon's Continuation-chain repair unblocked SpectralContinuationWitness. Statement repairs along the way: Section8.lean had never elaborated (Complex.abs, missing Inv instance, wrong IsSymmetric.smul, phantom sourceSelectedProjection fields, subtype inner-product coercions in the compression sections — all fixed, sorried bodies of the unclaimed sections untouched); Sources/Section8/Smallness.lean had a dangling docstring parse error (now `/-!`). CircleContinuationData was restated over operatorPath with Ring.inverse; endpoints theorem restated via starProjection of the selected subspaces. Section8.lean now compiles; its remaining 9 sorries are the unclaimed DirectRotationCompression/SourceTheorems sections. |
| jon | Frontier/Lemma63.lean, MathAhead/Lemma63.lean, AGENTS.md | Section 6 Lemma 6.3 promotion: correct the overstrong block hypothesis to the source-faithful `K∘P = Q∘K∘P`, lift the MathAhead proof up to close both Frontier sorries (approximation-number + finite singular-value forms), slim MathAhead to a re-export shim; codify the scratch→promotion workflow in AGENTS.md | 2026-07-23 | done — both Frontier theorems proved axiom-clean (no sorryAx), 2 sorries removed; the previous `K∘P = Q∘K` signature trivialized leakage (`Q∘K∘(1−P)=0` outright). Machinery ported: prefix-square-energy left-compression bound, paper HS energy domain split (basis-free Pythagoras), rank bounds, opNorm-via-zeroth-approx (needs 0<n), finite singular-value bridge. GeometryAll + Frontier.All green (8899 jobs); default build unaffected (Experimental-only). |
| jon | DoubleAngle.lean | finiteGap_mixedIntervalExterior, internalGap_reflection_transport, sinAngle_reflected_eq_sinTwoAngle, doubleAngle_directedGap_identity + supporting lemmas (restricted-spectrum invariance under reflection conjugation, starProjection of reflectedSubspace, cross-block identity, reflection norm invariance) | 2026-07-23 | done — all four proved axiom-clean. Consequences: sinTwoTheta_reflectionDefect and sinTwoTheta_perturbation are now FULLY PROVED (no sorryAx). DoubleAngle 6→2 leaves (sinAngle_reflected_mem_gauge_eq blocks ideal_sinTwoTheta; reflectionDefect_range_le_residual blocks sinTwoTheta_residual). sinTwoTheta_generalSeparation waits only on General.lean's norm_sylvester_le_of_generalSeparation_rclike. Key steps: units-transfer conjugation for restricted spectra via codRestrict intertwiners; P_{JU} = J P J via eq_starProjection_of_mem_orthogonal; cross-block identity P_{Uᗮ} J P_U = sinTwoAngleOperator; norm_starProjection_sub_eq_max collapses the symmetric gap. |
| jon (window B) | MathAhead/HiddenFoundations/HalmosClassification.lean (new), Frontier/Section3.lean (Classification section: sameHalmosOperatorInvariant_of_pairEquiv (new), twoProjection_operator_classification) | Thm 3.1 operator-level Halmos classification spine | 2026-07-23 | done (forward direction) — FORWARD half of twoProjection_operator_classification proved axiom-clean and PROMOTED as sameHalmosOperatorInvariant_of_pairEquiv. HalmosClassification.lean (imports only Frontier/Core): isometryEquiv_intertwines_projection (Submodule.starProjection_map_apply conjugation), summandEquiv (e.submoduleMap + ofEq), intertwines_halmosCosineSq (e intertwines the projection polynomial), sameHalmosInvariant_of_pairEquiv (four elementary summand equivs via map_inf/map_orthogonal_equiv + generic cosine² intertwining). The iff's forward arm is now real; the sorry is localized to the BACKWARD obligation only. Two remaining bricks documented on the theorem: (1) generic 2×2 Halmos model upgrading a bare cos²Θ unitary-equivalence to a pair-unitary intertwining both projections — the sole genuinely missing mathematics (needs the reducing-angle-pair reconstruction from cos²Θ, absent from Mathlib/Spectra); (2) block-diagonal Hilbert-sum assembly gluing the four elementary summand isometries + generic pair-unitary (on the four elementary summands intertwining is automatic, so this is pure Hilbert-sum gluing). Items 2–3 (SameSpectralMultiplicity/theorem3_1_spectralMultiplicity_classification, compactAngleEigenvalueList/corollary3_1) left untouched: both are sorry DEFINITIONS needing direct-integral multiplicity theory resp. an infinite-dimensional compact self-adjoint spectral theorem — neither exists in pinned Mathlib v4.32.0, so a placeholder def would be a fake. Did NOT touch OneSpace (window A). |
| jon (window A) | Frontier/Section3.lean (OneSpace section: proposition3_1_positivity_characterization, proposition3_3/3_4/3_5, fixedCosineSubspace) | Props 3.1/3.3/3.4/3.5 acute direct-rotation cluster | 2026-07-23 | done — Prop 3.1 DONE (axiom-clean); 3.3/3.4/3.5 assessed, documented, NOT faked. **Prop 3.1** proved axiom-clean. AUDIT FINDING #1: `IsPaperDirectRotation`'s diagonal-compression fields are numerical (`0 ≤ re⟪x,(PTP)x⟫`), strictly weaker than operator positivity, so they do NOT force uniqueness — finite-dim counterexample: on `U=V=H`, every scalar `exp(Iθ)`, `\|θ\|<π/2`, satisfies all five fields yet ≠ 1. Minimal source-faithful fix = self-adjoint diagonal compressions (the genuine cosine blocks are); added as two `IsSelfAdjoint` hypotheses to Prop 3.1 (rather than mutating the shared Core.lean def while window B active). Proof: SA diagonals + skew crossed blocks ⇒ four `star T` blocks match `R_U T R_U` ⇒ `T = R_U(star T)R_U` ⇒ `T² = R_V R_U`; accretivity fixes the branch via `spectraDirectRotation_unique_of_sq`. AUDIT FINDING #2 (Prop 3.4, documented in its docstring, left as open obligation): the printed half-angle bound `re⟪x,\|S\|x⟫ ≥ ‖x‖²/2` is mis-stated — the reflected-pair route (`Uref=U`, `Vref=reflectedSubspace V U`, square = `R_V R_U`) needs `re⟪(R_V R_U)x,x⟫ ≥ 0` = `2 re⟪halmosCosineSq x,x⟫ - ‖x‖² ≥ 0`, i.e. the bound belongs on the cosine SQUARE `\|S\|²` (cos ≥ 1/√2), and must be a uniform strict gap for `Vref` to be acute. **Prop 3.3** now ALSO DONE axiom-clean (2026-07-24) — my earlier "wall" call was wrong. Route avoids the nonacute spectral decomposition entirely: for ANY RHP unitary square root T of G=R_V R_U, `T + star T = 2\|S\|` (S = canonical intertwiner) by positive-sqrt uniqueness of `(T+starT)²=4 star S·S`. That yields the two compression fields (accretive) and the skew crossed-blocks field (\|S\| commutes with P). `T\|S\|=S` kills X=T·P−Q·T on ran\|S\|; on ker\|S\|=ker S the orthogonal split (V⊥Vᗮ) puts Px∈sourceDefect, Pᗮx∈targetDefect, and hcross + `T²=−1 there` handle X on ker\|S\|; (ker\|S\|)ᗮ=closure(ran\|S\| ) assembles X=0 (intertwining). **3.5**+`fixedCosineSubspace` still open but NOT a wall: the compiled `boundedSelfAdjointSpectralSubspace` PVM machinery (`InfiniteDimensional/Core/SpectralProjection.lean`) + `halmosCosineSq_commute_projection` supply the spectral-subspace-with-reduction foundation (define fixedCosineSubspace as the cosine²-spectral subspace at {c²}); remaining work = reduction by projection V + maximality. **3.4** open with a documented hypothesis correction (Finding #2). Did NOT touch Classification section (window B) or Core.lean. |

| jon | DoubleAngle.lean, DoubleAngleGenuine.lean | reflectionDefect_range_le_residual (leaf closure) → sinTwoTheta_residual axiom-clean | 2026-07-24 | done — leaf `reflectionDefect_range_le_residual` PROVED axiom-clean (propext/Classical.choice/Quot.sound only), so `sinTwoTheta_residual` is now fully proved. Route: moved `reflectionDefect_eq_neg_two_smul_offdiag`, `offdiag_adjoint`, `norm_reflectionDefect_le_two_mul_norm_cross`, `norm_cross_le_norm_residual` UP from DoubleAngleGenuine into DoubleAngle, generalizing ℂ→𝕜 (RCLike); deleted the ℂ copies (downstream Genuine users resolve against the moved RCLike versions since ℂ is an RCLike instance). Leaf = `‖reflDefect V A‖ ≤ 2‖P_{Vᗮ}AP_V‖` (sharp off-diagonal estimate) chained with `‖P_{Vᗮ}AP_V‖ ≤ ‖residual‖` for `V = range X` (hmem/hsurj trivial). Only ℂ→𝕜 gotcha: `norm_star _` gets stuck on `NormedStarGroup ?m` metavariables at general RCLike — pass the argument explicitly (`norm_star T₁`). DoubleAngle now has exactly ONE remaining leaf: `SymmetricNormIdeal.sinAngle_reflected_mem_gauge_eq` (blocks `ideal_sinTwoTheta`) — untouched (separate hard leaf). DoubleAngleGenuine stays sorry-free/green. |
| jon (window A) | Frontier/RemainingSourceSurface.lean (trialResidual, section7_sinTwoTheta_source_ideal, section7_tanTwoTheta_source_ideal), Frontier/Section3.lean (Props 3.3 done) | close-out sweep of near-complete source statements | 2026-07-24 | done — 3/5 RemainingSourceSurface items DONE axiom-clean: `trialResidual` defined concretely (`Zᗮ.starProjection ∘L T ∘L Z.subtypeL`); `section7_sinTwoTheta_source_ideal` = one-line instantiation of the proven `sinTwoTheta_addBounded_gauge_of_spectrum_gap` (DoubleAngle/UnboundedIdeal); `section7_tanTwoTheta_source_ideal` promoted via `tanTwoTheta_addBounded_gauge_of_spectrum_gap` AFTER correcting the RHS — FINDING #3: the printed bound `2·N.gauge E` was too strong; the tangent block carries the intrinsic double-cosine denominator `(2·N.gauge E)/(1 − 2·directedGap²)` (positive under quarter-acute). `theorem5_1_banach_sylvester` ALSO DONE axiom-clean — promoted the complete scratch proof under the corrected hypothesis. FINDING #4: the printed bounded-below separation is insufficient on general Banach (range need not be complemented ⇒ no bounded left inverse); the faithful hypothesis is an explicit `BoundedLeftInverseData A (γ+δ)⁻¹` (moved the structure + `map_zero`/`comp_le_mul`/`comp_left_le_mul` scaling lemmas into the frontier), which on Hilbert the separation supplies via `lowerFramePolarData`. ONLY `theorem6_3_generalizedTanTheta_ideal` remains in this file (genuinely NOT close — needs new IsAcute-from-spectral-gap + infinite-dim ideal-gauge tan-θ majorization for tanAngleOperatorC). SWEEP TOTAL this session: Props 3.1, 3.3 (Section3) + trialResidual, section7_sinTwoTheta, section7_tanTwoTheta, theorem5_1 (RemainingSourceSurface) = 6 endpoints, all axiom-clean. Did NOT touch Classification/Core (window B) or Section8 (edward). |
| jon (namek) | ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/Basic.lean; NEW ForTauCeti/LinearAlgebra/Dimension/RankCompLe.lean; the `DavisKahan` + `ForTauCetiRoadmap` call sites of the declarations renamed below; `dev/tauceti/extraction-manifest.json` name entries; dev/tauceti-signature-polish-todo.md §5.1 only. **Does NOT touch** §5.2 Adjoint, §5.3 FiniteDimensional, §5.4 MinMax (edward's, released but its 2 open warnings are his finding), any `comparator/*.json`, or any Challenge module | **Tau Ceti §5.1 — reconcile the section against the tree, then close every genuinely-open item.** The AUDIT block at the head of §5 says the doc and the code diverged; reconciliation is part of the lane. Plan, after a per-name consumer count: (a) **P0 `rank_comp_left_le_of_rank_le`** — "privatize or reuse" is *not* executable as written: it has **4 independent consumers** in 3 `DavisKahan` modules plus `ApproximationNumber/FiniteDimensional.lean`, so privatizing it would break them. It is also not approximation-number API at all — it is the natCast-bounded, cross-universe rank-of-composition fact, which Mathlib does not have (`LinearMap.lift_rank_comp_le_right` is the lifted form). Disposition therefore mirrors what the §5.1 `Cardinal.lift_le_natCast` item already did: state it at `LinearMap` level in its own dependency-closed, separately-upstreamable module and leave the operator-ideal PR carrying no rank plumbing. (b) Rename from the conclusion outward: `approximationNumber_eq` (0 external consumers), `lt_approximationNumber_add_pos` (2), `approximationNumber_add_le` (1), `approximationNumber_comp_right_le`/`_comp_left_le` (13 each). (c) `@[simp]` on `approximationNumber_smul`; docstring fix on `approximationNumber_zero` ("first" → "zeroth"). Rename gate per AGENTS.md: `lake build Challenge` + `scripts/check_declaration_name_drift.py` + repo-wide grep. | 2026-07-27 | done — **§5.1 has no open item left.** The P0 could not be privatized (4 independent consumers) so it moved out and generalized: `LinearMap.rank_comp_le_natCast_right` + two `ContinuousLinearMap` specializations in the new `ForTauCeti/LinearAlgebra/Dimension/RankComp.lean`, leaving `Basic.lean` with approximation-number API and no rank plumbing. Mathlib has `rank_comp_le_left`/`rank_comp_le_right`/`lift_rank_comp_le_right` but no natCast-bounded form, and the gap is structural: `rank_comp_le_right` kills the lift by forcing the outer codomain into the domain's universe, which a `ContinuousLinearMap` between independent spaces cannot do. Seven renames, each against a Mathlib precedent — **the one to know about: `approximationNumber_zero` changed meaning**, it is now the zero *operator* (matching `LinearMap.singularValues_zero`, the object we prove it equal to) and the index statement is `approximationNumber_index_zero`; `norm_add_le` decides that the subadditive `a_{m+n}(T+S) ≤ aₘ(T)+aₙ(S)` keeps the short name `approximationNumber_add_le`; `antitone_approximationNumber` → `approximationNumber_antitone` per `singularValues_antitone`. Three items verified-and-kept with reasons recorded instead of churned; two blocks were stale text. **FINDING (fixed here):** the extraction manifest's cluster was missing `ForTauCeti.SetTheory.Cardinal.Lift`, created by the *earlier* §5.1 lane — `staging_modules` is the copy set for `scripts/export_for_tauceti.py`, so an export would have emitted a TauCeti tree that could not compile. Both it and the new module are now registered (edward's `refresh_tauceti_pr1_consistency.py`, merged mid-lane, now recomputes this closure from live imports — it agrees). **FINDING (fixed here):** `ForTauCetiRoadmap/ApproximationNumbers/Suggested.lean` is not a `lean_lib`, so nothing compiles it; it had drifted to `ℝ≥0` signatures that its own README decision 2 overturned. Synchronized, and the fact that nothing checks it now says so in its header. Gates: default 9275 · `Challenge` 8894 · name-drift 0 findings · layers 0 violations. |
| jon (namek) | ForTauCeti/Analysis/OperatorIdeal/ApproximationNumber/{Adjoint,FiniteDimensional}.lean; dev/tauceti-signature-polish-todo.md §5.2 and §5.3. **Does NOT touch** Basic.lean or MinMax.lean (both closed), `comparator/*.json`, or any Challenge module | **Tau Ceti §5.2 + §5.3 — the last two open §5 subsections.** Both are small and both hinge on a reuse question the doc asks but does not answer. §5.2's single P1 wants `@[simp]` on `approximationNumber_adjoint` if it orients and does not loop (it does orient — it eliminates `adjoint` — and cannot loop), the private rank-transport kept private, and a check of whether that private proof duplicates Mathlib. §5.3 has exactly one open item, the P1 "possibly privatize" on `approximationNumber_le_singularValues`, whose stated criterion is whether both directions have independent consumers. Consumer counts run first, then the disposition follows the evidence rather than the sketch. | 2026-07-27 | done — **§5 is closed; no subsection of it is open or claimed.** §5.2: `@[simp]` added to `approximationNumber_adjoint` (it eliminates `adjoint`, so it orients and cannot loop), and both questions the item left open answered with evidence rather than assumed — the private rank transport does **not** duplicate Mathlib (`LinearMap.finrank_range_adjoint` is `finrank`-valued, so it needs finite-dimensional *ambient* spaces, which is exactly the case this helper is not about), and both `CompleteSpace` assumptions are load-bearing since `ContinuousLinearMap.adjoint` is an isometric equivalence requiring completeness on both sides. That helper also shrank 13 lines → 5 by reusing §5.1's new `ContinuousLinearMap.rank_comp_le_left` in place of a `change` down to `LinearMap` level — the first evidence the new module pulls its weight. §5.3: `approximationNumber_le_singularValues` **privatized** on the item's own criterion (its converse has an independent consumer in `DavisKahan/OperatorIdeal/ApproximationNumbers/Core.lean`; this direction has none). The asymmetry is documented on the declaration as deliberate: the public direction bounds an *arbitrary* approximant from below and is the shape that survives into infinite dimensions, while this one is built from the SVD. `@[simp]` on `approximationNumber_eq_singularValues` **declined** — it would rewrite the object this development is about into Mathlib's under any `FiniteDimensional` instance. Two finds in passing: `Basic.lean` was importing `Mathlib.Analysis.Normed.Operator.NNNorm` for an API with no `ℝ≥0` left in it (narrowed to `Operator.Basic`, which also drops an `RCLike` transitive dep from a module stated over a general normed field), and a dead `coe_nnnorm` inside a `simpa only` that the linter does not see (see cross-lane findings). **Also cleared the two `MinMax.lean:165` warnings** that the §5.4 lane released without taking — `simp [hy, norm_smul]` suffices, and `lake build ForTauCeti` is back to 0 warnings. Gates: default 9275 / 0 warnings · ForTauCeti 3400 / 0 warnings · name-drift 0 · layers 0 · PR-1 consistency OK. |
| jon (namek, §5/§13 window) | ForTauCeti/Analysis/InnerProductSpace/OperatorAbsoluteValue.lean (**deletion**); ForTauCeti/Analysis/InnerProductSpace/OperatorModulus.lean (one moved-in lemma); the `TauCeti.operatorAbs*` call sites in DavisKahan/Geometry/Angle/OperatorAngleComplex.lean, DavisKahan/OperatorIdeal/ApproximationNumbers/OperatorModulus.lean, DavisKahan/Sources/DavisKahan1970/SineTheta/Sharpness.lean and DavisKahan/Experimental/Scratch/SharedFoundations/Ideal/OperatorAbsoluteValueComplex.lean; dev/tauceti-signature-polish-todo.md §13 `operatorAbs` row; dev/tauceti/formathlib-to-fortauceti-migration.md name map. **Does NOT touch** `CourantFischerCompat` or `SymmetricOperatorIdealFamily.toRectangular` (the other two adapters — separate lanes), any `ClosedOperator`/`LinearPMap` file (the concurrently-claimed U1 lane), comparator JSON, or Challenge | **§13 adapter retirement, first of three: delete the `operatorAbs` shim.** §13's own rule is that an adapter must carry a deletion condition and that long-lived aliases obscure which API a reviewer is evaluating; this one's condition — "delete each declaration once its downstream users have moved to `.modulus`" — is written in its own header and has simply not been executed. The shim is 10 declarations, each a one-line restatement of a `ContinuousLinearMap.modulus` lemma that already exists, so the work is repointing ~30 call sites and deleting the file, not proving anything. One genuine placement fix falls out of it: `DavisKahan/Geometry/Angle/OperatorAngleComplex.lean:352` declares `_root_.TauCeti.operatorAbs_apply_eq_zero_iff` — a general fact about the modulus, injected into the *shim's* namespace from a paper-side module — which moves to `OperatorModulus.lean` as `ContinuousLinearMap.modulus_apply_eq_zero_iff`. Rename gate applies (`lake build Challenge` + name-drift + repo-wide grep). **Identity note:** another agent is also filing rows as `jon (namek)`; this window is the §5/§13 one and holds no `ClosedOperator` file. | 2026-07-27 | done — **shim deleted; `operatorAbs` is gone from every declaration name in the repository.** ~30 call sites repointed to `ContinuousLinearMap.modulus`, plus the three DKPS wrappers that had inherited the word (`modulus_sameApproximationSingularValues`, `modulus_mem_and_gauge_eq`, `paperNorm_modulus_eq`). **Two things the next adapter-retirement lane should expect, because neither was mechanical.** (1) The shim had accreted a declaration that was never in the canonical API — `operatorAbs_apply_eq_zero_iff`, declared *into the shim's namespace* from a paper-side module — so retiring an adapter means auditing what has been **added** to it, not only what it restates. It is now `ContinuousLinearMap.modulus_apply_eq_zero_iff` in the canonical module, generalized off endomorphisms and `@[simp]` like its `norm_modulus_apply` parent. (2) The shim had quietly **weakened** the canonical statements to whatever was convenient — `star`/`*` where the canonical API says `adjoint`/`∘L`. Those agree on an endomorphism algebra but **not up to `rw`**, so `norm_sinTwoAngleOperatorC` failed to typecheck until its two local facts were restated canonically. Gates: default 9274 (one fewer module) · `Challenge` 8894 · name-drift 0 · layers 0 · PR-1 consistency OK. Remaining §13 adapters, both unclaimed and both bigger: `CourantFischerCompat` (12 modules, four libraries, incl. a `Challenge` leaderboard) and `SymmetricOperatorIdealFamily.toRectangular` (68 consumers of the legacy structure). |
| jon (namek, §5/§13 window) | ForTauCeti/Analysis/InnerProductSpace/CourantFischerCompat.lean (**deletion**, in two stages); the `TauCeti.*` call sites of its 14 wrappers across DavisKahan, Acharyya2025 and Challenge/MathlibCandidate/CourantFischerWeyl/Leaderboard.lean; dev/tauceti-signature-polish-todo.md §13 `specSubspace` row + Appendix A; dev/tauceti/formathlib-to-fortauceti-migration.md CourantFischer name map. **Does NOT touch** `SymmetricOperatorIdealFamily.toRectangular` (the third adapter — separate lane), `ForTauCeti/Analysis/InnerProductSpace/CourantFischer.lean` itself (canonical, unchanged), any `ClosedOperator`/`LinearPMap` file (the concurrently-claimed U1 lane), or `comparator/*.json` | **§13 adapter retirement, second of three: delete the `CourantFischerCompat` shim.** Same deletion condition as `operatorAbs`, written in its own header and never executed. Bigger: 14 wrappers and roughly 150 call sites, and unlike `operatorAbs` it is **not** all mechanical — the shim splits cleanly in two and so does the lane. Stage 1 is the eight eigenvalue wrappers, which are pure renames over an existing canonical signature (one, `re_inner_map_self_eq_sum_of_eigenbasis`, has zero consumers and just goes). Two of them keep their *name* and change only their namespace (`exists_unit_vector_re_inner_le_eigenvalue`, `eigenvalues_eq_of_eigenbasis` are `LinearMap.IsSymmetric.*` canonically), so they must be repointed to dot notation rather than left bare, or they will resolve to nothing after the delete. Stage 2 is the `specSubspace` family, which is a **representation** change — predicate `p : Fin n → Prop` to set `{i | p i}` — so hypotheses change shape at each site and it cannot be sed'ed blind. Rename gate applies; the `Challenge` leaderboard reference makes `lake build Challenge` load-bearing here rather than routine. | 2026-07-27 | done — **shim deleted; §13 is down to ONE live adapter.** Stage 1 (eight eigenvalue wrappers) and stage 2 (the `specSubspace` family) both landed. **The thing to know before taking `toRectangular`:** one wrapper could not be deleted *at all*. `TauCeti.abs_eigenvalues_sub_le_opNorm` is pinned **as data** — `comparator/candidate-02-courant-fischer-weyl.json` names it and the immutable conformance statement declares it — so it moved into the canonical `CourantFischer.lean` keeping its off-convention name, with the reason on the declaration. Check `comparator/*.json` and `Challenge/**/Leaderboard.lean` *before* planning a deletion. `check_comparator_signatures.py --no-build comparator/candidate-02-courant-fischer-weyl.json` PASSes, which is the real proof the move was type-preserving; a green build alone would not have shown that. **Second thing:** the `specSubspace` half was a representation change (predicate → set), not a rename, so hypotheses change shape — `rw [orthogonal_spanIndices]` yields `sᶜ` and will not close a goal phrased with `(· ∉ s)`. **Third, a near-miss worth copying the mitigation from:** the repo-wide substitution also renamed a *paper-facing* `Acharyya2025.abs_eigenvalues_sub_le` that merely shared a name with a shim wrapper, and the self-reference surfaced as **"fail to show termination"**, not as an unknown identifier — diff the declaration headers (`git diff -U0 | grep '^+theorem'`) after any repo-wide substitution. Bonus: `DavisKahan/Specialized/Statistics.lean` came out shorter — `CorrespondingEigenblock` now selects with a `Finset`, which deleted a private helper that existed only to restate `spanIndices`' definition. Gates: default 9273 · `Challenge` 8893 · ForTauCeti 0 warnings · name-drift 0 · layers 0 · comparator pre-flight PASS. |
| jon (namek, §5/§13 window) | proof bodies and `simp`/`rw` argument lists only, across DavisKahan, Acharyya2024/2025, DkpsQuench2026, Helm2025, ForMathlib and ForTauCeti — **no statement, name, namespace or signature changes**, so no comparator entry or Leaderboard can be orphaned and no rename gate is needed. **Does NOT touch** `vendor/Spectra` (upstream snapshot, its ~40 warnings are not ours to fix), `Challenge/**/Conformance.lean` (immutable), or `SymmetricOperatorIdealFamily.toRectangular` (the last §13 adapter, still unclaimed) | **Clear the pinned-Mathlib deprecation warnings out of the paper libraries — 172 of them, 17 distinct renames.** These are rot, not style: a deprecated alias becomes an error on the next Mathlib bump, and §14's checklist has no room for a library that compiles only because upstream is still being polite. Census from a full build, largest first: `EuclideanSpace.single_apply` → `PiLp.single_apply` (61), `Matrix.toEuclideanLin_apply` → `Matrix.toLpLin_apply` (28), `ContinuousLinearMap.mul_apply` → `mul_apply_eq_comp` (22), `.add_apply` → `add_apply` (16), `.sub_apply` → `sub_apply` (13), `Submodule.orthogonalProjection` → `orthogonalProjectionOnto` (7 + 4 in two derived names), `.zero_apply` → `zero_apply` (7), `.one_apply` → `one_apply_eq_self` (3), plus singletons. **Known wrinkle to expect:** `ContinuousLinearMap.add_apply` and friends are *unprotected* aliases, so some of these warnings fire on a **bare** `add_apply` written inside a `namespace ContinuousLinearMap` block, where there is no qualified text to substitute and the fix is `_root_.add_apply`; those get found by rebuilding after the qualified pass, not by grep. Deliberately out of scope: `push_neg` → `push Not` (3) is a tactic-syntax migration, not a name, and the ~525 non-deprecation warnings (unused section variables, unused simp arguments) are a separate lane. | 2026-07-27 | done — **zero deprecation warnings remain in `DavisKahan`, `ForTauCeti`, `ForMathlib` and the four paper libraries** (was 172, across 32 files). `push_neg` → `push Not` was folded in after all; it is three sites and works verbatim. **The wrinkle predicted in the claim was real and cost a second pass:** ten of the 172 fired on a *bare* name reached by dot notation (`(polarRange T).orthogonalProjection`), where a qualified-name substitution has nothing to match. Those only surface by rebuilding after the qualified pass — budget two build cycles for a sweep like this, not one. Nothing but proof bodies and `simp`/`rw` argument lists changed, confirmed by diffing declaration headers (`git diff -U0 | grep '^+theorem'` empty), so no rename gate was needed. `vendor/Spectra` carries ~40 of the same warnings and was deliberately left alone — upstream snapshot. Gates: default 9273 green · name-drift 0 · layers 0 · PR-1 consistency OK. **Remaining warning census for whoever takes the next hygiene lane: 483 in our libraries**, none of them deprecations — roughly 180 unused section variables, 164 unused simp arguments, 38 unreferenced binders, 27 `simpa`→`simp`, 21 never-executed tactics, 20 `<;>`-where-`;`-suffices. All are §14 checklist items and all are per-site judgement rather than substitution. |
| jon (namek, §5/§13 window) | DavisKahan/OperatorIdeal/ApproximationNumbers/ScalarGeneric.lean (new accessors on `KyFanDominantIdealFamily`); the ~376 `N.toRectangularSymmetricIdealFamily.{Mem,gauge}` call sites across ~60 modules; dev/tauceti-signature-polish-todo.md §13 `RectangularSymmetricIdealFamily` row. **Does NOT touch** the `RectangularSymmetricIdealFamily` structure itself, the `toRectangular` adapter, `ForTauCeti/Analysis/OperatorIdeal/Family/**` (canonical, unchanged), or any statement's mathematical content | **§13 adapter retirement, third and last — STEP 1 of N: decouple the call sites from the legacy structure's name.** Sizing first, because the §13 row is wrong in a way that matters: it says "legacy record still has 68 consumers", but `RectangularSymmetricIdealFamily` is not a record behind an adapter — it is a **field of `KyFanDominantIdealFamily`/`UnitaryInvariantIdealFamily`**, the structure the whole ideal-valued sin-Θ development is parameterized over, and 376 of its 520 references are `N.toRectangularSymmetricIdealFamily.{Mem,gauge}`. It has exactly **three** origins (`RectangularSymmetricIdealFamily.operatorNorm`, the `KyFanDominantIdealFamily.operatorNorm` construction, and `SymmetricOperatorIdealFamily.toRectangular`), so the structure is not the problem — the 376 textual dependencies on its *name* are. The canonical gauge is also `ℝ≥0∞` where this one is `ℝ`, so a direct swap re-proves every downstream bound in a different ordered algebra (truncated subtraction, no `linarith`): that is a multi-session mathematical project, not a polish pass, and it should not be attempted as a flag day. **Step 1, which is what this row claims:** give `KyFanDominantIdealFamily` its own `Mem`/`gauge` accessors and repoint all 376 sites to `N.Mem` / `N.gauge`. Pure legibility at the call sites — and it collapses the later representation swap from a 60-module edit into a one-file edit plus proof repair. No statement changes meaning. | 2026-07-27 | done — **376 call sites now go through `KyFanDominantIdealFamily.{Mem,gauge}`**; the legacy structure's name appears in no proof body. Remaining coupling is **73 production type-position uses across 30 modules** (plus 73 more in parked Experimental/Scratch), which is the real size of what is left. **Gotcha, recorded on the accessors:** they are `abbrev`, so `exact` sees through them but **`rw` does not** — it keys on the head symbol, and `KyFanDominantIdealFamily.gauge N A` vs `RectangularSymmetricIdealFamily.gauge N.toRectangularSymmetricIdealFamily A` differ there. Five proofs in `SinTheta/{Unbounded/AllGap,Unbounded/LegacyGap,Real/Unbounded,Real/Generalized}` needed one `simp only [KyFanDominantIdealFamily.gauge]` before a rewrite whose supporting block lemma is stated over the underlying ideal. Inlining the `let M := N.toRectangularSymmetricIdealFamily` aliases did **not** fix it and was a wasted pass — the accessor has to be unfolded, not the alias. Default build 9273 green; 37 files; no statement changed meaning. **Step 2, NOT started and deliberately unclaimed:** swap the representation inside `KyFanDominantIdealFamily` to `TauCeti.SymmetricOperatorIdealFamily`, keeping the `ℝ`-valued accessors as `(N.gauge A).toReal` / `N.gauge A ≠ ∞`. That is now a one-file change plus proof repair rather than a sixty-module flag day — but it is still where the mathematics is: the canonical gauge is `ℝ≥0∞`, so anything that reaches *past* the accessors into `ℝ` arithmetic (truncated subtraction, `linarith`, `gap * gauge ≤ gauge`) needs rework. Budget it as a session, not an hour. |

## Cross-lane findings (post here to save another agent a re-derivation)

- **The RankFactorization challenge statement is not Mathlib-clean, and both halves cannot
  be right** (jon/namek, 2026-07-28). Building `ForMathlib` under Mathlib's own linter set
  flags `[DecidableEq n]` on `exists_eq_mul_rank`, `exists_eq_mul_of_rank_le` and
  `rank_le_iff_exists_eq_mul`: it is in the type, never used, and Mathlib's advice is to drop
  it and call `classical` in the proof. But the same three signatures are pinned as **data** —
  `Challenge/MathlibPending/RankFactorization/Conformance.lean` restates them with the
  identical `variable … [DecidableEq n]` line, and `Leaderboard.lean` names one in
  `#print axioms`. Dropping the instance desynchronises them; keeping it means a Mathlib PR
  of this result would be asked to drop it anyway. Someone who owns the challenge lane has to
  decide, and the Mathlib-facing answer is the one without `[DecidableEq n]`. Until then
  `lean_lib ForMathlib` cannot carry `warningAsError`; the reasoning is recorded in the
  module and in `lakefile.toml`.

- **`ForMathlib` had the same two defects as `ForTauCeti`, and Mathlib's bar is higher**
  (jon/namek, 2026-07-28). 6 of its 12 modules carried no copyright or licence header, all 12
  lacked a `## Provenance` section, and the `lean_lib` set no `leanOptions` — so its warning
  count was measured against this repository's default rather than Mathlib's. Census under
  Mathlib's set: **22**, against 0 under ours; now **3** (the pinned ones above). One thing
  learned that also applies to `ForTauCeti`: Mathlib's header linter requires the copyright
  block to be **exactly** the four canonical lines, so the "Staged for Mathlib / Formalized
  by" prose these files carried has to live in the module docstring, not the header. It is
  now a `## Staging note` section. `ForTauCeti` has the same shape but does not see it,
  because Tau Ceti sets `style.header = false`.

- **~~Why the Spectra polar consumers cannot be repointed yet~~ — DONE, they are repointed**
  (jon/namek, 2026-07-28). The route recorded below worked exactly as written: prove
  `spectraPolarIsometry T = T.polarPartial` from the ForTauCeti uniqueness theorem — an
  equation between plain `H →L[ℂ] H`, so the dependent-type transport never arises — and
  let everything else follow by rewriting. `DavisKahan/Geometry/Polar/PolarIsometryFinal.lean`
  is now 332 lines → 167, Spectra-free, and **out of the vendored namespace**;
  `Section3Nonacute.lean` and `PolarIntertwining.lean` follow it. Rule-7 burn-down list:
  **12 → 10**. Two gotchas for the next such swap: (1) replacing a `def` by an `abbrev`
  changes what `rw [thatName]` unfolds to, so proofs that unfolded Spectra's `polarRange`
  to reach `topologicalClosure` need the *next* definition named too; (2) a proof that
  unfolds the bridge alias before rewriting (`rw [spectraPolarIsometry, …]`) defeats the
  bridge lemma — rewrite *with* the alias, don't unfold it.

- **(historical) Why the Spectra polar consumers could not be repointed** (jon/namek, 2026-07-28). With `ForTauCeti/Analysis/InnerProductSpace/PolarPartialIsometry.lean`
  now covering every declaration the consumers use, I rewrote
  `DavisKahan/Geometry/Polar/PolarIsometryFinal.lean` over it — 332 lines to 135,
  Spectra-free, in the Davis--Kahan namespace instead of Spectra's — and it **built clean**.
  The swap still had to be reverted, for one reason worth knowing before anyone retries it.

  `DavisKahan/Geometry/Polar/PolarIntertwining.lean` consumes the polar isometry through
  the *Interop bridge* (`spectraPolarIsometry` in
  `DavisKahan/Interop/Spectra/OperatorAbsoluteValue.lean`, a thin alias for Spectra's), which
  is architecturally correct and is **not** on the rule-7 burn-down list. It needs
  `adjoint (spectraPolarIsometry T) = spectraPolarIsometry T⋆`, and the only proof of that
  lived in the file I rewrote. So repointing `PolarIsometryFinal` orphans it.

  Bridging is harder than it looks. `Spectra.QuantumMechanics.Channels.absOp T = CFC.abs T =
  CFC.sqrt (star T * T)` and our `T.modulus = CFC.sqrt (T.adjoint ∘L T)` are the same
  operator and the equality should be short. But `polarRange T` and `T.polarInitial` are then
  *propositionally equal submodules of different syntactic types*, so
  `Spectra.polarPartial T : polarRange T →L H` does not transport to
  `T.polarPartialAux : T.polarInitial →L H` without dependent-type surgery.

  **The clean route avoids the transport entirely.** Both `polarIsometry T` and
  `T.polarPartial` are plain `H →L[ℂ] H`, so prove `spectraPolarIsometry T = T.polarPartial`
  directly from `ContinuousLinearMap.eq_polarPartial_of_comp_modulus` (the uniqueness theorem
  added today): Spectra's own `polar_decomposition` gives `U |T| = T` once `absOp = modulus`,
  and vanishing off the initial space is Spectra's projection identity. One equation between
  non-dependent types, and then every ForTauCeti polar result transfers to the bridge by
  rewriting. Budget an hour, not a day — but do that lemma *first*, not the file rewrite.

- **A production file declares its contents into the *vendored* library's namespace**
  (jon/namek, 2026-07-28). `DavisKahan/Geometry/Polar/PolarIsometryFinal.lean` opens
  `namespace Spectra` / `namespace QuantumMechanics` / `namespace Channels` and defines eight
  declarations there — `polarFinalRange`, `polarPartial_mem_finalRange`,
  `denseRange_polarPartial_final`, `polarPartial_final_surjective`, `polarPartialFinalEquiv`,
  `polarIsometry_comp_adjoint_self`, `adjoint_polarIsometry`, and an instance. To a reader
  they are indistinguishable from Spectra's own API, and `vendor/Spectra` is an upstream
  snapshot we do not own. This is the same failure mode as the `operatorAbs` shim that had
  accreted `operatorAbs_apply_eq_zero_iff` from a paper-side module, and it is worth a grep
  gate: nothing outside `vendor/` should declare into `namespace Spectra`.

- **Spectra's polar decomposition and the new `ForTauCeti` one are the *same construction*,
  arrived at independently** (jon/namek, 2026-07-28). I built
  `ForTauCeti/Analysis/InnerProductSpace/PolarPartialIsometry.lean` from the `TODO` in
  `PolarIsometry.lean` plus Mathlib's `LinearMap.extendOfNorm`, and only afterwards read
  `vendor/Spectra/Spectra/QuantumMechanics/Channels/PolarDecomp.lean`. The definitions line
  up one for one: `polarRange` = `polarInitial`, `absOpCorestrict` = `modulusCorestrict`,
  `polarPartial` = `polarPartialAux`, `polarIsometry` = `polarPartial`. Two differences
  matter. Ours is **rectangular** — `E →L[ℂ] F` with different source and target, built on
  our own `modulus`, where Spectra's is square-only and built on `CFC.abs`. And ours proves
  the range identification (`range W` closed, `= closure (range M)`) which Spectra's file
  leaves to the DavisKahan file above. So the replacement is a generalisation, not a
  transcription.

- **Two hard blockers stand between `ForTauCeti` and Tau Ceti acceptance, and neither is
  mathematical** (jon/namek, 2026-07-28). Found by reading `external/TauCeti/lakefile.toml`
  rather than by guessing at what upstream wants.

  1. **`ForTauCeti/Analysis/InnerProductSpace/RectangularUnitarilyInvariantNorm.lean` is 2131
     lines.** Tau Ceti's `lean_lib` sets `weak.linter.style.longFile = 1500` as a *hard*
     ceiling enforced in-build, and its PR workflow holds a **newly added** file to 1000.
     That module is the single file over either limit — every other one of the 67 is under
     1000. It cannot be accepted as-is and has to be split. This is invisible from inside
     this repository, because our own `lean_lib ForTauCeti` sets no `longFile` option at all.

  2. **Our `ForTauCeti` builds under weaker rules than upstream will apply.** Tau Ceti's
     library options are
     `warningAsError = true, weak.linter.mathlibStandardSet = true, weak.linter.style.longFile = 1500`.
     Our `lakefile.toml` gives `ForTauCeti` *no* `leanOptions`. So "`lake build ForTauCeti`
     is warning-free", which is true and which several lanes have reported, is measured
     against a strictly weaker linter set than the one that will gate the PR. Until the
     staging library is built with `mathlibStandardSet` on, the real upstream warning count
     is unknown. Suggested order: turn on the linter set *without* `warningAsError`, fix the
     census, then turn on `warningAsError` so it cannot regress.

  Good news from the same audit: the export machinery itself is sound. `export_for_tauceti.py
  --write` followed by `--check` round-trips **all 67 modules, MATCH, `export: OK`**, and none
  of the 67 target paths collides with an existing upstream file.

- **RESOLVED 2026-07-28 (U1 release): both red-`main` findings below are now stale.**
  `lake build DavisKahan.All` is green at **9176 jobs** on the current tree —
  `Restriction.lean`, `RestrictionExtras.lean`, `Riccati.UnboundedReduction`,
  `PairwiseHomogeneousUniqueness` and `ShiftedInverse` all elaborate. The two findings are
  kept below as the historical record of what the `LinearPMap` migration broke and why, not
  as open work.

- **UPDATE 2026-07-28 (later): `main` is now red on more than four, and one is a *syntax* error.**
  After merging `78f2557`, `DavisKahan/SpectralTheory/ReducingSubspace/Restriction.lean` fails with ~20 errors
  starting at `:88` (`Fields missing: carrier, add_mem', zero_mem', smul_mem'`) and including
  `:89:2: unexpected token '_'; expected command` — a parse error, so everything after it in the file is
  unelaborated and the downstream cascade (`RestrictionExtras`, `Riccati.UnboundedReduction`) follows from it.
  U1 lane files; untouched by me. Whoever holds U1 should look at `Restriction.lean:88-89` first, since the
  parse error is very likely the single root cause of the whole cascade.

- **`main` is currently red on four targets, all inside the U1 lane, and none of them are
  the ideal-family swap** (jon/namek, 2026-07-28). A full `lake build` of `origin/main` at
  `ea9c924`, taken *before* any of my edits, already failed on
  `DavisKahan.Sylvester.PairwiseHomogeneousUniqueness` and
  `DavisKahan.Sylvester.ShiftedInverse`; after merging `78f2557` the set grew to include
  `DavisKahan.SpectralTheory.ReducingSubspace.RestrictionExtras` and
  `DavisKahan.Riccati.UnboundedReduction`. The `RestrictionExtras` failure is a
  representation mismatch left by the `LinearPMap` migration and reads as
  `A.domain` vs `A.toLinearPMap.domain` at
  `DavisKahan/SpectralTheory/ReducingSubspace/RestrictionExtras.lean:36`. These are
  `jon (toothbrush)`'s claimed files, so I have not touched them — flagging so the failures
  are not mistaken for fallout from the ideal-family work, and so nobody else re-derives
  them. The one module the swap *did* break,
  `DavisKahan/Sylvester/Unbounded/OrderedCutoff.lean`, was repaired without editing it: the
  fix belonged in the simp lemma's statement, not at the call site (see the next finding).

- **A `@[simp]` lemma about an accessor must be stated in the form its call sites have
  already been unfolded into** (jon/namek, 2026-07-28). `KyFanDominantIdealFamily.{Mem,gauge}`
  are `abbrev`s over `.toRectangularSymmetricIdealFamily.{Mem,gauge}`, and downstream proofs
  reach block lemmas by first running `simp only [KyFanDominantIdealFamily.gauge]`. So when
  the storage swap made it natural to restate `kyFan_gauge` over the accessor, `simp` stopped
  matching at every one of those sites — the goal there has the *unfolded* head symbol. The
  lemmas are back in field form with the reason written above them. Generalization worth
  keeping: when a structure exposes both a raw projection and a reducible accessor, the simp
  set has to be stated in whichever form survives the project's own unfolding convention, and
  a green build of the defining file proves nothing about that.

- **Step 2 of the `toRectangular` retirement is gated on one missing object, and it is not
  in DavisKahan** — *superseded 2026-07-28: the object was built (`kyFanSymmetricIdealFamily`)
  and the swap has landed. Kept for the sizing argument, which was accurate.*
  (jon/namek, 2026-07-27). With step 1 landed, no production module outside
  `DavisKahan/OperatorIdeal/ApproximationNumbers/ScalarGeneric.lean` reaches into
  `toRectangularSymmetricIdealFamily` at all — verified by grep, the count is zero. So the
  representation swap really is a one-file change now. What blocks it: that file builds
  **two** `KyFanDominantIdealFamily` instances, `operatorNorm` and `kyFan`, and only the
  first has a canonical counterpart. `ForTauCeti/Analysis/OperatorIdeal/Family/` provides
  `TauCeti.operatorNormFamily` and **no Ky Fan family**. So before the swap someone has to
  build a `SymmetricOperatorIdealFamily` for `kyFanApproximationGauge`, which means proving
  its four laws in `ℝ≥0∞` where the existing proofs are in `ℝ` — subadditivity, homogeneity,
  the two-sided ideal bound and completeness, each needing `ENNReal.ofReal` bridging. That
  is new mathematics in ForTauCeti, not a migration, and it is the honest first task of any
  step-2 session. The 73 remaining production type-position uses of
  `RectangularSymmetricIdealFamily` are a separate and *legitimate* generality choice — the
  `SinTheta/**` block lemmas do not need Ky Fan dominance — so they are re-parameterization,
  not deletions, and they come after the swap, not before.

- **Two Experimental `sorry` leaves are already theorems in ForTauCeti, modulo scalars**
  (jon/namek, 2026-07-27, found while retiring the `operatorAbs` shim).
  `DavisKahan/Experimental/InfiniteDimensional/SinTheta/General.lean:550` defines
  `operatorAbsoluteValue (T : E →L[𝕜] E) : E →L[𝕜] E := sorry` — an *undefined* parallel
  modulus — and `Experimental/InfiniteDimensional/DirectRotation.lean` carries two "leaf
  obligations" about it that are proved outright in
  `ForTauCeti/Analysis/InnerProductSpace/OperatorModulus.lean`:

  | Experimental leaf (`sorry`) | ForTauCeti theorem |
  | --- | --- |
  | `operatorAbsoluteValue_sq` (`\|S\| ∘L \|S\| = star S ∘L S`) | `modulus_mul_self_eq_star_mul_self` |
  | `operatorAbsoluteValue_isSelfAdjoint` | `modulus_isSelfAdjoint` |

  A third, `SymmetricNormIdeal.operatorAbsoluteValue_mem_and_gauge_eq`, has the same shape as
  the proved `modulus_mem_and_gauge_eq` in
  `DavisKahan/OperatorIdeal/ApproximationNumbers/OperatorModulus.lean`.
  **The blocker is scalars, and it is real:** the Experimental development is stated at
  general `[RCLike 𝕜]` while `ContinuousLinearMap.modulus` is `ℂ`-only, because Mathlib
  registers the continuous functional calculus on Hilbert-space operators only over `ℂ`
  (said explicitly in that module's docstring). So this is "specialize the Experimental
  chain to `ℂ`, or route through complexification transfer", not a substitution — but it is
  three `sorry`s that do **not** need new mathematics. Left alone because Experimental is
  parked.

- **`DavisKahan/Experimental/MathAhead/Section4/InfiniteProposition41.lean` has not
  compiled since the `ℝ` codomain decision, and nothing noticed** (jon/namek, 2026-07-27,
  for whoever unparks Experimental). It still wraps approximation-number inequalities in
  `NNReal.coe_le_coe` at seven sites and calls `approximationNumber_le_nnnorm`, a name
  deleted when the codomain went real. `lake build DavisKahan.Experimental.MathAhead.Section4.InfiniteProposition41`
  fails at line 698. **Not fallout from the §5.1 renames** — I touched exactly one line in
  that file (a rank-lemma call site) and the failures are all in the `ℝ≥0` bridge. Left
  alone because Experimental is parked; recorded so the next person there does not
  re-diagnose it as fresh damage. The repair is mechanical: drop the `NNReal.coe_*`
  wrappers and repoint `approximationNumber_le_nnnorm` → `approximationNumber_le_norm`.

- **The `unusedSimpArgs` linter does not cover `simpa only [...]` argument lists**
  (jon/namek, 2026-07-27). `ApproximationNumber/Adjoint.lean` carried a dead `coe_nnnorm`
  inside a `simpa only`, left over from the `ℝ≥0` era, through a build that the
  warning-zero hygiene lane had certified at zero warnings. Removing it changes nothing.
  So "zero linter warnings" does **not** imply "no dead simp arguments"; a `grep -rn
  "simpa only \[" ForTauCeti/` sweep is the only way to find the rest.

- **`DavisKahan.approximationSingularValue` is now a pure alias** (jon/namek, 2026-07-27,
  for the §13 adapter-retirement lane). Since the codomain decision it reads
  `approximationSingularValue (n : ℕ) (K : E →L[𝕜] F) : ℝ := K.approximationNumber n` —
  same value, arguments flipped, no dot notation. That is the fourth live alias, and it is
  not in §13's table; add it there when that lane is claimed.

- **`ForTauCetiRoadmap/` is not a `lean_lib`, so `Suggested.lean` is never compiled**
  (jon/namek, 2026-07-27). Same rot class as the missing `Challenge.lean` root aggregate:
  it had drifted to `ℝ≥0` signatures its own README decision 2 overturned, plus an
  `open scoped NNReal` that does not supply the `ℝ≥0∞` notation it uses. I synchronized it
  by hand and said so in its header, but the structural fix — a non-default `lean_lib` so
  `lake build ForTauCetiRoadmap` is a runnable gate — is unclaimed.

- **Two unused simp arguments in `ApproximationNumber/MinMax.lean:165`** (jon,
  2026-07-27, for edward's open §5.4 lane). `lake build ForTauCeti` was at zero warnings
  after my hygiene pass; merging §5.4 reintroduced exactly two, both on the same line:

  ```
  simp [hy, Submodule.coe_smul, norm_smul, abs_inv]
        --   ^^^^^^^^^^^^^^^^^^              ^^^^^^^  both unused
  ```

  i.e. `simp [hy, norm_smul]` suffices. Left untouched because `MinMax.lean` is your open
  claim. Flagging because unused simp arguments are a §14 checklist item and the file is
  otherwise ready — and because it is worth knowing that the library is currently at
  warning-zero *except* for this one line, so it is cheap to keep it there.

- **The comparator gate is now actually runnable, and it gives precise diagnoses**
  (jon, 2026-07-27). `lake build Challenge` succeeds for the first time (8891 jobs) after
  the `globs` repair, so `scripts/check_comparator_signatures.py` can reach both sides of
  every config. Two practical notes for the comparator lane: pass `--no-build` and a single
  config path, otherwise it re-runs a full build per invocation and takes many minutes; and
  for `pending-davis-kahan-sylvester-pi-over-two.json` the gate now reports exactly

  ```
  ERROR ForMathlib.DavisKahanTheory.uiNorm_sylvester_le_of_spectralDistance
  ERROR ForMathlib.DavisKahanTheory.sylvester_hasFiniteUnitaryOrbitCertificate_of_spectralDistance
  ```

  i.e. both are pure `ForMathlib.` → `TauCeti.` namespace drift, matching the ~24 edward
  triaged. Both modules on both sides of that config now compile, so the repair is
  verifiable end to end rather than erroring out before comparison. Before the `globs` fix
  this config could not be diagnosed at all — its `Conformance` module had never compiled.

- **`main` currently builds RED on the `Challenge` library, and it is my fault**
  (jon, 2026-07-27). My §9.2 rename in `33000c0`
  (`Matrix.PosSemidef.eigenvalues₀_eq_zero_of_le` → `…_eq_zero_of_rank_le`, `i` made
  implicit) breaks `Challenge/MathlibPending/RankPsdRealization/Leaderboard.lean:7` with
  `Unknown constant`, which is why `lake build Challenge` fails with the unhelpful
  "some modules have bad imports". `Challenge` is **not** in `defaultTargets`, so the
  default build stays green and hides this. The repair is inside edward's committed
  comparator conformance-gate claim, so per the earlier-committed-row rule I have NOT
  touched it — but it is a one-line repoint, and the new signature is:

  ```lean
  theorem eigenvalues₀_eq_zero_of_rank_le (hA : A.PosSemidef) {d : ℕ}
      (hrank : A.rank ≤ d) {i : Fin (Fintype.card n)} (hi : d ≤ (i : ℕ)) :
      hA.isHermitian.eigenvalues₀ i = 0
  ```

  i.e. `#print axioms TauCeti.Matrix.PosSemidef.eigenvalues₀_eq_zero_of_rank_le` in the
  Leaderboard, and in the Conformance restatement `(i : Fin _)` becomes implicit. Every
  *other* `Challenge.MathlibPending.*` module I built individually is green, so this is the
  only live breakage.

  **UPDATE (jon, same day): I have now landed the repair myself.** Rationale, since this
  sits inside edward's committed claim: it is my regression, `main` was red, I offered twice
  and waited, and the fix turned out to have **zero design content** — contrary to what the
  comparator claim anticipated, `Challenge/MathlibPending/RankPsdRealization/Conformance.lean`
  needs **no** restatement. It declares its own challenge statement of
  `PosSemidef.eigenvalues₀_eq_zero_of_le` and builds green untouched; only the Leaderboard's
  `#print axioms` line referenced our renamed declaration. So the repair was a single
  identifier on `Leaderboard.lean:7`, and edward's lane keeps the whole comparator name-list
  job (the other ~30 stale names) untouched.

  **SEPARATE PRE-EXISTING BREAKAGE FOUND while verifying that fix, and it is not mine:**
  `lake build Challenge` *still* fails with "some modules have bad imports" — because there
  is **no `Challenge.lean` root aggregate at all**. Every other library in the repo has one
  (`ForTauCeti.lean`, `DavisKahan.lean`, `ForMathlib.lean`, the four paper libraries); the
  `[[lean_lib]] name = "Challenge"` block has no root module and no `globs`, so the library
  target can never build and only individual modules ever compile. This is the structural
  reason the conformance gate has been silently vacuous, independent of the stale names
  edward found: nothing ever builds `Challenge` as a whole.
- **Renaming lanes must build `Challenge` explicitly** (jon, 2026-07-27). `defaultTargets`
  is `ForMathlib, ForTauCeti, DavisKahan.All, Acharyya2024, Acharyya2025, DkpsQuench2026,
  Helm2025` — no `Challenge`. A repo-wide grep must also cover `Challenge/` and
  `comparator/*.json`, which restate staged declarations by name. This is exactly how the
  §9.2 regression above got past a green 9266-job build.

- **`Finset.expect` is NOT usable as the canonical mean in our scalar setting**
  (edward, 2026-07-27, probed in Lean). It needs `[Module ℚ≥0 M]`, and for
  `[RCLike 𝕜] [InnerProductSpace 𝕜 E]` that instance does not resolve —
  `NormedSpace ℝ E` is available only through the *def* `InnerProductSpace.rclikeToReal`
  / `NormedSpace.restrictScalars`, deliberately not instances. Relevant to the
  §8.1 `finiteMean` reuse audit.
- **`Finset.centroid 𝕜 s z : E` does typecheck** in that setting (only the
  `noncomputable` complaint), so it *is* a live reuse candidate — but note its
  empty-set behaviour: `Finset.affineCombination` is defined against
  `Classical.arbitrary P`, so `centroid` on `∅` is nonconstructive junk, whereas
  the current `finiteMean` returns `0` there (Mathlib's total-inverse
  convention) and `finiteMean_append` is stated to hold *at* `n = 0`. Any
  swap to `centroid` must re-examine that boundary case.
- **`appendFin z y` is `Fin.snoc z y`** — same `Fin.lastCases` construction, and
  `Fin.snoc_castSucc` / `Fin.snoc_last` are the upstream simp lemmas matching
  `appendFin_castSucc` / `appendFin_last`. The two are *not* `rfl`-equal
  (`snoc` inserts a cast) but are equal by `funext` + `Fin.lastCases` + `simp`.

## Spectral lane — COMPLETE through the Continuation chain (jon, 2026-07-23)

`SinTheta/General.lean`, `DoubleAngle.lean`, and `DirectRotation.lean` all
elaborate; the full chain `General → DoubleAngle → ContinuationCore →
ContinuationTransport → ContinuationAssembly →
ContinuationSpectralIdentification` builds green (8893 jobs), and the default
build stays green (9097 jobs). The remaining mathematical content is isolated
as documented **leaf obligations** inside those three files: the two RCLike
Sylvester engine transports, the measurable `spectralSubspace` triple,
`operatorAbsoluteValue` + ideal-gauge identity + ideal projector-difference
estimate (General); the reflected-subspace transports and double-angle
identities (DoubleAngle); the polar units/commutation and the two
Halmos-decomposition extremal theorems (DirectRotation). Signature note:
`sinTheta_perturbation`/`sinTheta_symmetric`/`spectralProjection_sinTheta`/
`ideal_sinTheta`/`sinTwoTheta_residual` gained necessary `left ≤ right` /
instance hypotheses.

## Parked (claim explicitly before starting)

- `Ideals/Rectangular.lean` (4): Schatten / Hilbert–Schmidt / trace-class rectangular
  families over RCLike — multi-session analytic campaign; Schauder absent from pinned Mathlib.
- Frontier Contour-blocked sections (Section3/8/9): **UNBLOCKED 2026-07-23** —
  the circle contour surface is complete (RieszCircle.lean sorry-free; the
  projection identity is proved through SinTheta/CayleySelectorBridge.lean).
  Consumers should target the RieszCircle signatures; note theorems 4/5 now
  take `0 ≤ radius` (supplied by `CircleSeparatesRealSpectrum.radius_pos`).
- `MathAhead/.../KyFanBochner.lean`: broken on `main` since de30805 (11 errors, pre-existing).
- Polar-decomposition campaign (5 leaves): `adjoint_polarIsometry` needs the abs-adjoint
  closure relation `closure_range_abs_adjoint_eq_closure_range` + partial-adjoint formula;
  Section3Nonacute needs `positiveSupportInverse` + `source_compression_polar_formula` +
  `canonicalPolarFactor_reflection_relation` + the crossed-block structure of
  `IsPaperDirectRotation`. None exist in Mathlib/Spectra/repo yet — a real multi-session
  campaign. `polarIsometry_comp_adjoint_self` (final-projection identity) was the one leaf
  whose supporting infra was already present; now proved (5c7b13c).

## Scratch overlay (external GPT-5.6 drop) — repaired by jon (2026-07-23)

The never-compiled `DavisKahan/Experimental/Scratch/` drop (ideal Banach
spaces, Hilbert–Schmidt Bochner layer incl. the `HilbertSchmidtComplexFamily`
dependency, free-beam Green identities and smooth kernel) now builds:
`lake build DavisKahan.Experimental.Scratch.All` — zero errors, zero sorries.
Key API decision: `IdealOperator.ofMem` must be used instead of anonymous
constructors in statements (the type-synonym unfolding otherwise leaks the
subtype topology into `Integrable` hypotheses), and the generic Bochner
theorems take `[NormedSpace ℝ (E →L[𝕜] F)]` as an instance argument.
| jon (window A) | Frontier/Section3.lean (OneSpace: proposition3_4_square_is_reflected_directRotation, fixedCosineSubspace, proposition3_5_fixedAngle_maximal + supporting lemmas) | Props 3.4/3.5 acute cluster closeout | 2026-07-24 | done — BOTH Props 3.4 and 3.5 proved axiom-clean (propext/Classical.choice/Quot.sound only). **Prop 3.4**: reflected pair Uref=U, Vref=reflectedSubspace V U. New lemma `reflectionOperator_reflectedSubspace` (R_{Vref}=R_V R_U R_V via starProjection_reflectedSubspace) ⇒ spectraReflectionProduct U Vref = (R_V R_U)², so G=R_V R_U is its accretive unitary square root = spectraDirectRotation U Vref via `spectraDirectRotation_unique_of_sq`. Hypothesis corrected per Finding #2: half-angle bound on the cosine SQUARE `re⟪x,halmosCosineSq x⟫ ≥ ‖x‖²/2` (derives hGre via 2S=1+G and Re S=\|S\| done — ²=halmosCosineSq), PLUS `IsAcute U (reflectedSubspace V U)` as an INDEPENDENT hypothesis. FINDING: acuteness of the reflected pair is NEITHER implied by nor implies the cosine-square bound (boundary cos²=1/2 → double angle π/2 → gap 1, not acute; a single principal angle in (π/4,π/2) → acute reflected pair yet reflection product with negative numerical real part). Both conditions genuinely needed; carried explicitly. Imports DoubleAngle (acyclic; first Frontier→InfiniteDimensional edge for reflectedSubspace). **Prop 3.5**: `fixedCosineSubspace` DEFINED as the c²-EIGENSPACE `ker(halmosCosineSq − c²)` (= {c²}-spectral subspace for a singleton; eigenspace form makes the eigenvalue equation definitional, sidestepping PVM eigenvalue-extraction). Forward + maximality both proved. FINDING: `IsFixedCosineReducingSubspace` was mathematically WRONG — printed predicate constrained only M∩U and M∩V, so a nonzero exterior vector (Uᗮ∩Vᗮ, compatible with acuteness) spans an M meeting it vacuously yet with cos²=1≠c² for c<1; MAXIMALITY FALSE as stated. CORRECTED by adding the two complement conditions (M∩Uᗮ→‖Pᗮ_V·‖=c‖·‖, M∩Vᗮ→‖Pᗮ_U·‖=c‖·‖), matching the source's "all nonzero vectors make the fixed angle" clause. Key machinery: polarization `eigen_of_reducing_quadratic` (T preserves W + ⟪Tw,w⟫=0 on W ⇒ T=0 on W), `halmosCosineSq_symm` (=1−(P_U−P_V)²), reduces-via-commute, source/complement quadratic-form value lemmas. hacute/c≤1 retained (source-faithful); proof needs only 0<c. Did NOT touch Classification section (window B). |
| jon (window A) | MathAhead/HiddenFoundations/FreeBeam/** (20 files, moved from Scratch/FreeBeam/**), MathAhead/Section9All.lean, Scratch/All.lean | Section 9 free-beam PROMOTE-ONLY: relocate the compiled FreeBeam scratch drop into the maintained MathAhead tree | 2026-07-24 | done — the entire FreeBeam campaign (3 smooth-core Green/kernel files + 9 Abstract operator-theory bridges + 4 Classical ODE-mode files + 3 Analytic assembly files + 2 aggregates) `git mv`'d Scratch→MathAhead, namespace `…Experimental.Scratch.FreeBeam` → `…Experimental.MathAhead.HiddenFoundations.FreeBeam`, imports repointed, wired through `FreeBeam.All`→`MathAhead.Section9All`→`MathAhead.All`/`HiddenFoundations.All`; `Scratch/All.lean` FreeBeam imports dropped. Builds green (`MathAhead.Section9All` 8799 jobs, `Scratch.All` 9003 jobs), 0 sorry in the promoted tree. FINDING (scope): this is reduction machinery only — it derives symmetry/self-adjointness/compact-resolvent/`>500` from a concrete interval Sobolev space + coercive form + numerical root-localization (`4.73 < firstPositiveRoot`), all still packaged as unfilled `structure` fields. So `Frontier/Section9Analytic.lean`'s 20 sorries are NOT discharged (per user: promote-only); grounding them needs those 3 concrete inputs (interval H²/H⁴ Sobolev + Rellich likely absent from pinned Mathlib) — a separate campaign. Did NOT touch Frontier/Section9Analytic.lean. |
