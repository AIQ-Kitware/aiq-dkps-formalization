\input{preamble}
\title{Source-order modernization of Yu--Wang--Samworth: a population-gap Davis--Kahan theorem for statistics}
\author{}
\date{2015 journal paper; reconstructed from the supplied 2014 preprint transcription}
\begin{document}
\maketitle

\section*{Source map and scope}
\begin{sourceguide}
\item[Primary source] Yi Yu, Tengyao Wang, and Richard J. Samworth, \emph{A Useful Variant of the Davis--Kahan Theorem for Statisticians}, Biometrika 102 (2015), 315--323; preprint arXiv:1405.0680.
\item[Local evidence] Supplied transcription {\small\code{Yu-Wang-Samworth-2014-Useful-DavisKahanVariant(1).tex}}.\\ SHA--256 \texttt{\seqsplit{ed45f19eb1a619a182a737aa971b2120727d2bebb86760852f860a9fbe54ecfe}}.
\item[Redistribution] The transcription is not copied into the repository. This note is a new, source-order mathematical reconstruction.
\item[Coverage] Principal-angle definitions; the statistical Davis--Kahan baseline (Theorem 1); Theorem 2; the sharpness examples; Corollary 1; the application audit; Theorem 3 for singular subspaces; Lemma A1; all displayed proof identities and constants. Numbering is the published Biometrika numbering, checked against the article on 2026-08-13.
\item[Formalization target] \code{ForTauCeti/Analysis/InnerProductSpace/Spectral/EigenFrame.lean}, \code{.../Spectral/ResidualGap.lean}, \code{.../YuWangSamworth/Residual.lean}, \code{.../YuWangSamworth/Statistics.lean}, \code{.../YuWangSamworth/SingularSubspace.lean}, and the paper-facing package \code{YuWangSamworth2015}.
\end{sourceguide}

The purpose is not to repeat the paper's prose. The reconstruction isolates every mathematical assertion, normalizes notation, expands all hidden matrix identities, and records exactly how the paper's finite-dimensional proof maps to the intrinsic Lean development.

\section{Principal angles and distances between subspaces}

Let $V,\widehat V\in\R^{p\times d}$ have orthonormal columns:
\[
  V^TV=I_d,
  \qquad
  \widehat V^T\widehat V=I_d.
\]
Let
\[
  \sigma_1\ge\cdots\ge\sigma_d\ge0
\]
be the singular values of $\widehat V^TV$. The principal angles are
\[
  \theta_j:=\arccos\sigma_j\in[0,\pi/2].
\]
Write
\[
  \Theta(\widehat V,V):=\diag(\theta_1,\dots,\theta_d),
  \qquad
  \sin\Theta(\widehat V,V):=\diag(\sin\theta_1,\dots,\sin\theta_d).
\]
The Frobenius sine distance is
\[
  \norm{\sin\Theta(\widehat V,V)}_{\F}^2
  =\sum_{j=1}^d\sin^2\theta_j
  =d-\norm{\widehat V^TV}_{\F}^2.
\]
If $V_1$ is an orthonormal complement to $V$, then
\[
  \norm{V_1^T\widehat V}_{\F}^2
  =\norm{\sin\Theta(\widehat V,V)}_{\F}^2.
\]

The basis-sensitive distance is minimized over orthogonal alignments:
\[
  \min_{O\in O(d)}\norm{\widehat VO-V}_{\F}.
\]
If $O$ is the Procrustes alignment obtained from an SVD of $\widehat V^TV$, then
\[
  \norm{\widehat VO-V}_{\F}^2
  =2\sum_{j=1}^d(1-\cos\theta_j)
  \le2\sum_{j=1}^d\sin^2\theta_j.
\]
Therefore
\[
  \norm{\widehat VO-V}_{\F}
  \le\sqrt2\norm{\sin\Theta(\widehat V,V)}_{\F}.
\]

\section{The mixed-gap Davis--Kahan baseline used by statisticians}

\sourceanchor{Introduction, stated Davis--Kahan theorem and equation (1).}

Let $\Sigma,\widehat\Sigma\in\R^{p\times p}$ be symmetric, with decreasing eigenvalues
\[
  \lambda_1\ge\cdots\ge\lambda_p,
  \qquad
  \widehat\lambda_1\ge\cdots\ge\widehat\lambda_p.
\]
Fix $1\le r\le s\le p$, set $d=s-r+1$, and choose orthonormal eigenvector blocks
\[
  V=(v_r,\dots,v_s),
  \qquad
  \widehat V=(\widehat v_r,\dots,\widehat v_s),
\]
with
\[
  \Sigma v_j=\lambda_jv_j,
  \qquad
  \widehat\Sigma\widehat v_j=\widehat\lambda_j\widehat v_j.
\]
The source defines a mixed population--sample separation
\[
  \delta
  :=\inf\left\{
      \abs{\widehat\lambda-\lambda}:
      \lambda\in[\lambda_s,\lambda_r],\ 
      \widehat\lambda\in
      (-\infty,\widehat\lambda_{s+1}]
      \cup[\widehat\lambda_{r-1},\infty)
    \right\},
\]
with endpoint conventions printed as
\[
  \widehat\lambda_0=-\infty,
  \qquad
  \widehat\lambda_{p+1}=\infty.
\]

\sourcecomment{These are the \emph{published} indices, checked against the Biometrika article on 2026-08-13. The 2014 preprint prints $\widehat\lambda_{s-1}$ and $\widehat\lambda_{r+1}$ instead, which name the wrong endpoints for a decreasing spectrum: for a block $r,\dots,s$ the sample eigenvalues outside the block are those with index below $r$ (bounded below by $\widehat\lambda_{r-1}$) and above $s$ (bounded above by $\widehat\lambda_{s+1}$). This note previously reproduced the preprint's indices with a comment calling them awkward; the published article settles it. The formal development uses explicit spectral-block separation predicates and was unaffected either way.}

\begin{theorem}[Statistical Davis--Kahan baseline]
If $\delta>0$, then
\[
  \norm{\sin\Theta(\widehat V,V)}_{\F}
  \le
  \frac{\norm{\widehat\Sigma-\Sigma}_{\F}}{\delta}.
\]
\end{theorem}
The source states that both Frobenius norms may be replaced simultaneously by the operator norm or, more generally, by any orthogonally invariant norm.

\sourceanchor{Section 1, numerical illustration that $\delta$ can vanish.}

The source immediately illustrates that $\delta$ can vanish even when the population eigenvalues are well separated. With
\[
  \Sigma=\diag(50,40,30,20,10),
  \qquad
  \widehat\Sigma=\diag(54,37,32,23,21),
\]
and the block $r=s=4$, the population eigenvalue $\lambda_4=20$ lies inside the ray $(-\infty,\widehat\lambda_5]=(-\infty,21]$, so $\delta=0$ and the displayed theorem has no content there --- while the population gap at the same block is $\min(\lambda_3-\lambda_4,\lambda_4-\lambda_5)=10$. This is the motivation for the population-gap theorem of the next section.

For a single eigenvector, $r=s=j$, this specializes to
\[
  \sin\Theta(\widehat v_j,v_j)
  \le
  \frac{\norm{\widehat\Sigma-\Sigma}_{\op}}
       {\min\bigl(
          \abs{\widehat\lambda_{j-1}-\lambda_j},
          \abs{\widehat\lambda_{j+1}-\lambda_j}
        \bigr)}.
\]
After choosing the sign of $\widehat v_j$ so that $\widehat v_j^Tv_j\ge0$,
\[
  \norm{\widehat v_j-v_j}
  \le\sqrt2\sin\Theta(\widehat v_j,v_j).
\]
The usual statistical workflow then invokes Weyl's inequality to replace the random mixed gaps by fractions of the population gaps on a high-probability event. The main theorem eliminates that intermediate event from the theorem statement.

\section{Main population-gap theorem}

\sourceanchor{Theorem 2, equations (2)--(3), sharpness discussion, equation (4), and Corollary 1.}

Define the population exterior gap around the block $r,\dots,s$ by
\[
  \Delta
  :=\min(\lambda_{r-1}-\lambda_r,\lambda_s-\lambda_{s+1}),
\]
with
\[
  \lambda_0:=+\infty,
  \qquad
  \lambda_{p+1}:=-\infty.
\]
Let
\[
  E:=\widehat\Sigma-\Sigma.
\]

\begin{theorem}[Yu--Wang--Samworth population-gap theorem]
If $\Delta>0$, then
\begin{equation}
  \norm{\sin\Theta(\widehat V,V)}_{\F}
  \le
  \frac{2\min\bigl(\sqrt d\norm{E}_{\op},\norm{E}_{\F}\bigr)}{\Delta}. \tag{YWS-1}
\end{equation}
Moreover, there exists $\widehat O\in O(d)$ such that
\begin{equation}
  \norm{\widehat V\widehat O-V}_{\F}
  \le
  \frac{2^{3/2}\min\bigl(\sqrt d\norm{E}_{\op},\norm{E}_{\F}\bigr)}{\Delta}. \tag{YWS-2}
\end{equation}
\end{theorem}

\sourcecomment{Both conclusions are formalized at the printed generality and, since 2026-08-13, in the printed \emph{shape}. \code{TauCeti.consecutiveEmb} is the block $r,\dots,s$ and \code{TauCeti.OrderedBlockBoundaryGap} is the hypothesis $\Delta\le\min(\lambda_{r-1}-\lambda_r,\lambda_s-\lambda_{s+1})$, both in the source's own variables $r$ and $s$, with the block size tied to them by $r+d=s+1$, the printed $d=s-r+1$ written without truncated subtraction. ($d$ cannot be eliminated --- it is the index type of the $d$ columns of $V$ and $\widehat V$ --- but it is pinned by that hypothesis, and both the gap and the bound are written at $r$ and $s$.) The printed conventions $\lambda_0=+\infty$ and $\lambda_{p+1}=-\infty$ are modelled by \emph{vacuous quantification} --- there is no index $q$ with $q+1=r$ when $r$ is the first index --- so the interior, top and bottom blocks need no separate statements and no extended arithmetic is introduced. \code{OrderedBlockBoundaryGap.indexGap} propagates it to the intrinsic separation the general theorem consumes, using only antitonicity of the sorted spectrum, and it constrains the population spectrum alone: $\widehat\lambda_s=\widehat\lambda_{s+1}$ remains admissible, so the wrapper does not reintroduce a sample eigengap. (YWS-1) is \code{TauCeti.yuWangSamworth\_sinTheta\_block\_le}.

For (YWS-2) the Lean conclusion now exhibits $\widehat O$: \code{TauCeti.frameComp hv O} is the matrix product $\widehat V\widehat O$, whose $i$th column is $\sum_j\widehat O_{ji}\widehat v_j$, and \code{yuWangSamworth\_alignedFrame\_block\_le} bounds $\norm{\widehat V\widehat O-V}_{\F}$ against the \emph{supplied} population frame. $\widehat O$ is a bundled linear isometry equivalence of the block's coordinate space, which over $\R$ is exactly an element of $O(d)$; \code{TauCeti.adjoint\_comp\_self\_eq\_id} states this as $\widehat O^T\widehat O=I$. The earlier statement asserted only that \emph{some} pair of frames spanning the two blocks achieves the bound, which is the basis-free content of the same claim but does not say that the population frame is untouched. Over $\R$ the alignment is produced as an honest matrix: \code{yuWangSamworth\_alignedFrame\_block\_real\_le} returns $\widehat O$ in \code{Matrix.orthogonalGroup (Fin d) R} with $(\widehat V\widehat O)_i=\sum_j\widehat O_{ji}\widehat v_j$, so every symbol of the printed conclusion appears in one statement.}

The paper emphasizes two improvements over the commonly quoted statistical Davis--Kahan form:
\begin{enumerate}[leftmargin=2em]
\item the denominator contains only population eigenvalues;
\item the numerator automatically chooses between the dimension-scaled operator norm and the Frobenius norm.
\end{enumerate}

Let
\[
  \Lambda:=\diag(\lambda_r,\dots,\lambda_s).
\]
The proof actually establishes the sharper residual forms
\[
  \norm{\sin\Theta(\widehat V,V)}_{\F}
  \le
  \frac{\norm{\widehat V\Lambda-\Sigma\widehat V}_{\F}}{\Delta},
\]
and
\[
  \norm{\widehat V\widehat O-V}_{\F}
  \le
  \frac{\sqrt2\norm{\widehat V\Lambda-\Sigma\widehat V}_{\F}}{\Delta}.
\]
The displayed theorem replaces the residual by a directly usable perturbation bound.

\sourcecomment{Both residual forms are compiled, at frame generality and with the source's indexing: \code{yuWangSamworth\_sinTheta\_block\_le\_residual} and \code{yuWangSamworth\_alignedFrame\_block\_le\_residual}, the second again exhibiting $\widehat O$. They are reachable directly from the source-facing surface rather than only from an internal module, since the paper states them as available results rather than as proof steps.}

\subsection{Sharpness example with orthogonal target subspaces}

\sourcecomment{The construction reproduced below is the \emph{preprint's}: the block of interest is the top $d$ eigenvectors of $\Sigma=\diag(3,\dots,3,1,\dots,1)$. The published article replaces it by a middle-block construction: $\Sigma=\diag(\lambda_1,\dots,\lambda_p)$ with $\lambda_1=\dots=\lambda_{p-2d}=5$, $\lambda_{p-2d+1}=\dots=\lambda_{p-d}=3$ and $\lambda_{p-d+1}=\dots=\lambda_p=1$, and $\widehat\Sigma$ diagonal with first $p-2d$ entries $5$, next $d$ entries $2$ and last $d$ entries $2+\epsilon$, the block of interest being the eigenvectors for the eigenvalue $3$. The population gap is then genuinely two-sided, $\min(5-3,3-1)=2$. Both constructions give orthogonal blocks, $\norm{\widehat V\widehat O-V}_{\F}=\sqrt2\norm{\sin\Theta}_{\F}=\sqrt{2d}$ against a bound of $\sqrt{2d}(1+\epsilon)$, so both prove the same sharpness claim. \textbf{Both are now formalized}: the preprint's in \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/OrthogonalSharpness.lean} and the published one in \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/MiddleBlockSharpness.lean}, the latter over the full range $0<\epsilon<3$. The endpoints are not counterexamples but degeneracies of the construction: the model needs $2<2+\epsilon<5$ so that $2+\epsilon$ is the \emph{second} level of the sorted sample spectrum and the perturbed block occupies the same ordered positions as the population block of threes. At $\epsilon=3$ the level $2+\epsilon$ merges with the leading level $5$ and its multiplicity becomes $p-d$ (\code{card\_middleSharpnessSample\_level\_three}); at $\epsilon=0$ it merges downwards with the $2$s. So the range is maximal whenever $2d<p$; in the degenerate case $p=2d$ there is no leading level $5$ and $\epsilon<3$ is sufficient without being necessary. The published instance was the harder of the two because its block sits in the middle of both spectra, so the branch-selection hypothesis cannot be produced by any ``leading $d$ eigenvectors'' argument; the missing foundation was the position of an arbitrary eigenvalue level set inside the sorted eigenbasis.}

Let
\[
  \Sigma=\diag(\underbrace{3,\dots,3}_{d},
                       \underbrace{1,\dots,1}_{p-d}),
\]
and
\[
  \widehat\Sigma
  =\diag(\underbrace{2-\varepsilon,\dots,2-\varepsilon}_{p-d},
         \underbrace{2,\dots,2}_{d}),
\]
where $0<\varepsilon$ and $1\le d\le\lfloor p/2\rfloor$. The top-$d$ eigenspaces are orthogonal. Hence for every $O\in O(d)$,
\[
  \norm{\widehat VO-V}_{\F}
  =\sqrt2\norm{\sin\Theta(\widehat V,V)}_{\F}
  =\sqrt{2d}.
\]
The perturbation norm and population gap give
\[
  \sqrt{2d}
  \le\sqrt{2d}(1+\varepsilon)
  =
  \frac{2^{3/2}\sqrt d\norm{E}_{\op}}
       {\lambda_d-\lambda_{d+1}}.
\]
This shows that the aligned-basis constant and dimension dependence cannot be uniformly improved beyond the displayed constant scale.

\subsection{Sharpness scale for nearby one-dimensional eigenspaces}

Take
\[
  \Sigma=\diag(3,1),
\]
and
\[
  \widehat\Sigma
  =R_\varepsilon\diag(3,1)R_\varepsilon^T,
  \qquad
  R_\varepsilon=
  \begin{pmatrix}
    \sqrt{1-\varepsilon^2}&-\varepsilon\\
    \varepsilon&\sqrt{1-\varepsilon^2}
  \end{pmatrix}.
\]
For
\[
  v=(1,0)^T,
  \qquad
  \widehat v=(\sqrt{1-\varepsilon^2},-\varepsilon)^T,
\]
we have
\[
  \sin\Theta(\widehat v,v)=\varepsilon,
\]
\[
  \norm{\widehat v-v}^2
  =2-2\sqrt{1-\varepsilon^2},
\]
and
\[
  \frac{2\norm{\widehat\Sigma-\Sigma}_{\op}}{3-1}
  =2\varepsilon.
\]
Thus the sine bound is tight up to factor $2$, and the vector bound up to factor $2^{3/2}$, even in a small-angle regime.

\subsection{Why a double-angle estimate does not directly solve the statistical problem}

For oriented unit vectors $v,\widehat v$ with $c:=\widehat v^Tv\ge0$,
\[
  \sin^2(2\Theta(\widehat v,v))
  =(2c)^2(1-c^2).
\]
Since
\[
  \norm{\widehat v-v}^2=2-2c,
\]
the source rewrites this as its equation (4). \textbf{The printed equation~(4) is
false: it omits a square on the factor $2-\norm{\widehat v-v}^2$.} Writing
$t:=\norm{\widehat v-v}^2=2-2c$, so that $c=1-t/2$, $1-c=t/2$ and
$1+c=(4-t)/2$, gives
\[
  (2c)^2(1-c^2)=4c^2(1-c)(1+c)=c^2t(4-t)=\tfrac14 t(2-t)^2(4-t),
\]
so the correct identity is
\begin{equation}
  \sin^2(2\Theta(\widehat v,v))
  =\frac14\norm{\widehat v-v}^2
    \bigl(2-\norm{\widehat v-v}^2\bigr)^2
    \bigl(4-\norm{\widehat v-v}^2\bigr). \tag{YWS-3}
\end{equation}
\sourcecomment{Corrected 2026-07-29. This note previously reproduced the
printed form, with $2-\norm{\widehat v-v}^2$ to the first power, so the
repository asserted a false identity here while refuting it in Lean. Both
directions are machine checked:
\code{TauCeti.DavisKahanTheory.yuWangSamworth\_equation4} proves the corrected
identity and
\code{TauCeti.DavisKahanTheory.yuWangSamworth\_equation4\_printed\_counterexample}
refutes the printed polynomial at $c=3/5$. The defect is recorded as item
\code{YWS-EQ4-double-angle} of \code{dev/yu-wang-samworth-2015-full-source-census.json}.}
A bound on $\sin2\theta$ alone does not control $\sin\theta$ globally because $\sin2\theta$ is small both near $0$ and near $\pi/2$. An additional acute-overlap hypothesis, such as $c\ge1/\sqrt2$, would be needed.

\begin{corollary}[Rank-one form]
Fix $j$ and assume
\[
  \Delta_j:=\min(\lambda_{j-1}-\lambda_j,
                  \lambda_j-\lambda_{j+1})>0.
\]
For normalized eigenvectors $v,\widehat v$ corresponding to the $j$th decreasing eigenvalues,
\[
  \sin\Theta(\widehat v,v)
  \le
  \frac{2\norm{E}_{\op}}{\Delta_j}.
\]
If $\widehat v^Tv\ge0$, then
\[
  \norm{\widehat v-v}
  \le
  \frac{2^{3/2}\norm{E}_{\op}}{\Delta_j}.
\]
\end{corollary}

\sourcecomment{Both displays are compiled with exactly this hypothesis --- only the two neighbouring population eigenvalues --- as \code{yuWangSamworth\_corollary1\_sinTheta\_le} and, over $\R$ under $\widehat v^Tv\ge0$, \code{yuWangSamworth\_corollary1\_real\_le}. That the sample eigenvector is genuinely arbitrary is witnessed by \code{yuWangSamworth\_corollary1\_scalarSample}: for $\Sigma=\diag(1,0)$ and $\widehat\Sigma=I/2$ every unit vector of the plane satisfies $\widehat\Sigma\widehat v=\widehat\lambda_1\widehat v$, and the corollary bounds the angle for each of them. A formulation pinning $\widehat v$ to a chosen eigenbasis of $\widehat\Sigma$ would cover exactly one.}

\section{The source's audit of statistical applications}

This section of the paper does not introduce a new perturbation theorem. It makes the following mathematical diagnosis of existing proof patterns.

\begin{enumerate}[leftmargin=2em]
\item In covariance, correlation, sparse PCA, factor-model, false-discovery, and leave-one-observation analyses, authors often apply a mixed-gap Davis--Kahan theorem and then use Weyl's inequality to recover a population-gap denominator. The population-gap theorem packages those two steps into one deterministic result.
\item Spectral clustering papers apply variants to adjacency matrices or graph Laplacians. Statements that appear to assume only a population gap may still hide a requirement that the perturbed and population matrices have the same number of eigenvalues in the selected interval. This is effectively a mixed spectral-separation condition.
\item The new theorem avoids that hidden interval-counting requirement by selecting corresponding ordered eigenblocks and imposing only the two exterior population gaps.
\end{enumerate}

These are literature-comparison claims rather than independent formal theorem statements. They motivate the theorem's interface and explain why its denominator is useful in probabilistic applications.

\sourcecomment{The claims about what other authors wrote are prose and stay prose, but their deterministic mathematical content is formalized. \code{TauCeti.mixedGap\_of\_populationGap\_weyl} is the recovery step of item 1 --- a population gap $\Delta$ at the selected index block becomes a mixed gap $\Delta-\epsilon$ once the perturbation is $\epsilon$-operator-small --- and \code{yuWangSamworth\_weylRecovered\_le\_populationGap\_bound} shows that on the event $2\epsilon\le\Delta$ the two-step route lands on exactly the constant the population-gap theorem proves with no event at all, which is item 3. That the event is not automatic is witnessed by the paper's own Section 1 data, where $\Delta=10$ and $\norm{E}_{\op}=11$; and in that same example no sample eigenvalue lies in the interval spanned by the population block, so the interval-counting requirement of item 2 fails there too.}

\section{Extension to rectangular matrices and singular subspaces}

\sourceanchor{Theorem 3 and equations (A7)--(A8) in the Appendix proof.}

Let $A,\widehat A\in\R^{p\times q}$ and set
\[
  D:=\widehat A-A.
\]
Let the singular values be
\[
  \sigma_1\ge\cdots\ge\sigma_{\min(p,q)},
  \qquad
  \widehat\sigma_1\ge\cdots\ge\widehat\sigma_{\min(p,q)}.
\]
Fix
\[
  1\le r\le s\le\rank(A),
  \qquad
  d=s-r+1,
\]
and define the squared population singular gap
\[
  \Delta_{\mathrm{sv}}
  :=\min(\sigma_{r-1}^2-\sigma_r^2,
         \sigma_s^2-\sigma_{s+1}^2)>0,
\]
with source conventions
\[
  \sigma_0^2:=+\infty,
  \qquad
  \sigma_{\rank(A)+1}^2:=-\infty.
\]

\sourcecomment{\textbf{The second printed convention is false.} With $s=\rank(A)$ it makes $\Delta_{\mathrm{sv}}=\min(\infty,\infty)=\infty$, so the displayed bound below asserts $\norm{\sin\Theta(\widehat V,V)}_{\F}\le0$ --- that the sample and population right singular subspaces coincide. They need not: take $A$ and $\widehat A$ to be the orthogonal projections of $\R^2$ onto the two coordinate axes. Both have rank one, so $r=s=1=\rank(A)$ is admissible and the printed hypothesis holds; yet the two right singular subspaces are orthogonal and $\norm{\sin\Theta}_{\F}=1$. Machine-checked in \code{YuWangSamworth2015/YuWangSamworth2015/Rectangular/RankBoundary.lean} as \code{yuWangSamworth_theorem3_printed_rankBoundary_refutation}.

The defect is a mis-stated index, and the paper's own proof shows the intended one. That proof passes to $A^TA\in\R^{q\times q}$, ``with eigenvalues $\sigma_1^2\ge\cdots\ge\sigma_q^2$'', and applies Theorem 2, whose convention is $\lambda_{q+1}:=-\infty$ --- at the \emph{ambient} index $q$, not at $\rank(A)$. So the corrected conventions are
\[
  \sigma_0^2:=+\infty,
  \qquad
  \sigma_{q+1}^2:=-\infty,
  \qquad
  \sigma_j:=0\ \text{for}\ \min(p,q)<j\le q,
\]
for the right singular blocks, and $\sigma_{p+1}^2:=-\infty$ for the left ones. Under that reading the theorem below is true, and it is the reading the Lean development has always used: its gap hypothesis is the intrinsic separation of the sorted spectrum of $A^\star A$, which counts the zero eigenvalues. Recorded as gap \code{theorem3-rank-boundary}; no erratum is known.}
Let $V,\widehat V\in\R^{q\times d}$ contain the corresponding right singular vectors:
\[
  Av_j=\sigma_ju_j,
  \qquad
  \widehat A\widehat v_j=\widehat\sigma_j\widehat u_j.
\]

\begin{theorem}[Yu--Wang--Samworth singular-subspace theorem]
\[
  \norm{\sin\Theta(\widehat V,V)}_{\F}
  \le
  \frac{
    2(2\sigma_1+\norm{D}_{\op})
    \min(\sqrt d\norm{D}_{\op},\norm{D}_{\F})
  }{\Delta_{\mathrm{sv}}}.
\]
Moreover, some $\widehat O\in O(d)$ satisfies
\[
  \norm{\widehat V\widehat O-V}_{\F}
  \le
  \frac{
    2^{3/2}(2\sigma_1+\norm{D}_{\op})
    \min(\sqrt d\norm{D}_{\op},\norm{D}_{\F})
  }{\Delta_{\mathrm{sv}}}.
\]
The identical statements hold for the corresponding left singular-vector blocks $U,\widehat U$.
\end{theorem}

The paper presents this as a population-gap counterpart of Wedin's generalized $\sin\theta$ theorem.

\sourcecomment{The \emph{corrected} theorem is now also stated in the paper's notation --- consecutive singular indices and the squared boundary gap --- in \code{YuWangSamworth2015/YuWangSamworth2015/Rectangular/SingularBlock.lean}. The bridge \code{sq\_singularValues\_eq\_eigenvalues\_rightGram} is where the correction enters: \code{LinearMap.singularValues} vanishes past the rank by construction, so squared singular values and the sorted spectrum of $A^\star A$ agree at \emph{every} ambient index, zeros included. \code{singularBoundaryGap\_of\_rank\_le} makes the difference from the printed convention concrete: when the block ends at or past $\rank(A)$ the correct lower boundary gap is $\sigma_s^2-0$, a finite positive number the caller must supply, where the printed convention offered a vacuous one. These statements are labelled corrected, not exact.}

\section{Appendix lemma: Frobenius compression}

\begin{lemma}[Orthogonal compression]
Let $M\in\R^{m\times n}$. If $U\in\R^{m\times p}$ and $W\in\R^{n\times q}$ have orthonormal columns, then
\[
  \norm{U^TMW}_{\F}\le\norm{M}_{\F}.
\]
If instead $U$ and $W$ have orthonormal rows, then
\[
  \norm{U^TMW}_{\F}=\norm{M}_{\F}.
\]
\end{lemma}

For the inequality, extend $U,W$ to square orthogonal matrices $[U\ U_1]$ and $[W\ W_1]$. Orthogonal invariance of the Frobenius norm gives
\[
  \norm{M}_{\F}
  =\norm{
     \begin{pmatrix}U^T\\U_1^T\end{pmatrix}
     M
     \begin{pmatrix}W&W_1\end{pmatrix}
   }_{\F}
  \ge\norm{U^TMW}_{\F}.
\]
For orthonormal rows, $UU^T=I_m$ and $WW^T=I_n$, so
\[
  \norm{U^TMW}_{\F}^2
  =\tr(U^TMWW^TM^TU)
  =\tr(MM^TUU^T)
  =\tr(MM^T)
  =\norm{M}_{\F}^2.
\]

\section{Proof of the population-gap theorem}

Set
\[
  \Lambda:=\diag(\lambda_r,\dots,\lambda_s),
  \qquad
  \widehat\Lambda:=\diag(\widehat\lambda_r,\dots,\widehat\lambda_s).
\]
The perturbed eigen-equation gives the residual identity
\[
  0=\widehat\Sigma\widehat V-
       \widehat V\widehat\Lambda
   =\Sigma\widehat V-
       \widehat V\Lambda
     +E\widehat V-
       \widehat V(\widehat\Lambda-\Lambda).
\]
Hence
\[
  R:=\widehat V\Lambda-
      \Sigma\widehat V
  =E\widehat V-
    \widehat V(\widehat\Lambda-\Lambda).
\]

\subsection{Two upper bounds for the residual}

Using the compression lemma and Weyl's eigenvalue inequality,
\begin{align}
  \norm{R}_{\F}
  &\le\norm{E\widehat V}_{\F}
      +\norm{\widehat V(\widehat\Lambda-\Lambda)}_{\F}\notag\\
  &\le\sqrt d\norm{E}_{\op}
      +\norm{\widehat\Lambda-\Lambda}_{\F}\notag\\
  &\le2\sqrt d\norm{E}_{\op}. \tag{YWS-4}
\end{align}
Alternatively, using Frobenius compression and Hoffman--Wielandt,
\begin{align}
  \norm{R}_{\F}
  &\le\norm{E}_{\F}
      +\norm{\widehat\Lambda-\Lambda}_{\F}\notag\\
  &\le2\norm{E}_{\F}. \tag{YWS-5}
\end{align}
Therefore
\[
  \norm{R}_{\F}
  \le2\min(\sqrt d\norm{E}_{\op},\norm{E}_{\F}).
\]

\subsection{Population gap gives the lower bound}

Choose $V_1\in\R^{p\times(p-d)}$ such that
\[
  P:=[V\ V_1]\in O(p),
\]
and
\[
  P^T\Sigma P=
  \begin{pmatrix}
    \Lambda&0\\0&\Lambda_1
  \end{pmatrix},
\]
where $\Lambda_1$ contains all eigenvalues outside the selected block.

Decompose $R$ in the $V\oplus V_1$ splitting. Orthogonality and Frobenius contraction give
\begin{align}
  \norm{R}_{\F}
  &\ge
  \norm{V_1V_1^T\widehat V\Lambda-
        V_1\Lambda_1V_1^T\widehat V}_{\F}\notag\\
  &\ge
  \norm{V_1^T\widehat V\Lambda-
        \Lambda_1V_1^T\widehat V}_{\F}. \tag{YWS-6}
\end{align}
Vectorization uses
\[
  \operatorname{vec}(ABC)
  =(C^T\otimes A)\operatorname{vec}(B),
\]
so
\begin{align}
 &\norm{V_1^T\widehat V\Lambda-
        \Lambda_1V_1^T\widehat V}_{\F}\notag\\
 &\quad=
 \norm{(\Lambda\otimes I_{p-d}-I_d\otimes\Lambda_1)
       \operatorname{vec}(V_1^T\widehat V)}\notag\\
 &\quad\ge
 \Delta\norm{\operatorname{vec}(V_1^T\widehat V)}. \tag{YWS-7}
\end{align}
Every diagonal entry of the Kronecker difference has magnitude at least $\Delta$ because one eigenvalue lies inside the selected population block and the other lies outside it.

Finally,
\begin{align*}
  \norm{\operatorname{vec}(V_1^T\widehat V)}^2
  &=\tr(\widehat V^TV_1V_1^T\widehat V)\\
  &=\tr((I-VV^T)\widehat V\widehat V^T)\\
  &=d-\norm{\widehat V^TV}_{\F}^2\\
  &=\norm{\sin\Theta(\widehat V,V)}_{\F}^2.
\end{align*}
Combining (YWS-4)--(YWS-7) proves (YWS-1).

\subsection{Procrustes alignment}

Choose $O_1,O_2\in O(d)$ so that
\[
  O_1^T\widehat V^TVO_2
  =\diag(\cos\theta_1,\dots,\cos\theta_d),
\]
and set
\[
  \widehat O:=O_1O_2^T.
\]
Then
\begin{align}
  \norm{\widehat V\widehat O-V}_{\F}^2
  &=2d-2\sum_{j=1}^d\cos\theta_j\notag\\
  &\le2d-2\sum_{j=1}^d\cos^2\theta_j\notag\\
  &=2\norm{\sin\Theta(\widehat V,V)}_{\F}^2. \tag{YWS-8}
\end{align}
Together with (YWS-1), this gives (YWS-2).

\section{Proof of the singular-subspace theorem}

Apply the symmetric theorem to the Gram matrices
\[
  A^TA,
  \qquad
  \widehat A^T\widehat A.
\]
Their selected eigenvectors are precisely the right singular vectors, and the eigenvalues are the squared singular values. Thus
\begin{equation}
  \norm{\sin\Theta(\widehat V,V)}_{\F}
  \le
  \frac{2\min\bigl(
      \sqrt d\norm{\widehat A^T\widehat A-A^TA}_{\op},
      \norm{\widehat A^T\widehat A-A^TA}_{\F}
    \bigr)}{\Delta_{\mathrm{sv}}}. \tag{YWS-9}
\end{equation}

Since
\[
  \widehat A^T\widehat A-A^TA
  =D^T\widehat A+A^TD,
\]
submultiplicativity gives
\begin{align}
  \norm{\widehat A^T\widehat A-A^TA}_{\op}
  &\le(\norm{\widehat A}_{\op}+\norm{A}_{\op})
       \norm{D}_{\op}\notag\\
  &\le(2\sigma_1+\norm{D}_{\op})\norm{D}_{\op}. \tag{YWS-10}
\end{align}
Here
\[
  \norm{\widehat A}_{\op}
  \le\norm{A}_{\op}+\norm{D}_{\op}
  =\sigma_1+\norm{D}_{\op}.
\]

For the Frobenius estimate, vectorization and Kronecker operator norms yield
\begin{align}
  \norm{\widehat A^T\widehat A-A^TA}_{\F}
  &\le
  \norm{(\widehat A^T\otimes I_q)
        \operatorname{vec}(D^T)}
  +\norm{(I_p\otimes A^T)\operatorname{vec}(D)}\notag\\
  &\le
  (\norm{\widehat A}_{\op}+\norm{A}_{\op})
  \norm{D}_{\F}\notag\\
  &\le
  (2\sigma_1+\norm{D}_{\op})\norm{D}_{\F}. \tag{YWS-11}
\end{align}
Substituting (YWS-10)--(YWS-11) into (YWS-9) proves the right-subspace bound. The Procrustes conclusion follows from (YWS-8). Applying the same argument to $AA^T$ and $\widehat A\widehat A^T$ proves the left-subspace version.

\section{Formalization map}

\begin{formalizationmap}
Principal angles & Singular values of $\widehat V^TV$, Frobenius sine distance, cross-complement identity. & \code{ForTauCeti/.../PrincipalAngles.lean}, \code{AlignedBasis.lean}, and the canonical subspace API in \code{ForTauCeti/.../YuWangSamworth/Statistics.lean}. \\
Ordered eigenframes & An orthonormal family of eigenvectors belonging to the \emph{sorted} eigenvalues at prescribed indices, with the choice inside a repeated eigenspace left free. This is the paper's hypothesis on $V$ and $\widehat V$. & \code{ForTauCeti/.../Spectral/EigenFrame.lean}: \code{IsEigenFamily}, \code{IsOrderedEigenframe}, \code{IsOrderedEigenframe.span_eq_spanIndices}, \code{IsOrderedEigenframe.internalGap_span}. Fully proved. \\
Population residual lower bound & $\Delta^2$ times the energy of the trial frame outside the population block is bounded by the squared residual. Nothing is assumed about the trial frame. & \code{ForTauCeti/.../Spectral/ResidualGap.lean}: \code{sq_gap_mul_sum_sq_norm_starProjection_orthogonal_le}. Fully proved. \\
Residual upper bounds & Frobenius branch by orthonormal compression and Hoffman--Wielandt; operator branch by Weyl at the selected index. & \code{ForTauCeti/.../YuWangSamworth/Residual.lean}: \code{sum_sq_norm_apply_orthonormal_le}, \code{sum_sq_norm_frameResidual_le}, \code{sum_sq_norm_frameResidual_le_of_opNorm}. Fully proved. \\
Sine theorem (YWS-1) & Population-only gap bound, for arbitrary ordered eigenframes on both sides. & \code{ForTauCeti/.../YuWangSamworth/Statistics.lean}: \code{yuWangSamworth_sinTheta_frame_le}; index-block form \code{yuWangSamworth_sinTheta_le}. Fully proved. \\
The source's $r{:}s$ indexing & The contiguous block $r,\dots,s$ and the two-sided boundary gap $\min(\lambda_{r-1}-\lambda_r,\lambda_s-\lambda_{s+1})$, with $\lambda_0=+\infty$ and $\lambda_{p+1}=-\infty$ modelled by vacuous quantification. Constrains the population spectrum only, so repeated sample eigenvalues stay admissible. & \code{ForTauCeti/.../YuWangSamworth/ConsecutiveBlock.lean}: \code{consecutiveEmb}, \code{OrderedBlockBoundaryGap}, \code{OrderedBlockBoundaryGap.indexGap}; the printed statements \code{yuWangSamworth_sinTheta_block_le}, \code{yuWangSamworth_alignedFrame_block_le} and the two residual forms. Fully proved. \\
Residual-numerator forms & The sharper inequalities the paper says its proof establishes. & \code{Statistics.lean}: \code{yuWangSamworth_sinTheta_le_residual}, \code{yuWangSamworth_alignedBasis_le_residual}. Fully proved. \\
Procrustes step and (YWS-2) & Aligned orthonormal frames with squared distance at most twice the squared sine distance, giving the constant $2^{3/2}$. & \code{AlignedBasis.lean}: \code{sum_sq_norm_aligned_le}; \code{Statistics.lean}: \code{exists_aligned_orthonormalBasis}, \code{yuWangSamworth_alignedBasis_frame_le}. Fully proved. \\
The alignment $\widehat O$ itself & $\widehat V\widehat O$ as a matrix product, with the population frame held fixed and $\widehat O^T\widehat O=I$ available; over $\R$, $\widehat O$ as an element of $O(d)$. & \code{ForTauCeti/.../YuWangSamworth/Procrustes.lean}: \code{frameComp}, \code{frameComp_apply}, \code{adjoint_comp_self_eq_id}, \code{exists_unitary_sum_sq_norm_frameComp_sub_le}, \code{yuWangSamworth_alignedFrame_le}, \code{yuWangSamworth_alignedFrame_le_residual}, \code{frameAlignMatrix}, \code{frameAlignMatrix_mem_orthogonalGroup}, \code{yuWangSamworth_alignedFrame_real_le}; \code{ConsecutiveBlock.lean}: \code{yuWangSamworth_alignedFrame_block_real_le}. Fully proved. \\
Corollary 1 & Both displays: the single-eigenvector sine bound, and the real sign-aligned difference with constant $2^{3/2}$. & \code{Statistics.lean}: \code{yuWangSamworth_eigenvector_frame_sinTheta_le}, \code{yuWangSamworth_eigenvector_real_le}, \code{yuWangSamworth_eigenvector_frame_le}; with the printed neighbouring-eigenvalue gap, \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/Corollary1.lean}: \code{yuWangSamworth_corollary1_sinTheta_le}, \code{yuWangSamworth_corollary1_real_le}. Fully proved. \\
Sample degeneracy & A witness that the sample eigenvector is genuinely arbitrary: $\Sigma=\diag(1,0)$, $\widehat\Sigma=I/2$, every unit vector admissible. & \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/Corollary1.lean}: \code{yuWangSamworth_corollary1_scalarSample}; supporting counting lemmas \code{eigenvalues_basisDiagonal_eq_of_card}, \code{eigenvalues_basisDiagonal_const}. Fully proved. \\
Equation (4) & Corrected double-angle identity, with the printed polynomial refuted. & \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/AngleIdentity.lean}: \code{yuWangSamworth_equation4}, \code{yuWangSamworth_equation4_printed_counterexample}. \\
Theorem 3 & Right and left singular-subspace bounds through the Gram operators, for arbitrary ordered singular frames. & \code{YuWangSamworth2015/YuWangSamworth2015/Rectangular/Theorem4.lean}: \code{yuWangSamworth_rightSingularSubspace_frame_le}, \code{yuWangSamworth_leftSingularSubspace_frame_le}, and the aligned-frame counterparts. Proved in the corrected form. \\
Theorem 3's rank-boundary convention & The printed $\sigma_{\rank(A)+1}^2:=-\infty$ is refuted; the ambient-dimension convention is the repair. & \code{YuWangSamworth2015/YuWangSamworth2015/Rectangular/RankBoundary.lean}: \code{yuWangSamworth_theorem3_printed_rankBoundary_refutation}, which also concludes $\norm{\sin\Theta}_{\F}=1$. \\
Theorem 3 in singular-value notation & Consecutive singular indices and the squared boundary gap, with the corrected zero continuation past the rank. & \code{YuWangSamworth2015/YuWangSamworth2015/Rectangular/SingularBlock.lean}: \code{sq_singularValues_eq_eigenvalues_rightGram}, \code{singularBoundaryGap_of_rank_le}, \code{yuWangSamworth_rightSingularSubspace_block_le} and its three siblings. Proved in the corrected form. \\
Lemma A1 & Orthonormal compression cannot increase the Frobenius norm; orthonormal rows give equality. & \code{YuWangSamworth2015/YuWangSamworth2015/Appendix/Lemma5.lean} (preprint numbering in the name), plus the column-sum form \code{TauCeti.sum_sq_norm_apply_orthonormal_le} that the Theorem 2 proof consumes. \\
Theorem 1's mixed separation, and the Section 1 illustration & The printed $\delta$ as an infimum over the two rays, and the printed five-by-five pair at which it vanishes while the population gap is $10$. & \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/MixedGap.lean}: \code{mixedSeparation}, \code{section1\_mixedSeparation\_eq\_zero}, \code{section1\_populationGap}, \code{internalGap\_section1}. Fully proved. \\
Section 3's diagnosis & The deterministic content of the mixed-gap-then-Weyl pattern: the recovery step, its side condition, and the fact that on that side condition it lands on the population-gap constant. & \code{Statistics.lean}: \code{mixedGap\_of\_populationGap\_weyl}; \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/MixedGap.lean}: \code{yuWangSamworth\_weylRecovered\_le\_populationGap\_bound}, \code{section1\_weylRecovery\_fails}, \code{section1\_no\_sample\_eigenvalue\_in\_block}. Fully proved. \\
Sharpness & All three Section 2 constructions, against the theorems' own hypotheses; the published middle-block model over its full range $0<\epsilon<3$, maximal for $2d<p$. & \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/OrthogonalSharpness.lean} (preprint's top-block form), \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/MiddleBlockSharpness.lean} (the published middle-block form, with \code{card_middleSharpnessSample_level_three} recording the upper breakpoint), \code{YuWangSamworth2015/YuWangSamworth2015/Symmetric/PlanarSharpness.lean}. \\
Middle-block branch selection & The position of an arbitrary eigenvalue level set inside Mathlib's sorted eigenbasis, which is what a middle block needs. & \code{ForTauCeti/.../EigenblockSpan.lean}: \code{eigenvalues\_level\_eq\_Ico}, \code{spanIndices\_Ico\_eq\_eigenspace}; \code{BasisDiagonal.lean}: \code{card\_filter\_lt\_eigenvalues\_basisDiagonal}; \code{YuWangSamworth/TopEigenblock.lean}: \code{correspondingEigenblock\_eigenvalueLevel}. Fully proved. \\
\end{formalizationmap}

\section{Source-faithfulness and transcription audit}

\begin{enumerate}[leftmargin=2em]
\item The paper appeared in 2015, while the reconstruction was originally built from a 2014 preprint transcription. The published Biometrika article was read on 2026-08-13 and this note now follows it. Three differences from the preprint are recorded in place: the numbering (item below), Theorem 1's endpoint indices, and the Section 2 orthogonal-blocks sharpness construction.
\item \textbf{The numbering question is settled.} The published article numbers the results Theorem 1, Theorem 2, Corollary 1, Theorem 3, Lemma A1, with displayed equations (1)--(4) and appendix equations (A1)--(A8) --- which is what this note's \code{\textbackslash sourceanchor} lines already used. The 2014 preprint shares one counter across environments and therefore numbers the same results Theorem 1, Theorem 2, Corollary 3, Theorem 4, Lemma 5, which several declarations used by \code{YuWangSamworth2015} still spell. Those names are pinned by \code{comparator/*.json} and are not being changed; the translation table --- Corollary 1 $=$ Corollary 3, Theorem 3 $=$ Theorem 4, Lemma A1 $=$ Lemma 5 --- lives in \code{dev/yu-wang-samworth-2015-full-source-census.json} under gap \code{preprint-numbering-aliases}.
\item The transcription contains a duplicate, deprecated Davis--Kahan theorem and unfinished scratch calculations after its end-of-document marker. They are not part of the paper and are excluded from the reconstruction.
\item \textbf{Two printed statements in this paper are false, and both are machine checked here in both directions.} Equation (4) omits a square on $2-\norm{\widehat v-v}^2$; see the correction recorded with that equation. Theorem 3's convention $\sigma_{\rank(A)+1}^2:=-\infty$ is wrong at a rank boundary; see the correction recorded with that theorem. Each carries a compiled counterexample to the printed statement and a compiled corrected statement.
\item The source uses finite real matrices. The Lean development generalizes the central symmetric-operator statements to finite-dimensional real or complex inner-product spaces and expresses subspaces intrinsically.
\item The source's SVD theorem is obtained through squared Gram matrices. The repository additionally contains a self-adjoint dilation route, which is mathematically equivalent but should be identified as a modern alternative rather than attributed to the paper.
\end{enumerate}

\begin{thebibliography}{8}
\bibitem{YWS} Y. Yu, T. Wang, and R. J. Samworth, \emph{A Useful Variant of the Davis--Kahan Theorem for Statisticians}, Biometrika 102 (2015), 315--323.
\bibitem{DavisKahan1970} C. Davis and W. M. Kahan, \emph{The Rotation of Eigenvectors by a Perturbation. III}, SIAM J. Numer. Anal. 7 (1970), 1--46.
\bibitem{Wedin1972} P.-\AA. Wedin, \emph{Perturbation bounds in connection with singular value decomposition}, BIT 12 (1972), 99--111.
\bibitem{HoffmanWielandt1953} A. J. Hoffman and H. W. Wielandt, \emph{The variation of the spectrum of a normal matrix}, Duke Math. J. 20 (1953), 37--39.
\bibitem{StewartSun1990} G. W. Stewart and J.-G. Sun, \emph{Matrix Perturbation Theory}, Academic Press, 1990.
\end{thebibliography}

\end{document}
