import DavisKahan.Experimental.MathAhead.HiddenFoundations.ContourReuseBridge
import DavisKahan.Experimental.MathAhead.HiddenFoundations.CircleContourGeometry

/-!
# Section 8 continuation mathematics-ahead aggregate

This lane is intentionally separate because its existing consumer chain still
passes through the pre-existing `SinTheta/General.lean` Mathlib-migration
breakage.  The circle geometry and contour bridge can be repaired independently
before that consumer is rebuilt.
-/
