\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs,tabularx,array}
\usepackage[section]{placeins}
\usepackage{float}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,allcolors=blue]{hyperref}
\usepackage{pdfcomment}
\usepackage[nameinlink]{cleveref}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{listingsutf8}
\setlength{\emergencystretch}{3em}

% ArXiv-friendly Lean code: no shell escape, minted, or external highlighter.
\definecolor{leankeyword}{rgb}{0.0,0.0,0.55}
\definecolor{leancomment}{rgb}{0.0,0.48,0.15}
\definecolor{leannumber}{rgb}{0.45,0.45,0.45}
\definecolor{leanbackground}{rgb}{0.965,0.965,0.945}
\lstdefinelanguage{lean}{%
  sensitive=true,
  morecomment=[l]{--},
  morecomment=[s]{/-}{-/},
  morekeywords=[1]{import,open,namespace,section,end,noncomputable,variable,variables,
    theorem,lemma,def,abbrev,example,instance,structure,class,inductive,notation,local,
    by,where,let,have,show,fun,match,with,if,then,else,do,return,in,forall},
  morekeywords=[2]{simp,simpa,rw,erw,dsimp,unfold,ring,ring_nf,nlinarith,linarith,
    positivity,norm_num,field_simp,omega,exact,refine,apply,assumption,intro,intros,
    ext,funext,cases,rcases,obtain,constructor,left,right,classical,by_contra,
    contrapose,convert,using,suffices,calc},
}
\lstdefinestyle{leanpaper}{%
  language=lean,
  columns=fullflexible,
  backgroundcolor=\color{leanbackground},
  commentstyle=\color{leancomment},
  keywordstyle=\color{leankeyword},
  numberstyle=\tiny\color{leannumber},
  basicstyle=\ttfamily\scriptsize,
  breakatwhitespace=false,
  breaklines=true,
  captionpos=b,
  keepspaces=true,
  numbers=left,
  numbersep=5pt,
  showspaces=false,
  showstringspaces=false,
  showtabs=false,
  tabsize=2,
  aboveskip=0.8em,
  belowskip=0.8em,
  literate=
    {\\KField}{{$\Bbbk$}}1
    {ℝ}{{$\mathbb{R}$}}1
    {₀}{{$_0$}}1
    {₁}{{$_1$}}1
    {ₗ}{{$_\ell$}}1
    {Λ}{{$\Lambda$}}1
    {α}{{$\alpha$}}1
    {β}{{$\beta$}}1
    {δ}{{$\delta$}}1
    {→}{{$\to$}}1
    {∘}{{$\circ$}}1
    {∧}{{$\land$}}1
    {∨}{{$\lor$}}1
    {≤}{{$\le$}}1
    {⊆}{{$\subseteq$}}1
    {‖}{{$\Vert$}}1
    {•}{{$\bullet$}}1
    {∃}{{$\exists$}}1
    {∈}{{$\in$}}1
    {∑}{{$\sum$}}1
}
\crefname{lstlisting}{code}{code}
\Crefname{lstlisting}{Code}{Code}
\renewcommand{\lstlistingname}{Code}

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{remark}[theorem]{Remark}

\input{snapshots/provenance_macros.tex}
\input{snapshots/accounting_macros.tex}
\input{snapshots/formalization_theory_macros.tex}
\input{snapshots/formalization_provenance_macros.tex}
\input{snapshots/resource_valuation_macros.tex}

\newcommand{\Lean}{\textsc{Lean}}
\newcommand{\YWS}{Yu--Wang--Samworth}
\newcommand{\DK}{Davis--Kahan}
\newcommand{\TauCeti}{Tau Ceti}

\newcommand{\note}[1]{\textcolor{red}{\textbf{NOTE:} #1}}

\title{Formalizing Yu--Wang--Samworth:\\
Source Auditing, Davis--Kahan, and the Cost of Building\\
Spectral Perturbation Theory in Lean}
% Additional authors are expected; keep the confirmed ordering below stable.
\author{Jonathan Crall\thanks{Kitware, Inc.; \texttt{jon.crall@kitware.com}}
\and Edward Wang\thanks{Johns Hopkins University; \texttt{ewang43@jhu.edu}}}
\date{Draft -- August 2026}

\begin{document}
\maketitle

\begin{abstract}
We report a source-faithful \Lean{} 4 formalization of Yu, Wang, and Samworth's
widely used statistical variant of the Davis--Kahan theorem and of all
\FormalizationDKResultCount{} mathematical results established by Davis and
Kahan in their 1970 paper: the four unnumbered Section~2 theorems and every named
theorem, proposition, lemma, and corollary.  Twenty-eight of these Davis--Kahan
results are proved at the scope stated in the source; Proposition~4.4 is false
as printed and is formally refuted and repaired.  Comparing the formal
statements with the published sources also exposed two printed defects in YWS.

The development required a substantial prerequisite layer.  A compiler-backed
inventory rooted at the formalized YWS and Davis--Kahan results identifies
\FormalizationBasicTheoryCount{} basic theory areas, including principal-angle
and singular-subspace geometry, Borel spectral calculus, unbounded
self-adjoint operator theory, unitarily invariant norms, direct rotations,
operator angles, and Sylvester equations.  We report the scope of these local
theories qualitatively and record formalization provenance so that adapted or
ported material receives explicit credit.

We also preserve lower-bound resource telemetry for the formalization.  At the
pinned analysis snapshot, the retained ledger records at least
\LedgerTurnsScientific{} model turns, \LedgerOutputTokensScientific{} output
tokens, and \LedgerBillableInputTokensScientific{} tokens in its billable-input
accounting measure.  Exact commit-attributed telemetry covers
\ExactMeasuredCommitShare{} of the included primary-repository commits and
\ExactMeasuredLeanLineChurnShare{} of cumulative Lean source-line churn.  These
coverage ratios provide two simple denominators for rough project-scale
extrapolation, while the observed ledger quantities remain the directly measured
lower bounds.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

The Davis--Kahan sine-theta theorem is a standard tool for turning perturbations
of a symmetric operator into perturbation bounds for eigenspaces
\citep{davis1970rotation,chen2021spectral}.  In statistics, the variant of
\citet{yu2015davis} is particularly useful because it replaces an awkward
sample-and-population separation condition with a population eigengap and gives
aligned-frame and singular-subspace forms suited to statistical estimation.
Formalizing these statements at their published scope required spectral
subspaces, principal angles, singular-value comparison, orthogonal alignment,
unitarily invariant norms, and interfaces between finite-dimensional matrix
statements and operator theory.

We formalize the published YWS paper as a source document and develop the
operator-theoretic prerequisites needed to state and prove its results at their
published scope.  The development verifies the statistical perturbation results,
identifies source defects through formal audit, and supplies reusable
spectral-perturbation foundations.

\paragraph{Contributions.}
\begin{enumerate}
  \item Formalization coverage across both papers.  For Davis--Kahan, the
        development covers all \FormalizationDKResultCount{} results established
        in the paper: the four unnumbered Section~2 theorems and every named
        theorem, proposition, lemma, and corollary \citep{davis1970rotation}.
        The formal statements retain the scope asserted by the source, including
        real and complex Hilbert spaces, infinite-dimensional settings,
        unbounded self-adjoint operators where the source extends to them, and
        arbitrary unitarily invariant norms where specified.  Twenty-eight
        results are proved as stated; Proposition~4.4 is formally refuted and
        repaired.  For YWS, the development covers the theorem-level claims of
        Theorems~1--3, including both conclusions of Theorem~2 and the
        right-, left-, and aligned-frame forms of Theorem~3, together with the
        supporting lemmas, identities, equations, examples, and corrections
        tracked in the source audit \citep{yu2015davis}.  Exact source coverage
        is given in \cref{app:source-coverage}.
  \item A formal source audit that exposes and machine-checks two printed YWS
        defects and the failure of Davis--Kahan Proposition~4.4.  The development
        includes corrected YWS statements, an explicit $\mathbb{R}^4$
        counterexample to Proposition~4.4, localization of the failure in its
        printed proof, and a repaired Davis--Kahan result.
  \item A compiler-backed account of the prerequisite formalization,
        classifying project-local named-declaration dependencies into
        \FormalizationBasicTheoryCount{} mathematical theory areas with scoped
        coverage and formalization-provenance assessments.
  \item Resource accounting for the formalization campaign, including observed
        lower-bound model-resolved token measurements, explicit coverage ratios,
        and revisable estimates of API-equivalent cost and operational energy.
\end{enumerate}

\section{Why Formalize Davis--Kahan and YWS?}
\label{sec:why}

\subsection{Statistical importance}

\input{snapshots/openalex_bibliometrics_macros.tex}

% Manually populated.
\newcommand{\GoogleScholarRetrievedDate}{17 August 2026}
\newcommand{\GoogleScholarDavisKahanCitations}{2086}
\newcommand{\GoogleScholarYuWangSamworthCitations}{1067}

\paragraph{Use and continued relevance.}
Both results remain widely used.  A dated OpenAlex snapshot keyed by the
articles' DOIs records \OpenAlexDavisKahanCitations{} citing works for
Davis--Kahan \citep{davis1970rotation} and
\OpenAlexYuWangSamworthCitations{} for Yu, Wang, and Samworth
\citep{yu2015davis} as of \OpenAlexRetrievedDate{}.  As an independent
bibliographic cross-check, Google Scholar reported
\GoogleScholarDavisKahanCitations{} and \GoogleScholarYuWangSamworthCitations{}
citations, respectively, on \GoogleScholarRetrievedDate{}.

\paragraph{Population-gap formulation in statistics.}
Yu, Wang, and Samworth change little notation while changing how the
perturbation bound enters a statistical argument.  In a typical application, $\Sigma$ is a population
matrix determined by the statistical model, while $\widehat{\Sigma}$ is a
random estimator.  Consequently, the population eigengap
\[
  \Delta
  =
  \min\{
    \lambda_{r-1}(\Sigma)-\lambda_r(\Sigma),
    \lambda_s(\Sigma)-\lambda_{s+1}(\Sigma)
  \}
\]
is a deterministic property of the model, whereas gaps involving eigenvalues
of $\widehat{\Sigma}$ are themselves random quantities.  A perturbation theorem
whose separation hypothesis mixes the population and sample spectra therefore
creates an additional probabilistic obligation: before using the eigenspace
bound, one must first prove that the random sample eigenvalues remain suitably
separated.

Theorem~2 of Yu, Wang, and Samworth removes that intermediate dependence.  Once
a population gap $\Delta>0$ is assumed, a bound on
$\widehat{\Sigma}-\Sigma$ can be converted directly into a bound on the
estimated eigenspace.  This gives the modular form routinely needed in
statistical arguments:
\[
  \text{population separation}
  \;+\;
  \text{stochastic matrix-estimation error}
  \quad\Longrightarrow\quad
  \text{eigenspace-estimation error}.
\]
The denominator is supplied by assumptions on the population model, while
  concentration or random-matrix arguments control the numerator.
Introducing a sample-eigengap assumption would change the statistical content
even if the resulting perturbation theorem were valid.  The displayed YWS theorem also depends on a
broader collection of spectral, geometric, and operator-theoretic foundations;
\cref{sec:foundations} summarizes those foundations and their formalization
provenance.

\subsection{Reusable mathematical foundations}

A second motivation comes from the prerequisite structure of the source
results.  Formalizing Davis--Kahan at the breadth of the 1970 paper and YWS at
its statistical scope requires a connected collection of projection geometry,
principal angles, singular-subspace comparison, unitarily invariant norms,
operator ideals, spectral calculus, real/complex transfer, direct rotations,
and Sylvester-type arguments.  The resulting interfaces have scope beyond the
individual source theorems and provide reusable components for later
formalizations in spectral perturbation and operator theory.

The prerequisite work therefore produces outputs beyond the source theorems.  A
later development can inherit definitions, spectral constructions, geometric
lemmas, and proof interfaces whose construction absorbed much of the work here.
\Cref{sec:foundations} gives the compiler-backed inventory and scoped coverage
assessment for these foundations.

\subsection{The Davis--Kahan Sine Theta Theorem in Lean}
\label{sec:dk-headline}

The sine-theta theorem below is one representative result from a complete
formalization of the \FormalizationDKResultCount{} results established in
\citet{davis1970rotation}.  Our counted result set consists of the four
unnumbered Section~2 theorems and every named theorem, proposition, lemma, and
corollary in the paper.  ``Full source scope'' means that the formal theorem
retains the ambient scalar field, dimension, bounded or unbounded operator
setting, and norm class asserted by the corresponding source statement.  The
full result inventory and its machine-readable correspondence are described in
\cref{app:source-coverage}.

The first headline result in Section~2 of \citet{davis1970rotation} is the
sine-theta theorem.  In the paper's coordinate setup, let $E_0$ parameterize a
trial subspace with restricted operator $A_0$, let $F_0$ parameterize the exact
comparison subspace, let $F_1$ parameterize its orthogonal complement with
$AF_1=F_1\Lambda_1$, and let
\[
  R=AE_0-E_0A_0,
  \qquad
  \sin\Theta_0=(I-F_0F_0^*)E_0.
\]
If, for some $\beta\leq\alpha$ and $\delta>0$, either
\[
  \operatorname{spec}(A_0)\subset[\beta,\alpha],
  \qquad
  \operatorname{spec}(\Lambda_1)\cap
    (\beta-\delta,\alpha+\delta)=\varnothing,
\]
or the same condition holds with $A_0$ and $\Lambda_1$ interchanged, then for
every unitarily invariant norm
\begin{equation}
  \delta\,\lVert\sin\Theta_0\rVert\leq\lVert R\rVert.
  \label{eq:dk-sintheta-headline}
\end{equation}
The source states this in the real/complex, infinite-dimensional setting and
allows the relevant self-adjoint operators to be unbounded when the residual is
bounded.  The Lean signature retains that scope.  Readers unfamiliar with Lean
syntax can consult \cref{app:reading-lean} for a short guide to the signatures
shown here.

\begin{lstlisting}[style=leanpaper,
  caption={Lean signature for the Davis--Kahan sine-theta theorem.  The predicate
  \texttt{IsTrialResidual} packages the residual identity
  $R=AE_0-E_0A_0$ with its domain conditions; \texttt{IsExactSpectralDecomposition}
  records the exact spectral subspace, its orthogonal complement, and
  $AF_1=F_1\Lambda_1$; \texttt{N.Mem R} records membership in the norm ideal on
  which \texttt{N.gauge} evaluates the unitarily invariant norm.  Surrounding
  Lean context is omitted.},
  label={lst:dk-sintheta-headline}]
theorem sinTheta_headline
    [ContinuousLinearMap.HasMinMaxLowerBoundEverywhere.{u, v} \KField]
    [HasUnboundedSylvesterKyFan.{u, v} \KField]
    (N : UnitaryInvariantNorm)
    (A : ClosedOperator (\KField := \KField) (E := E))
    (A₀ : ClosedOperator (\KField := \KField) (E := F))
    (Λ₁ : ClosedOperator (\KField := \KField) (E := G))
    (E₀ : F →L[\KField] E)
    (F₀ : H →L[\KField] E)
    (F₁ : G →L[\KField] E)
    (sinTheta₀ : F →L[\KField] E)
    (R : F →L[\KField] E)
    (hSinTheta₀ :
      sinTheta₀ =
        (ContinuousLinearMap.id \KField E - F₀ ∘L F₀.adjoint) ∘L E₀)
    (hA : A.IsSelfAdjoint)
    (hA₀ : A₀.IsSelfAdjoint)
    (hΛ₁ : Λ₁.IsSelfAdjoint)
    (htrial : IsTrialResidual A A₀ E₀ R)
    (hexact : IsExactSpectralDecomposition A Λ₁ F₀ F₁)
    {β α δ : ℝ}
    (hβα : β ≤ α)
    (hδ : 0 < δ)
    (hspectral :
      (LinearPMap.realSpectrum A₀.toLinearPMap ⊆ Set.Icc β α ∧
          LinearPMap.realSpectrum Λ₁.toLinearPMap ⊆
            {x : ℝ | x ≤ β - δ ∨ α + δ ≤ x}) ∨
        (LinearPMap.realSpectrum Λ₁.toLinearPMap ⊆ Set.Icc β α ∧
          LinearPMap.realSpectrum A₀.toLinearPMap ⊆
            {x : ℝ | x ≤ β - δ ∨ α + δ ≤ x}))
    (hR : N.Mem R) :
    δ * N.gauge sinTheta₀ ≤ N.gauge R := by
\end{lstlisting}

\Cref{lst:dk-sintheta-headline} formalizes \cref{eq:dk-sintheta-headline}.
Its equality hypothesis identifies the sine-angle operator with
$(I-F_0F_0^*)E_0$.  The bundled residual and spectral
hypotheses collect the domain conditions needed for unbounded operators, and the
bracketed typeclass arguments provide the generic real/complex analytic
infrastructure used by the proof.

The signature makes the source's implicit norm-ideal membership explicit.

\section{A Source-Faithful Formalization of YWS}
\label{sec:yws}

The YWS development covers the theorem-level claims of Theorems~1--3,
including both conclusions of Theorem~2 and the right-, left-, and aligned-frame
forms of Theorem~3.  It also formalizes the supporting lemmas, identities,
equations, and examples used in the paper's arguments.  Printed defects are
represented by machine-checked counterexamples and corrected statements rather
than by weakening the surrounding mathematical scope.  The exact source
correspondence is recorded in \cref{app:source-coverage}.

\subsection{The population-gap theorem}

Let $\Sigma,\widehat\Sigma\in\mathbb{R}^{p\times p}$ be symmetric, with
population eigenvalues $\lambda_1\geq\cdots\geq\lambda_p$.  For
$1\leq r\leq s\leq p$, let $d=s-r+1$ and let $V,\widehat V\in\mathbb{R}^{p\times d}$
contain orthonormal eigenvectors at the ordered indices $r,\ldots,s$.  Define
\[
  \Delta=
  \min\{\lambda_{r-1}-\lambda_r,\lambda_s-\lambda_{s+1}\},
  \qquad
  \lambda_0=+\infty,\quad \lambda_{p+1}=-\infty.
\]
The main contribution of \citet{yu2015davis}, Theorem~2, states that if
$\Delta>0$, then
\begin{equation}
  \lVert\sin\Theta(\widehat V,V)\rVert_{\mathrm F}
  \leq
  \frac{2\min\{\sqrt d\,\lVert\widehat\Sigma-\Sigma\rVert_{\mathrm{op}},
                    \lVert\widehat\Sigma-\Sigma\rVert_{\mathrm F}\}}
       {\Delta},
  \label{eq:yws-sintheta}
\end{equation}
and there exists $\widehat O\in O(d)$ such that
\begin{equation}
  \lVert\widehat V\widehat O-V\rVert_{\mathrm F}
  \leq
  \frac{2^{3/2}\min\{\sqrt d\,\lVert\widehat\Sigma-\Sigma\rVert_{\mathrm{op}},
                         \lVert\widehat\Sigma-\Sigma\rVert_{\mathrm F}\}}
       {\Delta}.
  \label{eq:yws-aligned}
\end{equation}
Only the population spectrum enters $\Delta$; the supplied sample eigenframe may
therefore be chosen arbitrarily inside a repeated sample eigenspace.

The Lean signature in \cref{lst:yws-theorem2-sintheta} uses the same consecutive
eigenvector block as the paper.  Each frame is represented by an ordered
orthonormal eigenvector family, while a bundled hypothesis encodes the two
population boundary gaps and the endpoint convention.  The left-hand side is
the Frobenius norm of the sine principal angles between the two spanned
subspaces.

\begin{lstlisting}[style=leanpaper,
  caption={Lean signature for the first conclusion of YWS Theorem 2.
  \texttt{Sigma} and \texttt{SigmaHat} are the population and perturbed symmetric
  operators; \texttt{V} and \texttt{Vhat} are their ordered eigenvector blocks.
  \texttt{IsEigenvectorBlock} certifies the corresponding orthonormal
  eigenvector relations, \texttt{SourcePopulationGap} encodes the population
  gap $\Delta$, and \texttt{hSinThetaNorm} identifies \texttt{sinThetaNorm} with
  the Frobenius sine-angle quantity in \cref{eq:yws-sintheta}.  Surrounding Lean
  context is omitted.},
  label={lst:yws-theorem2-sintheta}]
theorem theorem2_sinTheta
    {p d r s : Nat}
    (Sigma SigmaHat :
      EuclideanSpace Real (Fin p) →ₗ[Real] EuclideanSpace Real (Fin p))
    (hSigma : Sigma.IsSymmetric) (hSigmaHat : SigmaHat.IsSymmetric)
    (hr : r ≤ s) (hs : s < p) (hd : d = s - r + 1)
    (V Vhat : Fin d → EuclideanSpace Real (Fin p))
    (hV : IsEigenvectorBlock Sigma hSigma hr hs hd V)
    (hVhat : IsEigenvectorBlock SigmaHat hSigmaHat hr hs hd Vhat)
    (sinThetaNorm : Real)
    (hSinThetaNorm :
      sinThetaNorm =
        sinThetaFrobenius (Submodule.span Real (Set.range V))
          (Submodule.span Real (Set.range Vhat)))
    (Delta : Real) (hDelta : 0 < Delta)
    (hgap : SourcePopulationGap Sigma hSigma r s Delta) :
    sinThetaNorm ≤
      2 * min
        (Real.sqrt d * ‖SigmaHat - Sigma‖_op)
        ‖SigmaHat - Sigma‖_F / Delta := by
\end{lstlisting}

\subsection{Aligned frames and rectangular singular subspaces}

The second conclusion of YWS Theorem~2 gives the aligned-frame bound in
\cref{eq:yws-aligned}, with an orthogonal alignment matrix in $O(d)$.  The
companion Lean signature retains that existential orthogonality condition and
the expanded Frobenius distance; it is given in
\cref{app:yws-aligned-signature}.

The rectangular singular-subspace theorem is derived from the symmetric theory
through the two Gram operators.  Self-adjoint spectral perturbation is applied
to those operators, after which the aligned spectral frames are transported to
the left and right singular spaces.  The reusable Gram, singular-value, and
frame-transport results belong to the singular-subspace theory summarized in
\cref{tab:formalized-theories}.

\section{Formalization as Source Audit}
\label{sec:audit}

Formalization provides a detailed audit of a mathematical source because each
hypothesis, boundary convention, and intermediate identity must support a
kernel-checked derivation.  This can expose defects that remain unobtrusive in
an informal proof: an algebraic identity may fail on a small example, a boundary
convention may collapse an asserted bound, or an extremal claim may conflict
with an explicit finite-dimensional construction.  The YWS and Davis--Kahan
developments produced examples of all three phenomena.

\subsection{Two defects in YWS}

The formalization records source defects explicitly.  First, printed
equation (4) is a false polynomial identity: it is missing a square, and the
repository contains both a concrete counterexample to the printed formula and a
proof of the corrected identity.  Second, the printed convention
$\sigma^2_{\operatorname{rank}(A)+1}:=-\infty$ in Theorem 3 is incorrect at the
rank boundary.  When $s=\operatorname{rank}(A)$ it makes a denominator infinite
and can force a zero upper bound even when the sample and population singular
subspaces are orthogonal.  The intended theorem is repaired by the ambient-index
boundary convention used in the paper's own Gram-matrix proof.

For equation~(4), a two-dimensional numerical example already exposes the
error.  Take
\[
u=(1,0),\qquad v=(3/5,4/5).
\]
Then $\|u\|=\|v\|=1$, $\langle v,u\rangle=3/5$, and
$\|v-u\|^2=4/5$.  The two sides of the identity as printed are therefore
\[
(2\langle v,u\rangle)^2(1-\langle v,u\rangle^2)
   = \frac{576}{625},
\qquad
\frac14\|v-u\|^2
  (2-\|v-u\|^2)
  (4-\|v-u\|^2)
   = \frac{96}{125},
\]
so they are unequal.  The correction is exactly one square:
\begin{equation}
\label{eq:yws-equation4-corrected}
(2\langle v,u\rangle)^2(1-\langle v,u\rangle^2)
=
\frac14\|v-u\|^2
  (2-\|v-u\|^2)^2
  (4-\|v-u\|^2).
\end{equation}
Both the numerical counterexample and the corrected identity for arbitrary
real unit vectors are machine checked.  The corresponding declarations and
source locations are recorded in \cref{app:source-coverage}.

The rank-boundary defect in Theorem~3 also occurs in the smallest nontrivial
dimension.  Let
\[
A=
\begin{pmatrix}
1&0\\0&0
\end{pmatrix},
\qquad
\widehat A=
\begin{pmatrix}
0&0\\0&1
\end{pmatrix}.
\]
Both matrices have rank one.  Taking $r=s=1$, their population and sample top
right singular subspaces are the perpendicular coordinate axes, and hence
\[
\|\sin\Theta(\widehat V,V)\|_F=1.
\]
Under the printed conventions
$\sigma_0^2:=+\infty$ and
$\sigma_{\operatorname{rank}(A)+1}^2:=-\infty$, however, both boundary
separations are infinite:
\[
\sigma_0^2-\sigma_1^2=+\infty,
\qquad
\sigma_1^2-\sigma_2^2=+\infty.
\]
Thus the printed denominator is infinite and its asserted upper bound collapses
to zero, contradicting the displayed value above.  This concrete $2\times2$ example is machine checked; the exact declaration is
listed in \cref{app:source-coverage}.

The correction is to take the boundary in the ambient Gram spectrum, as the
proof of Theorem~3 itself does.  For right singular subspaces of
$A\in\mathbb{R}^{p\times q}$, extend the singular values by
$\sigma_j(A)=0$ for
$\operatorname{rank}(A)<j\le q$ and place the terminal convention at
$\sigma_{q+1}^2:=-\infty$; for left singular subspaces the corresponding
ambient endpoint is $p+1$.  Consequently the theorem uses a finite
$\Delta>0$ satisfying
\[
\Delta
\le
\min\left\{
  \sigma_{r-1}^2-\sigma_r^2,\,
  \sigma_s^2-\sigma_{s+1}^2
\right\},
\]
with the singular values interpreted at the appropriate ambient dimension.
For the example above, the lower boundary is therefore the genuine Gram
eigenvalue gap
\[
\sigma_1^2-\sigma_2^2=1-0=1,
\]
in place of $+\infty$.  The corrected boundary reduction and the corresponding
right- and left-singular-subspace statements are machine checked; exact Lean
names are given in \cref{app:source-coverage}.

\subsection{Davis--Kahan Proposition 4.4 is false as printed}

A separate audit of the 1970 source produced an explicit four-dimensional
counterexample to Proposition 4.4 for the trace norm.  With two principal angles
$\pi/4$, the direct rotation has trace displacement
$4\sqrt{2-\sqrt{2}}$, while an admissible orthogonal competitor has trace
displacement $2\sqrt{2}$.  Thus the claimed minimizing property fails inside the
stated $\Theta\leq\pi/3$ regime.

The same witness also localizes a specific defect in the published proof.
Proposition~4.3 is a separate valid result.  The defective step is the displayed
\emph{equation (4.3)} inside the proof of Proposition~4.4.
Writing $K=I-W$ and taking the two principal-plane projectors
$\Omega_1E=\operatorname{span}\{e_0,e_3\}$ and
$\Omega_2E=\operatorname{span}\{e_1,e_2\}$, the source passes from its valid
Ky Fan variational identity to the asserted lower bound
\[
  \lVert K\rVert_4 \geq
  \lVert K\Omega_1\rVert_2+\lVert K\Omega_2\rVert_2.
\]
For the counterexample, however,
\[
  \lVert K\rVert_4=2\sqrt2
  \quad\text{while}\quad
  \lVert K\Omega_1\rVert_2=\lVert K\Omega_2\rVert_2=2,
\]
so the asserted inequality would require $2\sqrt2\geq4$.  The block values and
the resulting contradiction are machine checked.
The two domain planes are orthogonal, while their images under $K$ overlap;
the corresponding singular-value sums therefore fail to add.  On this witness, the later blockwise calculation where the $\pi/3$ bound
is consumed behaves as claimed; the invalid step is the earlier summation
across blocks.

The strict trace-norm comparison directly falsifies Proposition~4.4,
independently of the diagnosis of its printed proof.  Proposition~4.3 concerns
the squared displacement $(I-W)^*(I-W)$ and remains valid.  The
finite-dimensional formalization proves that result both at the unitarily
invariant norm level and in the source's Ky Fan formulation.  On the counterexample,
the relevant partial sums of squared singular values satisfy the required
domination while the corresponding partial sums of singular values fail it.

The repository also contains a repaired result for the $Q$-norm class.  We
searched for prior treatments of the trace-norm failure inside the advertised
short-angle regime.  Bolotnikov, Li, and Rodman showed by 2001 that canonical
completion can fail for Schatten $p<2$ norms, including the trace norm, but their
explicit negative example is the orthogonal-subspace endpoint $\theta=\pi/2$,
outside the Davis--Kahan hypothesis $\theta_{\max}\leq\pi/3$
\citep{bolotnikov2001minimal}.  Qiu, Zhang, and Li later repeated the
Davis--Kahan short-angle minimality claim \citep{qiu2006grassmann}.  To the best
of our knowledge, no published correction or equivalent counterexample
identifies failure of Proposition~4.4 within its stated real short-angle regime.

\section{Formalizing Missing Foundational Theory}
\label{sec:foundations}
\label{sec:tauceti}

There was a considerable gap between the formal statements required by YWS and
Davis--Kahan and the operator-theoretic infrastructure available in Mathlib.  The
formalization therefore developed substantial prerequisite theory in addition
to the source theorems themselves.  A compiler-backed dependency snapshot rooted
at the formalized YWS and Davis--Kahan results reaches
\FormalizationTraceDeclarationCount{} project-local declarations connected by
\FormalizationTraceEdgeCount{} dependency edges across
\FormalizationTraceModuleCount{} modules.  Because imported theorem proof terms
can be opaque to this environment-level index, these counts give a conservative
lower bound on named declaration dependence.

The dependency extraction and reviewed theory classification are kept separate
and joined programmatically.  The exact result scope, declaration data, and
classification procedure are given in \cref{app:source-coverage}.

\subsection{Basic theory areas}

We summarize the compiler graph by \FormalizationBasicTheoryCount{} mathematical
areas.  The labels are more specific than ``operator theory'' and much coarser
than individual Lean modules.
The scope column describes the local subsystem developed in this repository.
``Broad partial'' indicates work across several aspects of a topic with explicit
remaining gaps; it carries no field-level percentage interpretation.

\begin{table}[H]
\centering
\small
\caption{Basic prerequisite theories in the YWS and Davis--Kahan formalization,
with reviewed qualitative descriptions of local scope.  Source-result
reachability and declaration-level evidence are given in
\cref{tab:formalized-theories-audit}.}
\label{tab:formalized-theories}
\input{snapshots/formalization_basic_theory_table.tex}
\end{table}

The finite-dimensional foundations in \cref{tab:formalized-theories} include
orthogonal projections and principal angles, singular values and Gram operators,
spectral gaps and variational eigenvalue arguments, and residual/Ritz
compression identities.  These are the interfaces closest to
the statistical statements of \citet{yu2015davis}, while also sitting in the
classical perturbation-theory lineage of Davis--Kahan, Wedin, and Stewart--Sun
\citep{davis1970rotation,wedin1972perturbation,stewart1990matrix}.  YWS draws most heavily on projection and principal-angle geometry,
singular-subspace machinery, spectral-gap reasoning, and unitarily invariant
norms.

The full Davis--Kahan development uses a broader range of foundations, including unitarily invariant norm and operator-ideal
machinery, polar decomposition and direct rotations, two-projection geometry,
operator angles and double-angle constructions, and Sylvester/separated-intertwiner
equations.  These topics are already part of the mathematical architecture of the
1970 paper \citep{davis1970rotation}; later operator-theoretic developments provide
useful comparison points for direct-rotation minimality and angle/Sylvester
methods \citep{bolotnikov2001minimal,qiu2006grassmann,grubisic2005sylvester,grubisic2010tan}.

The infinite-dimensional DK results also required a spectral-analysis layer that
is mostly absent from the displayed YWS theorem: projection-valued measures and
bounded Borel functional calculus, closed and unbounded self-adjoint spectral
theory, spectral restrictions and resolvents, and transfer between real and
complex Hilbert spaces.  The classical mathematical reference point is Kato's
operator perturbation theory \citep{kato1976perturbation}.  On the formal side,
Mathlib already supplies a substantial continuous functional-calculus foundation
\citep{mathlib2020,dedecker2025cfc}; the compiler trace here records the additional
project-local interfaces and constructions that the source-faithful DK development
actually depends on.

\subsection{Degree of formalization}

Coverage is described with four scoped labels: \emph{focused reusable slice},
\emph{substantial focused subsystem}, \emph{substantial reusable subsystem}, and
\emph{broad partial development}.  Field-level percentages lack a natural denominator, and declaration counts are
sensitive to API design.  The labels therefore describe the local theory:
a narrow reusable interface, a coherent cluster around one problem, a
substantial independently reusable subsystem, or a broader development with
known gaps.  The declaration graph and module inventory provide quantitative
evidence for those reviewed labels.

Final named-declaration dependence and historical development can differ.  Some
auxiliary results support API construction without remaining on a traced path to
a source result, while a small declaration may be reused by many results.  The
compiler graph records the first quantity conservatively; the scope labels
summarize the second at the level of mathematical theory.  Both layers are
retained so that future evidence can revise the qualitative assessment without
changing the recorded dependency snapshot.

\subsection{Formalization provenance and reuse}
\label{sec:proof-provenance}

We track mathematical provenance and formalization provenance separately.
Literature citations identify the mathematical source of a theorem.  Positive
formal-source citations identify Lean code, constructions, theorem selection,
or proof architecture derived from an existing formal development.  The latter
include documented ancestry in Spectra \citep{bornemann2026spectra} and
route-map or cross-check use of other Lean developments such as
\citet{zhang2026sltrepo}.  A project-local declaration receives project
formalization credit when no positive external formal-provenance citation
applies.  Ordinary Mathlib and Lean dependencies remain credited upstream
\citep{mathlib2020}.  Exact declaration-level claims and evidence locations are given in
\cref{app:source-coverage}.

Within the compiler-reached inventory,
\FormalizationProjectNativeDeclarationCount{} of
\FormalizationTraceDeclarationCount{} declarations have no positive external
formal-donor citation.  Another \FormalizationProjectReferencedDeclarationCount{}
are locally developed declarations whose documentation credits an external
formal development for theorem selection, comparison, or ancestry.  The
remaining \FormalizationExternalOriginDeclarationCount{} are directly
donor-derived at the documented level:
\FormalizationAdaptedDeclarationCount{} adapted or ported declarations and
\FormalizationCopiedDeclarationCount{} copied or essentially verbatim
declarations.  Spectra supplies the positive donor ancestry that survives in
this dependency inventory \citep{bornemann2026spectra}.

\begin{table}[H]
\centering
\small
\caption{Formalization provenance within the compiler-reached project-local
inventory.  Project formalization is the default when no positive external
formal-source citation applies; ordinary Mathlib and Lean dependencies lie
outside this denominator.}
\label{tab:formalization-provenance-credit}
\input{snapshots/formalization_provenance_credit_table.tex}
\end{table}

These counts record formalization provenance.  Originality and declaration-level
token allocation fall outside their interpretation.  Adapted material can require
substantial integration, and locally formalized declarations can encode
mathematics originating in the published literature.  The accounting gives
external formal work explicit credit and assigns uncited project-local
formalization to this project under the stated policy.

The reusable mathematics is being prepared for upstream contribution as a
shared operator-theory foundation, so later formalizations can consume stable
spectral, geometric, and operator-theoretic interfaces.

\section{Resource Accounting}
\label{sec:process-study}

Instrumentation was introduced after the project was underway, and some work
performed through chat interfaces has no equivalent telemetry.  Every observed
token total below is therefore a lower bound on project-wide usage.  The
analysis snapshot is \AccountingSnapshot{}.  Commit-level coverage is computed
on the primary formalization repository, while the measured program ledger also
contains an author-confirmed cross-repository contribution associated with the
Tau Ceti foundations work.  Manuscript-only commits are excluded from the
formalization-history corpus.

Exact commit-attributed telemetry covers \ExactMeasuredCommitCount{} of
\HistoryCommitCount{} included primary-repository commits
(\ExactMeasuredCommitShare{}).  As a text-volume proxy, those commits account
for \ExactMeasuredLeanLineChurn{} of \TotalLeanLineChurn{} cumulative Lean
source-line changes, counting insertions and deletions
(\ExactMeasuredLeanLineChurnShare{}).  The retained ledger also contains
measured session-level usage without commit attribution, so the program-wide
lower bound is larger than the exact primary-repository subset.

Across the allowlisted program repositories, the retained ledger records at
least \LedgerTurns{} model turns, \LedgerOutputTokens{} output tokens, and
\LedgerBillableInputTokens{} tokens in its billable-input accounting measure.
The repository scope, token classes, and model-resolved breakdown are
given in \cref{app:accounting}.

\subsection{Valuation of observed usage}

Applying the dated public API price inputs used by this study gives an
API-equivalent list-price value of \MeasuredApiEquivalentCost{} for the observed
lower-bound usage.  Revaluing all recorded Anthropic cache writes at the
one-hour cache rate gives \MeasuredApiEquivalentCostOneHour{}.

Operational energy requires assumptions about serving throughput, hardware
power, cache work, per-turn overhead, and datacenter power usage effectiveness.
Under the central versioned serving scenario, the observed lower-bound usage
corresponds to \ModeledOperationalEnergy{}.  A separate per-token calculation
gives \PerTokenCrossCheckEnergy{}.  Provider energy meter readings are
unavailable, so both values are order-of-magnitude operational estimates.  The
assumptions remain separate from the measured ledger and can be replaced as
better empirical estimates become available \citep{crall2026llmresource}.

\subsection{Coverage-based project-scale extrapolation}

The exact primary-repository subset contains \ExactPrimaryBillableInputTokens{}
billable-input tokens and \ExactPrimaryOutputTokens{} output tokens; the central
serving model assigns \ExactPrimaryOperationalEnergy{} to that subset.  Scaling
this exact subset by the inverse commit-coverage fraction gives
\CommitScaledBillableInputTokens{} billable-input tokens,
\CommitScaledOutputTokens{} output tokens, and
\CommitScaledOperationalEnergy{}.  Scaling instead by the inverse Lean-line
churn fraction gives \LeanChurnScaledBillableInputTokens{} billable-input tokens,
\LeanChurnScaledOutputTokens{} output tokens, and
\LeanChurnScaledOperationalEnergy{}.

\begin{table}[H]
\centering
\small
\input{snapshots/resource_coverage_extrapolation_table.tex}
\caption{Observed lower bounds and two rough project-scale extrapolations.  The
first column uses all retained program telemetry and remains a lower bound.
The extrapolation columns scale only the exact primary-repository subset by the
inverse of its \ExactMeasuredCommitShare{} commit coverage or
\ExactMeasuredLeanLineChurnShare{} cumulative Lean-line-churn coverage.
Operational energy uses the same central serving assumptions in all three
columns.}
\label{tab:resource-extrapolation}
\end{table}

These scalings assume that token intensity per commit or per changed Lean line
is comparable between observed and unobserved portions of the primary history.
No statistical interval is attached to them.  Their spread gives a direct
sensitivity check on the choice of coverage denominator; the observed column in
\cref{tab:resource-extrapolation} remains the measured lower bound.

\section{Related Work}
\label{sec:related}

Large formal developments provide precedents for source-driven mathematical
verification and reusable library construction, including Flyspeck, the Liquid
Tensor Experiment, and Mathlib itself
\citep{hales2017kepler,scholze2022liquid,mathlib2020}.  LLM-assisted
formalization has progressed from natural-language autoformalization and
sketch-guided proving \citep{wu2022autoformalization,jiang2023dsp} to
long-horizon Lean workflows \citep{zhang2026leanmarathon}.  Recent statistical
formalization develops empirical-process infrastructure in Lean
\citep{zhang2026slt}; its public repository also contains later Davis--Kahan
extensions \citep{zhang2026sltrepo}.

Human use of AI during formalization has also become an object of study
\citep{collins2026humanai}.  The Anthropic Riemann-zeta case study reports
resource usage and multi-agent development traces for a research mathematics
task \citep{anthropic2026zeta}.  Recent formalization agents expose alternative
workflow choices that can be compared experimentally.  MathCode combines a
persistent Lean environment with reusable theorem libraries, planning, and
parallel subgoal work \citep{mathcode2026}; AutoLean provides logged
formalization and proving loops with compiler feedback, semantic evaluation, and
replanning \citep{autolean2026}.  Our resource study contributes a longitudinal
measured-usage record from a source-audited spectral-perturbation formalization.

\section{Conclusion}
\label{sec:conclusion}

The YWS and Davis--Kahan formalization includes the published theorems
and the spectral, geometric, and operator-theoretic interfaces needed to prove
them.  Formal source audit identified defects in both sources, while the
prerequisite development produced reusable foundations for later formal work.
That reusable layer is part of the scientific return from formalizing these
sources at their published breadth.

The resource analysis records substantial computational expenditure for the
observed portion of the project.  Token measurements are stored independently of
commit-level attribution and post-hoc valuation, so revised price or
operational-energy assumptions can be applied to the same ledger as empirical
estimates improve.

The commit-linked portion of the ledger also suggests a direct experiment for
future work.  Selected pre-commit repository states can be paired with the
formalization obligations solved by the corresponding commits and rerun under
alternative agent workflows such as MathCode and AutoLean
\citep{mathcode2026,autolean2026}.  The original commit-associated token usage
provides an imperfect historical baseline.  Comparisons would need to control
model choice, available context, stopping criteria, and semantic acceptance, and
commit attribution alone cannot establish causal per-task cost.  Such a study
could test whether persistent Lean sessions, theorem retrieval, planning and
replanning, or other orchestration choices reduce token and API expenditure for
comparable mathematical coverage and proof quality.

Our hope is that the reusable foundations developed here reduce the work
required by later formalizations.  This project still required substantial
computational expenditure, and the preserved record leaves a concrete open
question: how much of that expenditure can future workflows avoid while reaching
comparable source fidelity, mathematical coverage, and proof checking?

\bibliographystyle{unsrtnat}
\bibliography{references}

\clearpage
\appendix
\input{snapshots/census_macros.tex}

\section{Reading the Lean signatures}
\label{app:reading-lean}

A Lean theorem declaration has the form
\[
  \texttt{theorem name (parameters) (hypotheses) : conclusion := proof}.
\]
Parameters introduce the mathematical objects under discussion.  A hypothesis
such as \texttt{(hA : A.IsSelfAdjoint)} supplies a proof of the proposition
\texttt{A.IsSelfAdjoint}; bundled predicates such as
\texttt{IsTrialResidual} and \texttt{IsExactSpectralDecomposition} collect
several related assumptions behind a single proposition.  Braces mark arguments
that Lean normally infers, while square brackets request typeclass instances
that provide ambient algebraic or analytic structure.  The proposition after
the final colon is the theorem's conclusion; the term after \texttt{:=} is its
proof \citep{demoura2021lean4}.

Lean treats proofs of the same proposition as definitionally equal.  Thus a
hypothesis contributes the fact that its proposition holds, without creating a
mathematical distinction between different proof terms.  This proof-irrelevance
principle is also why later theorems can use a proved proposition without
depending on the internal form of its proof \citep{demoura2021lean4}.

\section{Supplementary YWS declaration}
\label{app:yws-aligned-signature}

The second conclusion of YWS Theorem~2 aligns the estimated and population
frames by an orthogonal matrix.

\begin{lstlisting}[style=leanpaper,
  caption={Lean signature for the aligned-frame conclusion of YWS Theorem 2.
  The predicate \texttt{IsEigenvectorBlock} has the same meaning as in
  \cref{lst:yws-theorem2-sintheta}; the conclusion produces an orthogonal matrix
  and the corresponding Frobenius alignment bound.  Surrounding Lean context is
  omitted.},
  label={lst:yws-theorem2-aligned}]
theorem theorem2_alignedFrame
    {p d r s : Nat}
    (Sigma SigmaHat :
      EuclideanSpace Real (Fin p) →ₗ[Real] EuclideanSpace Real (Fin p))
    (hSigma : Sigma.IsSymmetric) (hSigmaHat : SigmaHat.IsSymmetric)
    (hr : r ≤ s) (hs : s < p) (hd : d = s - r + 1)
    (V Vhat : Fin d → EuclideanSpace Real (Fin p))
    (hV : IsEigenvectorBlock Sigma hSigma hr hs hd V)
    (hVhat : IsEigenvectorBlock SigmaHat hSigmaHat hr hs hd Vhat)
    (Delta : Real) (hDelta : 0 < Delta)
    (hgap : SourcePopulationGap Sigma hSigma r s Delta) :
    ∃ O : Matrix (Fin d) (Fin d) Real,
      O ∈ Matrix.orthogonalGroup (Fin d) Real ∧
        Real.sqrt (∑ i, ‖(∑ j, O j i • Vhat j) - V i‖ ^ 2) ≤
          2 * Real.sqrt 2 *
            min
              (Real.sqrt d * ‖SigmaHat - Sigma‖_op)
              ‖SigmaHat - Sigma‖_F / Delta := by
\end{lstlisting}

\input{appendix_process.tex}

\end{document}
