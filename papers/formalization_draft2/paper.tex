\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs,tabularx,array}
\usepackage[section]{placeins}
\usepackage{float}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,allcolors=blue]{hyperref}
\usepackage{pdfcomment}
\usepackage[nameinlink]{cleveref}
\usepackage{xcolor}
\setlength{\emergencystretch}{3em}

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{remark}[theorem]{Remark}

\input{generated/census_macros.tex}
\input{generated/provenance_macros.tex}
\input{generated/accounting_macros.tex}
\input{generated/interaction_macros.tex}

\newcommand{\Lean}{\textsc{Lean}}
\newcommand{\YWS}{Yu--Wang--Samworth}
\newcommand{\DK}{Davis--Kahan}
\newcommand{\TauCeti}{Tau Ceti}

\title{Formalizing Yu--Wang--Samworth:\\
Source Auditing, Davis--Kahan, and the Cost of Building\\
Spectral Perturbation Theory in Lean}
% Additional authors are expected; keep the confirmed ordering below stable.
\author{Jonathan Crall\thanks{Kitware, Inc.; \texttt{jon.crall@kitware.com}}
\and Edward Wang\thanks{Johns Hopkins University; \texttt{ewang43@jhu.edu}}}
\date{Draft scaffold -- August 2026}

\begin{document}
\maketitle

\begin{abstract}
We report a source-faithful \Lean{} 4 formalization of Yu, Wang, and Samworth's
widely used statistical variant of the Davis--Kahan theorem.  The formalization
uses a paper-keyed source census: a progress-and-audit instrument constructed
from the source rather than from the Lean tree, so that missing formalizations
remain visible.  The current census has \YWSCensusEntryCount{} tracking entries,
covering named results, equations, identities, examples, source conventions,
and explicitly labeled extensions beyond the printed paper.  All
\YWSCensusVerifiedCount{} entries currently resolve to declarations in the
default build; this is a verification statement about the census, not a claim
that YWS contains that many named theorems.  Treating the source as an object to audit rather than
merely a theorem to restate exposed two printed source defects in YWS and, in a
deeper audit of the 1970 source, an explicit finite-dimensional counterexample
to Davis--Kahan Proposition 4.4.

The effort also required a substantial prerequisite layer of operator theory,
principal-angle geometry, singular-value machinery, unitarily invariant norms,
and spectral perturbation infrastructure that was not available at the required
interface in Mathlib.  These results are open source, are reused by a formalized
contemporary statistical theorem family, and are being organized for contribution
through an operator-theory roadmap in \TauCeti{}.

Finally, we study what this development cost and how human--LLM collaboration
actually occurred.  The repository contains a partially observed resource ledger
and retained interaction traces.  At the pinned analysis snapshot, the current
ledger records at least \LedgerTurns{} model turns, \LedgerOutputTokens{} output
tokens, and \LedgerBillableInputTokens{} tokens in its billable-input accounting
measure.  These are strict measured lower bounds: \CommitsWithoutExactAccounting{}
of \HistoryCommitCount{} commits lack exact commit-level accounting, and
\CommitsBeforeInstrumentation{} commits precede the instrumentation commit.
We therefore distinguish measured commit-attributed usage, measured but
commit-unattributed session usage, and genuinely unmeasured work, and we treat
extrapolation as a missing-data problem rather than reporting a single total.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

The Davis--Kahan sine-theta theorem is a standard tool for turning perturbations
of a symmetric operator into perturbation bounds for eigenspaces
\citep{davis1970rotation,chen2021spectral}.  In statistics, the variant of
\citet{yu2015davis} is particularly useful because it replaces an awkward
sample-and-population separation condition with a population eigengap and gives
aligned-frame and singular-subspace forms suited to statistical estimation.
A one-line citation to these results conceals substantial mathematical
infrastructure: spectral subspaces, principal angles, singular-value comparison,
orthogonal alignment, unitarily invariant norms, and the interfaces connecting
finite-dimensional matrix statements to operator theory.

This paper asks what happens when that entire chain is made executable in a
proof assistant.  Our main object is not an isolated convenient ``Davis--Kahan
lemma,'' but the published YWS paper as a source document.  The resulting
formalization serves three roles: a verification of a central statistical
perturbation result, an audit that can expose defects in the literature, and a
reusable mathematical layer for later statistical formalizations.

\paragraph{Contributions.}
The intended contribution list is:
\begin{enumerate}
  \item A source-faithful formalization of YWS at the published theorem numbering
        and generality, tracked by a paper-keyed census whose
        \YWSCensusEntryCount{} entries make absent, corrected, generalized, and
        deliberately extra material visible.
  \item Machine-checked diagnoses and repairs of two printed YWS source defects:
        the false polynomial identity in equation (4) and the rank-boundary
        convention in Theorem 3.
  \item A source audit of \DK{} Proposition 4.4, including an explicit
        $\mathbb{R}^4$ trace-norm counterexample and a repaired theorem surface.
  \item A demonstration that the resulting spectral machinery is reusable in a
        contemporary end-to-end statistical theorem family (Quench), rather than
        being an isolated verification artifact.
  \item A quantitative and qualitative study of the human--LLM development
        process, with explicit treatment of incomplete telemetry, session-level
        backfill, chat-interface work, and extrapolation uncertainty.
\end{enumerate}

\paragraph{Paper thesis.}
The broader claim we aim to support is that source-faithful formalization can be
\emph{executable scholarship for mathematical statistics}: it both verifies and
audits the literature, while converting prerequisite work into an amortizable
formal substrate for subsequent results.

\section{Why Formalize Davis--Kahan and YWS?}
\label{sec:why}

\subsection{Statistical importance}

\textbf{TODO:} quantify citation/use evidence for Davis--Kahan and YWS using a
reproducible bibliometric source appropriate for the venue.  Explain why the
population-gap formulation is the form statisticians routinely need.

\subsection{The visible theorem sits on a missing formal stack}

The practical motivation turned out to be stronger than citation count.  To
state and reuse the desired sine-theta results at paper-level generality, the
development had to construct foundational layers that were absent from the
needed Mathlib interface.  These include, among others, partial-isometry and
polar-decomposition machinery, principal-angle geometry, singular systems,
majorization and Ky Fan tools, unitarily invariant norm interfaces, spectral
subspace APIs, and perturbation lemmas for self-adjoint operators.

\textbf{TODO figure:} dependency DAG from Mathlib $\rightarrow$ foundational
operator theory $\rightarrow$ Davis--Kahan $\rightarrow$ YWS $\rightarrow$
Quench.  The figure should distinguish code newly developed here, pre-existing
Mathlib, and staging work intended for \TauCeti{}.

This is the amortization argument for foundational formalization.  Informally,
a later paper can cite Davis--Kahan in one sentence.  Formally, somebody must
first pay for the interfaces that make that sentence meaningful and reusable.
The cost analysis in \cref{sec:process-study} gives a partially observed empirical
view of that prerequisite burden.

\section{A Source-Faithful Formalization of YWS}
\label{sec:yws}

The package \texttt{FinishYuWangSamworth} is organized around the published
Biometrika numbering rather than around whichever auxiliary theorem was easiest
to expose in Lean.  Formalization progress is tracked by a \emph{paper-keyed
source census}: its rows are created from claims and audit obligations in the
source document, not discovered by enumerating declarations already present in
the Lean tree.  Consequently a missing formalization remains a visible row
instead of disappearing from a coverage report.

The current census contains \YWSCensusEntryCount{} entries.  They are not
\YWSCensusEntryCount{} named theorems: the rows include theorem conclusions,
lemmas, corollaries, equations, identities, examples, a definition, source
scope/convention checks, and exposition/audit material.  It also records
\YWSCensusExplicitAdditionCount{} items explicitly marked as additions beyond
the printed paper, so that repository extensions are not accidentally
attributed to YWS.  All \YWSCensusVerifiedCount{} entries currently have
\texttt{verification=proved\_in\_build}.  The independent census status field
records whether a row matches the source exactly, is generalized or
specialized, is a compiled correction to a defective source statement, or is
not source proof debt.

\begin{table}[t]
\centering
\input{generated/yws_census_kind_table.tex}
\caption{What the \YWSCensusEntryCount{} census entries count.  This is a
progress/audit inventory, not a theorem count.  The complete row-level inventory,
including source anchor, Lean declarations, verification state, and whether an
entry is an explicit non-source addition, is generated as
\texttt{yws\_census\_items.csv}.}
\label{tab:yws-census-kinds}
\end{table}

\subsection{The population-gap theorem}

\textbf{TODO:} state the exact YWS Theorem 2 surface in mathematical notation,
then give the corresponding Lean declaration(s).  Emphasize that the sample
eigenframe remains arbitrary at repeated sample eigenvalues: the formal wrapper
does not silently reintroduce a sample eigengap.

\subsection{Aligned frames and rectangular singular subspaces}

\textbf{TODO:} describe the orthogonal alignment conclusion and the generic
Gram-transport factoring used to obtain the left/right singular-subspace forms.
Explain which parts required new reusable API rather than theorem-specific proof
code.

\subsection{Sharpness as part of source fidelity}

The census also tracks the paper's sharpness constructions against the same
hypotheses as the theorem surfaces.  This matters because source faithfulness
includes claims about constants and dimension dependence, not only upper
bounds.

\textbf{TODO:} select one short sharpness example for the main text and move the
middle-block construction details to an appendix.

\subsection{Proof provenance and borrowed formal ideas}

Source faithfulness applies to the provenance of the formal development as well
as to the theorem statements.  Supporting layers were not all independently
invented in this repository: some code and proof ideas have ancestry in the
Spectra collaboration; other arguments were adapted from, or designed after
reading, external Lean libraries and Mathlib pull requests.  The repository
already records much of this information in module-level provenance blocks and
external-source registries.

The final paper should cite those sources at the mathematical points where their
ideas are used, and should distinguish at least four relationships: copied or
closely adapted code, ported mathematics with substantial API rewriting, a proof
strategy used for an independent re-derivation, and an ordinary upstream
Mathlib dependency.  These are different scholarly claims and should not be
collapsed into a generic acknowledgment.  A paper-local provenance inventory is
regenerated by \path{scripts/build_provenance.py}.

\textbf{TODO:} complete a declaration-level audit for every proof discussed in
the paper and add BibTeX entries for all cited donors before submission.

\section{Formalization as Source Audit}
\label{sec:audit}

\subsection{Two defects in YWS}

The formalization does not silently repair source defects.  First, printed
equation (4) is a false polynomial identity: it is missing a square, and the
repository contains both a concrete counterexample to the printed formula and a
proof of the corrected identity.  Second, the printed convention
$\sigma^2_{\operatorname{rank}(A)+1}:=-\infty$ in Theorem 3 is incorrect at the
rank boundary.  When $s=\operatorname{rank}(A)$ it makes a denominator infinite
and can force a zero upper bound even when the sample and population singular
subspaces are orthogonal.  The intended theorem is repaired by the ambient-index
boundary convention used in the paper's own Gram-matrix proof.

\textbf{TODO:} give minimal numerical counterexamples and exact corrected
statements, with declaration-level references.

\subsection{Davis--Kahan Proposition 4.4 is false as printed}

A separate audit of the 1970 source produced an explicit four-dimensional
counterexample to Proposition 4.4 for the trace norm.  With two principal angles
$\pi/4$, the direct rotation has trace displacement
$4\sqrt{2-\sqrt{2}}$, while an admissible orthogonal competitor has trace
displacement $2\sqrt{2}$.  Thus the claimed minimizing property fails inside the
stated $\Theta\leq\pi/3$ regime.

The repository also contains a repaired result for the $Q$-norm class.  We
separately investigated whether the trace-norm failure inside the advertised
short-angle regime had already been identified in the mathematical literature.
The search, last updated 14 August 2026, included exact theorem-number and
$\pi/3$ queries; direct rotation combined with trace, nuclear, and Schatten-norm
terminology; the concrete angle pattern $(\pi/4,\pi/4)$ versus $(\pi/2,0)$ and
its displacement costs; erratum/correction/counterexample terminology; publisher
records; and citation snowballing.

That search found important nearby prior work but not the same correction.
Bolotnikov, Li, and Rodman showed by 2001 that canonical completion can fail for
Schatten $p<2$ norms, including the trace norm, but their explicit negative
example is the orthogonal-subspace endpoint $\theta=\pi/2$, outside the
Davis--Kahan hypothesis $\theta_{\max}\leq\pi/3$ \citep{bolotnikov2001minimal}.
Qiu, Zhang, and Li later repeated the Davis--Kahan short-angle minimality claim
rather than flagging it as false \citep{qiu2006grassmann}.  We also found no
SIAM-recorded correction attached to the original article.  We therefore do
not make an unconditional priority claim: the narrower historical conclusion is
that we found no indexed or widely recognized correction, or equivalent
counterexample, showing failure of Proposition~4.4 within its stated real
short-angle regime.  Non-digitized material, subscriber-only citation databases,
and private correspondence remain limitations, and this search should be
repeated immediately before submission.  The standalone technical note and
historical-search supplement preserve the fuller semantic-alignment and search
audit.

\section{Reuse: the Quench Formalization}
\label{sec:quench}

The spectral layer is already consumed by the formalization of a 2026
query-efficiency theorem family \citep{helm2026query}.  The Quench package
contains paper-facing Theorem 2 statements, spectral/CMDS alignment bridges,
finite and asymptotic query-efficiency variants, and explicit evaluation bridges.

The role of this section is not to make Quench a second main theorem.  It is an
integration test for the central methodological claim: once the foundational
spectral stack exists, a new statistical formalization can import it rather than
rebuild it.

\textbf{TODO:} isolate the exact dependency path through
\path{DkpsQuench2026/Geometry/AlignedCMDS.lean} and quantify which YWS/DK
interfaces are reused directly.

\section{Resource and Interaction Study}
\label{sec:process-study}

The mathematical development was unusually well instrumented, but not from its
beginning and not in every interface.  We therefore treat resource accounting as
a partially observed historical process rather than as a single project bill.
The analysis snapshot is \AccountingSnapshot{}.  Commit-level coverage is
computed on the primary formalization repository, while measured token totals
also include an author-confirmed cross-repository contribution associated with
the Tau Ceti foundations work.  Commits that touch only
\path{papers/formalization_draft2/} are excluded from the formalization-history
corpus, preventing the production of this manuscript from being charged back to
the development it studies.

At this snapshot, \ExactMeasuredCommitCount{} of \HistoryCommitCount{} included
primary-repository commits have exact commit-level telemetry.  The program-wide
ledger nevertheless provides a strict measured lower bound of \LedgerTurns{}
model turns, \LedgerOutputTokens{} output tokens, and
\LedgerBillableInputTokens{} tokens in its billable-input accounting measure.
The larger program total includes \LedgerRepositoryCount{} explicitly allowlisted
ledger repositories; the commit-coverage denominator does not pretend that the
other repository's Git history is present in this checkout.  Measured-but-
unallocated \texttt{pending@...} segments and genuinely unmeasured work are kept
as separate evidence classes.  Detailed scope, reconciliation, and missing-data
methods are given in \cref{app:accounting}.

\subsection{Tokens must be resolved by model before pricing}

Token count alone is not a cost measure.  The ledger records an exact per-model
vector for ordinary input, cache writes, cache reads, and output, so measured
usage can be priced model-by-model once historically applicable rates are
verified.  The analysis checks that every per-model vector sums to its row-level
total and never applies one global average token price.

\begin{table}[t]
\centering
\small
\input{generated/model_token_table.tex}
\caption{Exactly measured program-wide tokens by model and token class at the
\AccountingSnapshot{} snapshot.  Monetary cost is computed only after
historically applicable model- and token-class-specific rates are supplied.}
\label{tab:model-tokens}
\end{table}

Git co-author trailers provide a second, independent provenance signal.  We keep
raw trailer labels, exact ledger model attribution, and study-specific
normalizations separate.  In particular, an author-confirmed historical rule
maps unledgered GPT-5.6 ``High'' and ``Thinking'' chat-interface labels to
GPT-5.6 Sol only within the recorded study interval; the rule matched
\BoundedGPTChatRuleCommitCount{} commits in this snapshot and is explicitly
bounded so that it cannot silently classify future history.  Exact ledger
attribution takes precedence.  Anthropic trailer/ledger differences are treated
as review signals rather than errors because model switches within a session can
leave a commit trailer that does not describe every model used in the preceding
work.  The full reconciliation is in \cref{app:model-provenance}.

\subsection{Human steering is a secondary empirical object}

The retained interaction corpus contains \InteractionPromptCount{} deduplicated
human prompts across \InteractionSessionCount{} sessions and
\InteractionActiveDays{} active days.  These traces support analyses of
intervention type, interruption, coordination, and the evolution of project
management tools such as the paper-keyed source census.  They also expose an
important provenance limitation: retrospective author memory does not reliably
recover every operational request made during a long-running agentic project.
Where contemporaneous traces survive, we therefore privilege them over later
recollection when reconstructing who requested or elaborated a tool.  The
quantitative taxonomy, the census chronology, and the ``interaction archaeology''
case study are intentionally developed in \cref{app:interactions}; much of that
material is appendix/cut material unless it changes the interpretation of the
mathematical results.

\section{From Project Infrastructure to Shared Infrastructure}
\label{sec:tauceti}

The project currently stages substantial reusable operator-theory code under
\texttt{ForTauCeti/}.  The intended endpoint is not a private theorem stack but a
shared operator-theory substrate organized against the \TauCeti{} roadmap.

\textbf{TODO:} describe the contribution status accurately at submission time:
which modules have merely been staged, which have open upstream PRs, and which
have landed.  Do not phrase planned contributions as completed upstream work.

This section should connect the empirical ``we paid the price'' observation to
its mathematical payoff: the foundational cost becomes valuable precisely when
future formalizations can import these results without repeating the same
interface-building work.

\section{Related Work}
\label{sec:related}

There is rapidly growing work on LLM-assisted theorem proving, document-scale
autoformalization, formalization of research mathematics, and human interaction
with proof assistants.  There are also increasingly large formal developments
in statistics.  The closest methodological comparison for the process-analysis
portion is the public Anthropic Riemann-zeta case study \citep{anthropic2026zeta},
which reports resource usage and multi-agent development traces for a research
mathematics task.

\textbf{TODO: fresh literature search immediately before submission.}  The
novelty claim should not be ``LLMs can formalize a paper'' or ``first
Davis--Kahan theorem in Lean.''  The intended distinction is the combination of
source-faithful YWS coverage, mathematical corrections discovered by audit,
reusable downstream statistical use, and unusually granular evidence about the
human--AI process required to obtain the formalization.

\section{Limitations}
\label{sec:limitations}

\begin{itemize}
  \item Resource accounting begins after substantial development, and transcript
        retention limits how much of the earlier period can be reconstructed.
  \item Chat-interface work is not recoverable from the local harness ledger;
        historical attribution depends on separate evidence and author review.
        Missing chat logs also limit reconstruction of initiative and steering,
        not merely token totals.
  \item Pending transcript spans can be broad.  Timestamp overlap identifies
        candidate commits but does not justify a per-commit token allocation.
  \item Missingness is not random, so extrapolated totals are model-dependent and
        should be presented with sensitivity analyses rather than as ground truth.
        Some model variants have no direct measured calibration in the ledger.
  \item Dollar costs depend on historically applicable model- and token-class
        rates.  Measured token counts remain primary evidence; monetary totals
        should be reported only with sourced rates and effective dates.
  \item Path-based component labels overlap and are only a first approximation
        to mathematical dependency-level cost attribution.
  \item Formal verification establishes the Lean statement.  Claims that a
        formal result corrects a prose source require a separate semantic-alignment
        argument, which we make explicit in the YWS and Davis--Kahan audits.
  \item The AI-formalization literature is changing rapidly; related-work and
        priority claims need a final contemporaneous search.
\end{itemize}

\section{Conclusion}
\label{sec:conclusion}

The paper's intended conclusion is not that Davis--Kahan can be ``prompted into
existence.''  The development history instead shows the cost of making a
classical statistical citation executable at source-level fidelity.  That cost
produced more than sine-theta theorem statements: it exposed defects in the
literature, forced foundational mathematical interfaces into existence, enabled
a downstream statistical formalization, and created open-source infrastructure
that can be amortized by later work.

The accounting analysis complements the mathematical contribution by making the
development process falsifiable and inspectable.  Where telemetry exists we
report measurements; where it is only session-level we preserve that ambiguity;
and where it is missing we model it explicitly as missing data rather than
retroactively treating an incomplete ledger as a complete project bill.

\appendix
\input{appendix_process.tex}

\begin{thebibliography}{9}

\bibitem[Davis and Kahan(1970)]{davis1970rotation}
Chandler Davis and W.~M. Kahan.
\newblock The rotation of eigenvectors by a perturbation. III.
\newblock \emph{SIAM Journal on Numerical Analysis}, 7(1):1--46, 1970.

\bibitem[Yu et~al.(2015)Yu, Wang, and Samworth]{yu2015davis}
Yi Yu, Tengyao Wang, and Richard~J. Samworth.
\newblock A useful variant of the Davis--Kahan theorem for statisticians.
\newblock \emph{Biometrika}, 102(2):315--323, 2015.

\bibitem[Chen et~al.(2021)Chen, Chi, Fan, and Ma]{chen2021spectral}
Yuxin Chen, Yuejie Chi, Jianqing Fan, and Cong Ma.
\newblock Spectral methods for data science: A statistical perspective.
\newblock \emph{Foundations and Trends in Machine Learning}, 14(5):566--806, 2021.

\bibitem[de~Moura et~al.(2015)de~Moura, Kong, Avigad, van Doorn, and von Raumer]{demoura2015lean}
Leonardo de~Moura, Soonho Kong, Jeremy Avigad, Floris van Doorn, and Jakob von Raumer.
\newblock The Lean theorem prover.
\newblock In \emph{Automated Deduction -- CADE-25}, 2015.

\bibitem[Mathlib Community(2020)]{mathlib2020}
The Mathlib Community.
\newblock The Lean mathematical library.
\newblock In \emph{Certified Programs and Proofs (CPP)}, pages 367--381, 2020.

\bibitem[Helm et~al.(2026)Helm, Johnson, and Priebe]{helm2026query}
Hayden Helm, Ben Johnson, and Carey~E. Priebe.
\newblock Query-efficient model evaluation using cached responses.
\newblock arXiv:2605.07096, 2026.

\bibitem[Bolotnikov et~al.(2001)Bolotnikov, Li, and Rodman]{bolotnikov2001minimal}
Vladimir Bolotnikov, Chi-Kwong Li, and Leiba Rodman.
\newblock Minimal distortion problems for classes of unitary matrices.
\newblock \emph{Electronic Journal of Linear Algebra}, 8:26--46, 2001.

\bibitem[Qiu et~al.(2005)Qiu, Zhang, and Li]{qiu2006grassmann}
Li Qiu, Yanxia Zhang, and Chi-Kwong Li.
\newblock Unitarily invariant metrics on the Grassmann space.
\newblock \emph{SIAM Journal on Matrix Analysis and Applications}, 27(2):507--531, 2005; published online 2006.

\bibitem[Anthropic(2026)]{anthropic2026zeta}
Anthropic.
\newblock Advancing mathematics with the Riemann zeta function.
\newblock Research report / web publication, 2026. Bibliographic metadata to recheck before submission.

\end{thebibliography}

\end{document}
